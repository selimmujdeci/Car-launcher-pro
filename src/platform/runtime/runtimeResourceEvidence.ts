/** ARCH-01/F7.1 — read-only adapter over AdaptiveRuntimeManager evidence. */
import { runtimeManager, type WorkerStatusRow } from '../../core/runtime/AdaptiveRuntimeManager';
import type { ResourceClass, ResourceDecision, ResourceDimension } from './runtimeResourceGovernance';
export interface ResourceEvidenceSnapshot { readonly runtimeMode: string; readonly thermalTier: number | 'UNKNOWN'; readonly effectiveCeiling: string | null; readonly memoryPressure: 'UNKNOWN' | 'MODERATE' | 'CRITICAL'; readonly workers: readonly WorkerStatusRow[]; readonly workerSaturation: 'AVAILABLE' | 'BUSY' | 'SATURATED' | 'UNKNOWN'; readonly tasks: readonly ResourceTaskProjection[]; readonly provenance: readonly string[]; }
export interface ResourceTaskProjection { readonly taskId: string; readonly resourceClass: ResourceClass; readonly dimensions: readonly ResourceDimension[]; readonly requestedBudget: number | null; readonly grantedBudget: number | null; readonly actualCounter: number | null; readonly decision: ResourceDecision; readonly deferCount: number | null; readonly preemptible: boolean | null; readonly resumable: boolean | null; readonly starvationRisk: boolean | null; readonly reason: string; readonly provenance: readonly string[]; }
export function readRuntimeResourceEvidence(): ResourceEvidenceSnapshot { const d = runtimeManager.getResourceDiagnostics(); const workers = d.workers; const active = workers.filter((x) => x.status === 'active').length; const capacity = workers.length; return Object.freeze({ runtimeMode: String(d.mode), thermalTier: d.observedThermalTier, effectiveCeiling: d.effectivePowerCeiling ? String(d.effectivePowerCeiling) : null, memoryPressure: d.memoryPressure, workers, workerSaturation: capacity === 0 ? 'UNKNOWN' : active >= capacity ? 'SATURATED' : active > 0 ? 'BUSY' : 'AVAILABLE', tasks: d.tasks.map((x) => ({ taskId: x.taskId, resourceClass: 'UNKNOWN' as ResourceClass, dimensions: [] as ResourceDimension[], requestedBudget: x.requestedBudget, grantedBudget: x.grantedBudget, actualCounter: x.runCount, decision: 'UNKNOWN' as ResourceDecision, deferCount: x.deferCount, preemptible: null, resumable: null, starvationRisk: null, reason: 'AdaptiveRuntimeManager does not expose a decision counter.', provenance: d.provenance })), provenance: d.provenance }); }
