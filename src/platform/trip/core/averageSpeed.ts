/**
 * averageSpeed — yolculuk ortalama hızının TEK tanımı.
 *
 * Tanımın kendisi saf oturum modelinde (`tripSessionModel`, hiçbir şey içe
 * aktarmama kilidi) yaşar; diğer tüketiciler buradan alır.
 */
export { averageSpeedKmh, AVG_SPEED_MIN_TIME_MS } from './tripSessionModel';
