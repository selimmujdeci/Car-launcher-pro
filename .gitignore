# Environment — API keys (never commit)
.env
.env.local
.env.*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Android keystore (sensitive — never commit)
*.jks
*.jsk
*.keystore

# Next.js build output
website/.next/
website/out/
website/website/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Claude Code session artifacts
.claude/*.lock
.claude/scheduled_tasks.lock

# Screenshots / local captures
Ekran görüntüsü*.png
Ekran g*.png

# Tool specific
.opencode/
.claude/settings.json
scratchpad.md

# ── Repo temizliği (2026-06-06) — build çıktıları, artefaktlar, hassas dosyalar ──

# Android imza anahtarı (HASSAS — asla commit etme)
/android/app/carospro-key

# Derlenmiş paketler (build çıktısı)
*.apk
*.aab

# Android build çıktı dizinleri
/android/app/build/
/android/build/
/android/app/release/

# Üretilmiş test artefaktları (kaynak değil)
test-artifacts/

# Playwright koşu artefaktları (her e2e koşusunda üretilir; kaynak değil)
test-results/
playwright-report/

# TypeScript incremental build cache (üretilmiş — tsc --build çıktısı)
*.tsbuildinfo

# Deno lock — Supabase edge function testleri (deno test) üretir; ana proje Deno değil
deno.lock

# Git worktree'leri (paralel checkout; reponun parçası değil)
.worktrees/

# Ekran görüntüleri ve geçici yakalama/analiz çıktıları
/notch*.png
/faz3-screens/

# Vosk offline STT modeli (57 MB binary) — git'e ALINMAZ.
# scripts/fetch-vosk-model.mjs ile indirilir, scripts/vosk-model-tr.sha256 ile doğrulanır.
# Bkz. docs/VOSK_MODEL_SETUP.md
/android/app/src/main/assets/vosk-model-tr/

# Teşhis çıktıları (üretilmiş artefakt — diag-restart.ps1 üretir; kaynak değil)
/tools/diag-output/

# Kişisel dev yardımcıları — gömülü mutlak yol; ürün/build'in parçası değil
/apk-server.cjs
/apk-share-server.cjs
/hz-notch.mjs

# Üretilmiş dosya envanteri (Get-ChildItem | Export-Csv çıktısı; kaynak değil)
/inventory.csv

# Üretilmiş sunum/pazarlama binary'leri (build_deck.cjs çıktısı; kaynak değil)
*.pptx

