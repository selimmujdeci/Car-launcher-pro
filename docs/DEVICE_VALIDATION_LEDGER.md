# 🔴🟢 SAHA DOĞRULAMA KÜTÜĞÜ — Device Validation Ledger

> **Bu kütük anayasanın "real-device kanıtı" invaryantının somut takibidir**
> (bkz. `CLAUDE.md` → Controlled Evolution, `AI.md` → REAL DEVICE REQUIRED).
>
> **Kural:** Eklenen hiçbir özellik — test yeşil + tsc temiz olsa bile — **gerçek
> araçta kanıtlanana kadar "başarılı" sayılmaz.** Her yeni özellik önce
> **🔴 CİHAZDA TEST EDİLMEDİ** bölümüne, net bir **kabul ölçütü** ile girer.
> Cihazda o ölçüt gözlemlenince **🟢 CİHAZDA DOĞRULANDI**'ya taşınır. Cihazda
> denenip başarısız olursa **❌ TEST EDİLDİ / DÜŞTÜ**'ye geçer (geri dönüş gerekli).
>
> "Build başarısı kanıt değildir. Gerçek davranış kullanıcı isteğiyle eşleşmeli."

---

## 🔴 CİHAZDA TEST EDİLMEDİ (bekliyor)

| # | Özellik | Nerede | Kabul ölçütü (cihazda ne gözlemlenmeli) | Eklendi |
|---|---------|--------|------------------------------------------|---------|
| 65 | **🔴 Device Validation Pending — Native Handshake + Supported PID Discovery (W5-OBD-PR1, PR #88)** — `performHandshake()` Android'de implement edildi (ÖNCEDEN YOKTU → guard hep `false` → el sıkışması gerçek araçta HİÇ çalışmıyordu; VIN okunmuyor, bitmap keşfi yapılamıyordu). Native `ElmProtocol.performHandshakeRaw` VIN (`0902`) + supported-PID bitmap zincirini (`0100→0120…01A0`, süreklilik-bit'li, desteklenmeyen blok sorulmaz) HAM döner; TS `buildHandshakeResult` çok-bloklu parse (PID 1–192) + `refinePidList` capability-güdümlü poll (zero-trust/fail-soft; PID 2F yakıt bitmap kanıtıyla oto-aktive, statik kara liste kalktı); timeout türleri (NO DATA/TIMEOUT/UNSUPPORTED) ayrı loglanır. | `ElmProtocol.performHandshakeRaw` · `OBD/BleObdManager.performHandshake` · `CarLauncherPlugin.performHandshake` · `core/val/OBDHandshake.ts` · `platform/obdPidConfig.refinePidList` · `platform/obdService.ts` | Gerçek araçta bağlantı sonrası `[OBD:Handshake]` logu VIN (destekleniyorsa 17-char) + `bloklar=` + `desteklenen=N PID` göstermeli; 2F destekleyen araçta bir sonraki reconnect'te yakıt PID poll'a GİRMELİ; desteklenmeyen PID poll'dan DÜŞMELİ; mevcut poll zinciri (RPM/hız/coolant) REGRESYONSUZ kalmalı. **🔴→🟡 KISMİ CİHAZ KANITI (2026-07-14, Redmi 23090RA98I + BLE OBD 00:10:CC:4F:36:03, gerçek araç):** `_hexTokens` compact-hex düzeltmesi (#89) kullanıcının aktif branch'ine (ana çalışma ağacı) UYGULANDI — önceden yalnız `.worktrees/hextokens`'teydi, kullanıcının derlediği APK bozuk parser taşıyordu → veri akmıyordu (kök). Taze APK (`apk:safe` + gradle assembleDebug, buildDir=C:/Temp/carlauncher) kurulup çalıştırıldı. 🟢 **BLE çift yönlü akış:** logcat GATT writeCharacteristic(len=5, ELM komutları) + notification(opcode 0x1b) → adaptör bağlı, veri akıyor. 🟢 **CANLI TELEMETRİ EKRANDA (screenshot kanıtı):** HIZ=15 km/h · DEVİR(RPM)=905(rölanti) · MOTOR(coolant)=80°C · MENZİL=278 km + yakıt barı dolu (~4 çubuk) → hız/RPM/coolant/yakıt gerçek araçtan akıyor, parser PID keşfini yapıyor. Full suite 3849 yeşil (yeni `obdHandshakeHexTokens.regression.test.ts` 15 senaryo, gerçek RAW_HANDSHAKE.json fixture → 19 PID/2F destekli), tsc temiz. **HÂLÂ 🔴 (tam 🟢 için):** `[OBD:Handshake]` JS console log satırı ayrıca yakalanmadı (Capacitor debug'da logcat'e forward kapalı — CDP gerekir); `desteklenen=N PID` sayısı + 2F'nin reconnect'te poll'a girişi + desteklenmeyenin düşüşü CANLI log'dan teyit edilmeli. | 2026-07-13 |
| 64 | **🔴 Device Validation Pending — Deep Scan Offline Pass Trigger + Runtime Wiring + Minimal Diagnostics (W5-3b)** — W5-3a'daki `runOfflinePass()` İLK kez üretim wiring'ine bağlandı. `platformCoreDeepScanWiring`'e tek deterministik giriş noktası `triggerDeepScanOfflinePass()` (hash/dedup guard + single-flight) + iptal (`cancelDeepScanOfflinePass`) + bounded teşhis (`getDeepScanOfflinePassStatus`) eklendi; SystemBoot **Wave 3**'te (Wave 2 ownership KURULDUKTAN SONRA) fire-and-forget çağrılır (`void ... .catch`; boot BLOKLANMAZ). **GUARD:** aynı guard anahtarı (varsayılan `deepscan.offline.boot`) EN FAZLA bir kez pass başlatır (duplicate YOK); yürüyen pass varken ikinci tetik reddedilir (single-flight); wiring restart'ında guard doğal sıfırlanır (durum wiring örneğine bağlı). **DAVRANIŞ DEĞİŞMEZ:** handler GEÇİLMEZ → tüm offline fazlar `skipped` → GERÇEK İŞ YAPILMAZ; change detection / knowledge / fingerprint / evidence / report ÇALIŞMAZ; aktif ECU/PID/DID/Firmware sorgusu YOK (runtime aktif-kayıt API'leri hiç çağrılmaz — gerçek zincir testiyle kanıtlı); runtime `idle → running → idle` döner (pass sonunda `runtime.reset()` W5-3a garantisi). **OWNERSHIP KORUNUR:** `OwnedOrchestrator` yüzeyi offline `runOfflinePass`/`cancelOfflinePass`'i gösterir ama AKTİF tarama API'lerini (`start`/`run`/`runNextPhase`) ASLA göstermez (compile-time; W5-3a kilidi bu davranışa GÜNCELLENDİ, kaldırılmadı). **TEŞHİS (bounded + immutable):** present/started/running/active/cancelled/triggerCount/lastRun/lastDuration/lastResult/lastReason/lastError/phaseCount/summaryAvailable — hepsi primitive/enum/sayı; VIN/GPS/secret/ham CAN-OBD/guard-anahtarı TAŞIMAZ. Event Bus / Capability Registry / HAL / Assistant / Driver DNA / Prediction / SQL / Supabase / Native / CAN / OBD DEĞİŞMEDİ (wiring bunları import ETMEZ). **HOST-DOĞRULANDI:** yeni trigger-wiring testleri (25+ senaryo: trigger/duplicate/hash-guard/tek-pass/single-flight/idle→running→idle/reset/summary/immutable/bounded/dispose/restart/cancel/timeout-iletimi/handler-yok/aktif-discovery-yok/privacy/input-immutable/Wave-sırası/EventBus-Capability değişmedi); W5-1 wiring fake'i + W5-3a 2 kilit yeni davranışa güncellendi; tam suite **3820 yeşil (210 dosya)**, tsc `-b --noEmit` temiz, lint 0 error, production build OK. **AÇIKÇA:** (a) gerçek handler YOK; (b) gerçek discovery YOK; (c) **W5-3c değer üreten (offline handler) PR'dır**; (d) aktif ECU sorgusu YOK (tip + faz + aktif-kayıt kapısı + handler-yok ile garanti); (e) Mali-400 doğrulaması bekliyor. | `platform/system/platformCoreDeepScanWiring.ts` (OwnedOrchestrator offline yüzey + `triggerDeepScanOfflinePass`/`cancelDeepScanOfflinePass`/`getDeepScanOfflinePassStatus` + bounded status), `platform/system/SystemBoot.ts` (Wave 3 trigger, fire-and-forget), `__tests__/deepScanOfflineTriggerWiring.test.ts` (yeni), `__tests__/platformCoreDeepScanWiring.test.ts` (fake offline yüzey), `__tests__/deepScanOfflinePass.test.ts` (2 kapsam kilidi güncellendi) | Gerçek cihazda (Xiaomi 23090RA98I + en az bir low-tier K24/Mali-400/T507): (1) boot'ta trigger TEK kez çalışır (`getDeepScanOfflinePassStatus().triggerCount=1`, duplicate boot/HMR çoğaltmıyor); (2) `runOfflinePass` çağrıldı ama araç OBD/CAN trafiğinde deep-scan kaynaklı ECU/PID/DID/Firmware isteği YOK (handler yok → aktif sorgu üretilmiyor); (3) pass sonrası runtime `idle` (`summaryAvailable=true`, `lastResult='ran'`); gerçek tarama sonradan başlayabiliyor; (4) boot süresi regresyonu yok (fire-and-forget → boot bloklanmıyor; hedef boot delta <10ms); (5) **Mali-400/K24 steady-state ek yük ölçüm gürültüsü seviyesinde** (pass tek-atış; timer/polling yok → ≈0); (6) background/foreground / restart'ta çift-pass veya leak yok; (7) teşhis raporunda offline pass status okunabilir (ADB'siz) — ⚠️ Not: `getDeepScanOfflinePassStatus` henüz `diagnosticSections`'a EKLENMEDİ (ayrı küçük teşhis PR'ı gerekir). Cihaz kanıtı olmadan 🟢 YAPMA. ⚠️ **Mali-400 doğrulaması bekliyor.** | 2026-07-13 |
| 63 | **🔴 Device Validation Pending — Tanı Raporu Sanitize Sertleştirme (Diagnostics PR-2)** — PR-1'in (#80) açtığı denetimde ortaya çıkan ÜÇ sanitize boşluğu kapatıldı. **(1) `selfTest` bölümü sanitize'ı TAMAMEN atlıyordu:** `reportSelfTestSnapshot` raporu ham spread ediyordu (`{...base, selfTest}`) → deny-list, VIN/MAC/koordinat/api_key maskeleri ve derinlik tavanı bu bölüme UYGULANMIYORDU. Prob `detail` alanları SERBEST METİN taşır: ham `Error.message` (`selfTestEngine` runProbe catch), ham fetch hata metni (probeBackend) ve stack karesi/dosya yolu (probeIdleRenderLoop) → **maskesiz kanal**. Artık inspector ile AYNI kapıdan geçer: yeni `_sanitizeSection()` (tek kapı — yeni bölüm eklenince sanitize'ı atlamak imkânsız). Teşhis değeri KAYBOLMAZ: stack karesi (`FullMapView.tsx:814`) okunur kalır, yalnız VIN/MAC/koordinat/anahtar maskelenir. **(2) `DENY_KEYS` camelCase'e KÖRDÜ:** eşleşme `key.toLowerCase()` idi, set'te `'api_key'` vardı → `apiKey`.toLowerCase() = `'apikey'` ≠ `'api_key'` → **SIZIYORDU**. Anahtar artık NORMALİZE edilir (küçült + alfanümerik olmayanı at) ve **TAM EŞLEŞME** yapılır. Liste genişledi: `apikey · accesstoken · refreshtoken · authorization · bearer · secret · password · jwt · token · email` (+ mevcut konum/VIN/MAC/telefon). **⚠️ TAM eşleşme ZORUNLU — substring/prefix YASAK:** `lat` substring olsaydı `platform` (W4E runtime teşhis bölümünün TAMAMI) ve `detectedPlatform` düşerdi; `vin` prefix olsaydı `vinMasked` (VIN sızıntısını ÖNLEMEK için üretilen maskeli türev) silinirdi → koruma raporu boşaltırdı. False-positive taraması: 14 üretici dosyada 326 benzersiz anahtar tarandı, **meşru alan düşmüyor**. **(3) Cycle guard YOKTU:** özyinelemeyi sınırlayan tek şey MAX_DEPTH'ti. Eklenen guard **ANCESTOR-PATH** desenidir (girerken ekle, `finally` ile çıkarken sil) — naif "görülen nesneler" seti PAYLAŞILAN REFERANSI (aynı nesne iki KARDEŞ dalda = DAG) cycle sanıp ikinci dalı sessizce boşaltırdı (DTC `[null,null]` ile aynı sınıf sessiz kayıp). Cycle → `[CYCLE]` işareti (sessiz kayıp DEĞİL, görünür); dizi elemanıysa ELENMEZ (uzunluk korunur). **Public API artık THROW ETMEZ:** getter/Proxy patlarsa yalnız o DÜĞÜM `[UNREADABLE]` olur, kardeş alanlar akar (`_safeSection`'ın düğüm-bazlı karşılığı) → tek zehirli alan TÜM tanı raporunu öldüremez. **DEĞİŞMEYEN (bilinçli):** MAX_DEPTH=6 · MAX_ARRAY_LEN=20 · MAX_STR_LEN=2048 · mevcut MASKS (VIN/MAC/COORD/api_key=/token=) · UI · RPC · DB/migration · rıza · teslimat modeli (`'sent'` yalanı AYRI PR). **HOST-DOĞRULANDI:** 17 yeni davranış kilidi + 4 kaynak-metin kilidi (regresyon kasası; PR-1'in dizi kilidi kaldırılmadı, yeni doğru davranışa GÜNCELLENDİ), tam suite **3711 yeşil (206 dosya)**, tsc temiz, lint 0 error, build OK. **BYTE ÖLÇÜMÜ (en zengin fixture, aynı test, önce/sonra):** `self_test` **5157 B → 5119 B (−38 B)** — sanitize maskeleme sonrası hafif KÜÇÜLDÜ, şişme YOK; `dev_inspector` **5479 B → 5479 B (0)** — inspector zaten aynı kapıdan geçiyordu. | `platform/remoteLogService.ts` (`_normalizeKey` + genişletilmiş DENY_KEYS + ancestor-path cycle guard + düğüm-bazlı fail-soft + `_sanitizeSection` tek kapı; `reportSelfTestSnapshot` + `reportDiagnosticSnapshot` onu kullanır), `__tests__/diagnosticSanitizeHardening.test.ts` (yeni, 17 test), `__tests__/regression.guards.test.ts` (4 kilit; dizi kilidi güncellendi) | Gerçek cihazda: (1) **"Tanı Robotu" (self-test) raporu gönderiliyor** ve gövdesinde `selfTest.results[].detail` alanları MEVCUT (bölüm kaybolmadı, sanitize onu boşaltmadı); (2) `selfTest.summary`/`worst`/`env` dolu ve `results.length` = summary toplamı (MAX_ARRAY_LEN=20 kırpması YOK — bugün 14 prob, 6 pay var); (3) gerçek self-test raporunda **ham `Error.message`/fetch hata metni içinde VIN/MAC/koordinat/anahtar İZİ YOK**, maskeler (`[VIN]`/`[MAC]`/`[COORD]`/`api_key=[MASKED]`) görünüyorsa doğru çalışıyor; (4) `platform` (Event Bus/HAL sayaçları) ve `vidMirror.vehicle.vinMasked` bölümleri raporda **HÂLÂ VAR** (yeni deny listesi meşru alanları düşürmedi — TAM eşleşme kanıtı); (5) `triage` mevcut ve `ruleErrors=0`; (6) DTC varken `obdDeep.dtc.codes[0].code` dolu (PR-1 kazanımı bozulmadı); (7) raporda `[CYCLE]`/`[UNREADABLE]` işareti **görünmüyor** (üretimde dairesel/zehirli düğüm yok — görünürse üretici tarafta bug var demektir, teşhis edilir); (8) rapor gövdesinde `truncated` alanı YOK ve `octet_length` < 45 KB; (9) low-tier cihazda (K24/Mali-400 veya T507) self-test raporu gönderimi başarılı, ANR/donma yok; (10) Xiaomi'de CPU/RAM/boot regresyonu yok. **Gerçek cihazda self-test raporu görülmeden 🟢 YAPMA** — host testleri kodu kanıtlar, sahadaki gerçek `detail` metinlerini kanıtlamaz. | 2026-07-13 |
| 62 | **🔴 Device Validation Pending — Tanı Raporu DTC Derinliği + Triyaj Null-Guard (Diagnostics PR-1)** — Denetimin (2026-07-12) KANITLI iki P0'ı kapatıldı. **(1) DTC kodları kabloda `null` gidiyordu:** `remoteLogService._deepSanitize` derinlik ≥ `MAX_DEPTH` olan KABI düşürür; gövde `depth=0` ile çağrıldığı için zincir `root(0)→obdDeep(1)→dtc(2)→codes[](3)→kod nesnesi(4)` **derinlik 4'te ölüyordu** → `codes:[null,null]`. Aynı derinlikteki `obdDeep.extended.samples[]` (katalog-dışı PID keşfi), `uiActivity.recent[].reasons[]`, `inspector.timeline[].signals/env` ve `inspector.runtime.workers[]` de düşüyordu. **`MAX_DEPTH` 4 → 6 (ÖLÇÜLMÜŞ, keyfi değil):** payload gövdesinin tüm bölümleri tarandı, en derin **kap** derinlik 4'te → 5 = minimum yeterli; 6 seçildi çünkü `sanitizeForRemote` ve inspector girişleri `depth=1`'den başlıyor (bir seviye pay) ve ölçülen payload'da derinlik ≥5'te KAP OLMADIĞI için **bugün 0 ek bayt** akıtır. Sanitize dizi dalı artık düşen elemanı **ELER** (`.map` ile yerinde bırakılan `undefined` JSON'da `null` oluyordu — sessiz "kod var ama boş" yalanı). **(2) DTC varken TRİYAJ TAMAMEN kayboluyordu:** `ruleObdDtc` null-guard'sız `c.code` okuyordu → TypeError → `buildTriageSnapshot` döngüsü kırılıyor → `_attachTriage`'ın yutucu catch'i `payload.triage`'ı HİÇ yazmıyor → admin **arızanın olduğu tam anda** "kritik bulgu yok" görüyordu (13 kuralın HEPSİ birden susuyordu: termal/bellek/güç/GPS/self-test dahil). Kural null-guard'landı (`dtc.count` "arıza var" kanıtıdır; kod METNİ yalnız zenginleştirme → liste boş/bozuk olsa da bulgu ÜRETİLİR) **+ motor seviyesinde KURAL İZOLASYONU** (bir kural patlarsa yalnız o kural düşer, diğerleri çalışır) **+ `ruleErrors` sayacı** (izolasyon sessiz olmasın — triyaj eksik çalıştıysa raporda görünür). **DEĞİŞMEYEN (bilinçli):** redaction/allowlist/deny-list/maskeler · `MAX_ARRAY_LEN`/`MAX_STR_LEN` · UI · RPC · DB/migration · rıza/önizleme · teslimat modeli (`'sent'` yalanı **PR-2'ye** bırakıldı). **HOST-DOĞRULANDI:** 13 yeni kilit yeşil (9 davranış + 4 kaynak-metin regresyon kasası), tam suite **3690 yeşil (205 dosya)**, tsc temiz, lint 0 error, build OK. **BYTE ÖLÇÜMÜ (en zengin fixture, aynı test, önce/sonra):** `support_snapshot` **3163 B → 4045 B (+882 B, +%27,9)** · `dev_inspector` **3747 B → 5480 B (+1733 B, +%46,3)** → ikisi de canlı sunucu tavanının (**64 KB**, migration 027) çok altında. | `platform/remoteLogService.ts` (MAX_DEPTH 4→6 + dizi dalı undefined filtresi), `platform/diagnosticTriage.ts` (ruleObdDtc null-guard + kural izolasyonu + `ruleErrors`), `__tests__/diagnosticDtcDepthTriage.test.ts` (yeni, 9 test), `__tests__/regression.guards.test.ts` (4 kaynak-metin kilidi) | **Gerçek araçta, DTC VARKEN** (arıza lambası yanan / kasıtlı DTC üretilmiş araç) gönderilen `support_snapshot` gövdesinde: (1) `obdDeep.dtc.codes[0].code` **dolu string** (`null` DEĞİL) ve `severity`/`system` dolu; (2) `codes` dizisinde **hiç `null` eleman yok**; (3) `payload.triage` **MEVCUT** ve `findings` içinde **`OBD_DTC_PRESENT`** bulgusu var (kritik DTC varsa `severity:'critical'`); (4) `triage.ruleErrors = 0`; (5) DTC varken diğer triyaj bulguları (termal/güç/GPS) **da** üretilmeye devam ediyor; (6) `obdDeep.extended.samples[0].pid` dolu (katalog-dışı PID keşfi artık sunucuya ulaşıyor); (7) Dev Inspector raporunda `inspector.timeline[0].signals.spd/rpm` **dolu**; (8) raporda VIN/MAC/koordinat/secret **YOK** (maskeler delinmedi); (9) gerçek rapor gövdesi **`truncated` alanı taşımıyor** ve `octet_length` < 45 KB (64 KB tavanı aşılırsa sunucu gövdeyi `{truncated:true}` kabuğuyla DEĞİŞTİRİR = %100 sessiz kayıp, üstelik UI yine "gönderildi" der — PR-2 konusu); (10) low-tier cihazda (K24/Mali-400 veya T507) rapor gönderimi başarılı, ANR/donma yok. **Araç + DTC olmadan 🟢 YAPMA** — host testleri kodu kanıtlar, gerçek DTC akışını kanıtlamaz. | 2026-07-13 |
| 61 | **🔴 Device Validation Pending — Deep Scan Offline Run Surface + Guard Band (W5-3a)** — Deep Scan Orchestrator'a tip-seviyesinde ayrılmış, SONLANDIRMAYAN offline yürütme yüzeyi `runOfflinePass()` eklendi. YALNIZ `OFFLINE_PHASE_SEQUENCE` fazlarını (capability/fingerprint/knowledge/evidence/change_detection/report) deterministik sırayla, kontak BİLİNMİYORKEN (`ignitionConfirmed=null`) yürütür — çünkü offline fazlar araca sorgu göndermez. **KORUMA BANDI (tip + çalışma-zamanı):** handler haritası `Partial<Record<OfflinePhase, PhaseHandler>>` → aktif faz (`vehicle_identity`/`protocol_detection`/`ecu_discovery`/`standard_pid_discovery`/`manufacturer_did_discovery`/`firmware_inventory`) anahtarı DERLEME HATASIDIR; `OfflinePhase = Exclude<DeepScanPhase, ActivePhase>` türetildiği için yeni faz sessizce sınıflandırılamaz. **YAPMAZ:** `_finalize()`/`completeScan()`/`persistence.completeScan()` ÇAĞIRMAZ → `hasCompletedFullScan`/`completedScanCount`/`changeCheckCount` DEĞİŞMEZ; persistence'a HİÇ YAZMAZ (yalnız mod kararı için `hasCompletedFullScan()` OKUR); `_applyResult()` (aktif-kayıt yolu) ÇAĞIRMAZ → keşif payload'ı (`ecus/pids/dids/firmware`) runtime aktif-kayıt API'lerine (`recordEcuDiscovery` vb.) GİTMEZ (o API'ler kontak yokken `waiting_for_ignition`'a düşürürdü); kontak-serbest tek yazma yolu `recordChangeDetection` (yalnız `changedFirmware/changedEcu`). **YAŞAM DÖNGÜSÜ:** pass sonunda `runtime.reset()` ZORUNLU (`try/finally`) → runtime `idle`, gerçek tarama sonradan başlayabilir, ikinci pass tekrar çalışır. **FAIL-SOFT:** handler throw/timeout(5sn varsayılan)/cancel tek fazı düşürür, KALAN offline fazlar DEVAM EDER; runtime `failed`'a düşmez; timeout timer'ı `finally`'de temizlenir (zero-leak). Bounded+immutable özet (sayımlar + faz sonuçları + sanitize uyarılar; VIN/koordinat/ham hex/secret/parmak-izi hash TAŞIMAZ). Ownership wiring yüzeyi (`OwnedOrchestrator`) DEĞİŞMEDİ → hâlâ yalnız `getSnapshot/dispose/isDisposed`; `start()/run()/runNextPhase()/runOfflinePass()` GÖSTERMEZ. SystemBoot/Event Bus/Capability/Native/OBD/CAN/SQL/Supabase DEĞİŞMEDİ. **HOST-DOĞRULANDI:** 32 yeni test yeşil; deep scan suite 177 yeşil; tam suite 3709 yeşil (205 dosya); tsc `-b --noEmit` temiz (aktif-handler `@ts-expect-error` compile-lock doğrulandı); lint 0 error (W5-3a dosyaları uyarısız); production build OK. **AÇIKÇA:** (a) Bu PR hiçbir yerden çağrılmıyor — üretim runtime davranışı DEĞİŞMİYOR. (b) W5-3b Trigger AYRI PR. (c) W5-3c gerçek offline Handler'lar AYRI PR. (d) Aktif ECU/PID/DID/Firmware sorgusunun gitmemesi tip (`OfflinePhase`) + faz sınırı (`OFFLINE_PHASE_SEQUENCE`) + aktif-kayıt kapısı ile GARANTİ ediliyor. | `platform/deepScan/deepScanOrchestrator.ts` (+304: `runOfflinePass`/`cancelOfflinePass`/guard band), `platform/deepScan/deepScanModel.ts` (`ActivePhase`/`OfflinePhase`/`OFFLINE_PHASE_SEQUENCE`/`isOfflinePhase`), `platform/deepScan/index.ts` (tip export), `__tests__/deepScanOfflinePass.test.ts` (yeni, 32 test) | Gerçek cihazda (Xiaomi 23090RA98I + en az bir low-tier K24/Mali-400/T507) — W5-3b Trigger bağlandıktan SONRA: (1) `runOfflinePass()` kontak `null` iken offline fazları yürütür, araç OBD/CAN trafiğinde deep-scan kaynaklı ECU/PID/DID/Firmware isteği YOK (aktif sorgu üretilmiyor); (2) pass sonrası runtime `idle`, `hasCompletedFullScan` false, `completedScanCount`/`changeCheckCount` artmadı; (3) gerçek tarama pass'ten sonra normal başlayabiliyor (idle guard'ına takılmıyor); (4) handler throw/timeout/cancel'da çökme/ANR yok, tek faz düşse kalanlar sürüyor; (5) **Mali-400/K24'te steady-state ek yük ölçüm gürültüsü seviyesinde** (timer/polling yok → ≈0; pass tek-atış); (6) background/foreground geçişinde leak/çift-pass yok. Cihaz kanıtı olmadan 🟢 YAPMA. ⚠️ **Mali-400 doğrulaması bekliyor.** ⚠️ Not: gözlemlenebilirlik dönen özet üzerinden; teşhis raporuna alan EKLENMEDİ (ayrı küçük PR). | 2026-07-13 |
| 60 | **🔴 Device Validation Pending — Deep Scan Runtime Ownership Wiring (W5-1)** — Deep Scan foundation singleton'ları (runtime/persistence/ignition, PASİF idi) İLK kez üretim yaşam döngüsüne bağlandı: yeni `platformCoreDeepScanWiring` bir `DeepScanOrchestrator`'ı DI fabrikasıyla (`createDeepScanOrchestrator`, HANDLER YOK) OLUŞTURUR ve SAHİPLENİR; paylaşılan runtime/persistence/ignition singleton'larını yalnız REFERANS alır. SystemBoot Wave 2'de Capability bridge'den SONRA, SystemOrchestrator'dan ÖNCE additive `_reg` (LIFO: orchestrator önce dispose; paylaşılan singleton'lar DISPOSE EDİLMEZ). **YALNIZ OWNERSHIP:** `start()`/`run()`/`runNextPhase()` ÇAĞRILMAZ → tarama başlamaz, faz yürümez, aktif ECU/PID/DID sorgusu YOK. Event Bus publish/Capability güncelleme/Assistant/handler/native/OBD/CAN/SQL/UI — HİÇBİRİ. **FAIL-CLOSED:** ignition kaynağı yok → `ignitionConfirmed=null` (RPM/voltaj/OBD'den kontak ÇIKARSANMAZ); aktif fazlar ileride tarama başlatılırsa `waiting_for_ignition`'da bloke. Bounded immutable status (present/started/runtimeState/scanState/ignitionConfirmed/mode/currentPhase/progress/warningCount/hasHandlers=false/activeHandlerCount=0/lastErrorCode/lastTransitionAt — VIN/koordinat/ham CAN-OBD/secret YOK). Tek-instance guard; import yan etkisiz; timer/polling/run-loop YOK → pasif steady-state CPU≈0. **HOST-DOĞRULANDI:** 38 yeni test yeşil; deep scan foundation kilidi W5-1'e GÜNCELLENDİ (SystemBoot ownership bağlar ama tarama başlatmaz — CLAUDE.md regresyon-kasası: kaldırılmadı, güncellendi); tam suite 3677 yeşil (204 dosya), tsc temiz, lint 0 error, build OK. | `platform/system/platformCoreDeepScanWiring.ts` (yeni), `platform/system/SystemBoot.ts` (Wave 2 kayıt), `__tests__/platformCoreDeepScanWiring.test.ts` (yeni, 38 test), `__tests__/deepScanRuntimeService.test.ts` (foundation kilidi güncellendi) | Gerçek cihazda (Xiaomi 23090RA98I + en az bir low-tier K24/Mali-400/T507): (1) boot'ta TEK orchestrator instance (`getDeepScanWiringStatus().present=true`, duplicate boot çoğaltmıyor); (2) runtime/persistence/ignition doğru bağlanıyor; (3) **scan otomatik BAŞLAMIYOR** (`scanState=idle`, `runtimeState=idle`, progress=0); (4) **aktif araç sorgusu ÜRETİLMİYOR** (OBD/CAN trafiğinde deep-scan kaynaklı ECU/PID/DID isteği YOK); (5) `ignitionConfirmed=null` fail-closed kalıyor; (6) shutdown sonrası leak yok (orchestrator disposed, paylaşılan singleton'lar ayakta); (7) Xiaomi'de boot/CPU/RAM regresyonu yok (boot delta <20ms hedefi); (8) **Mali-400/K24'te steady-state ek yük ölçüm gürültüsü seviyesinde** (timer/polling yok → ≈0); (9) W5-2 Event Bus bridge, W5-3 offline run, W5-4 capability evidence, W5-5 aktif discovery AYRI PR olarak kalıyor. Cihaz kanıtı olmadan 🟢 YAPMA. ⚠️ Not: `getDeepScanWiringStatus` henüz teşhis raporuna EKLENMEDİ → ADB'siz okumak için küçük teşhis PR'ı gerekir. | 2026-07-12 |
| 59 | **🔴 Device Validation Pending — Capability → Event Bus Bridge Runtime Wiring (W4)** — Mevcut `CapabilityEventBridge` foundation'ı (PR #54) İLK kez gerçek `capabilityRegistry` singleton'ına ve W3'ün TEK aktif `appEventBus`'ına bağlandı (W4C'nin Vehicle HAL eşleniği). Zincir: `capabilityRegistry` → `CapabilityEventBridge` → `getAppEventBus()`. SystemBoot Wave 2'de Capability Registry wiring'inden SONRA additive `_reg` kaydı (LIFO: bridge önce, registry/bus sonra dispose; savunmacı try/catch fail-soft). **YAYINLANAN EVENT'LER:** `capability.record.registered/changed/removed` + `capability.snapshot.changed` (RETAINED, küçük payload {revision, sayımlar, changedIds bounded} — full registry TAŞINMAZ). İmza-dedup (aynı record → event yok); domain=`capability`, source=`capability_registry`. İKİNCİ bus YARATILMAZ (getAppEventBus tüketilir). Deep Scan/Driver DNA/Prediction/Assistant Context/Kernel/tüketici migrasyonu YOK; bus'a ABONE YOK; yeni timer/polling YOK; throttle YOK (capability değişimleri NADİR → hot-path yok, Mali-400 riski YOK); import yan etkisiz; tek-instance guard; Registry+Bus PAYLAŞILAN → cleanup dispose ETMEZ. **HOST-DOĞRULANDI:** 36 yeni test yeşil, tam suite 3639 yeşil (203 dosya), tsc temiz, lint 0 error, build OK. | `platform/system/platformCoreCapabilityBridgeWiring.ts` (yeni), `platform/system/SystemBoot.ts` (Wave 2 kayıt), `__tests__/platformCoreCapabilityBridgeWiring.test.ts` (yeni, 36 test) | Gerçek cihazda (Xiaomi 23090RA98I + en az bir low-tier K24/Mali-400/T507): (1) boot'ta TEK Event Bus instance (`getEventBusStatus().present=true`, duplicate yok); (2) HAL + Capability bridge'leri TEK kez başlıyor (`getCapabilityBridgeStatus().started=true`, `getVehicleHalBridgeStatus().started=true`); (3) capability değişimleri doğru event'lerle ulaşıyor (`capability.snapshot.changed` retained); (4) kaynak olmayan capability için sahte available/event YOK; (5) 3 Hz HAL sinyalleri history/RAM şişirmiyor (`vehicle.signal.changed` transient — W4C davranışı korunur); (6) background/foreground sonrası bridge subscription çoğalmıyor; (7) Xiaomi'de CPU/RAM regresyonu yok; (8) **Mali-400/K24 event CPU bütçesi <%2**; (9) Deep Scan/Driver DNA/Prediction/Assistant Context ayrı PR olarak kalıyor. Cihaz kanıtı olmadan 🟢 YAPMA. ⚠️ Not: bridge sayaçları (`getCapabilityBridgeStatus`) henüz teşhis raporuna EKLENMEDİ → ADB'siz okumak için küçük teşhis PR'ı gerekir. | 2026-07-12 |
| 58 | **🔴 Device Validation Pending — Capability Registry Runtime Wiring (W3)** — Daha önce foundation olan Capability Provider Adapter + runtime provider'lar İLK kez gerçek `capabilityRegistry` singleton'ına SALT-OKUNUR AYNA modunda bağlandı. Zincir: `createRuntimeCapabilityProviders({env:{navigator,deviceTier}})` → `createCapabilityProviderAdapter({registry,providers})` → `capabilityRegistry`. SystemBoot Wave 2'de additive `_reg` kaydı (LIFO; savunmacı try/catch fail-soft), Vehicle store'a BAĞIMSIZ. **EN KÜÇÜK GÜVENLİ KAPSAM:** yalnız yan-etkisiz browser-API + deviceTier kanıtı (device.gps/microphone/bluetooth/wifi/cellular, navigation.gps); probe-tabanlı provider'lar (secureStorage/AI BYOK/modül readiness/offline map-routing) BİLİNÇLİ bağlanmadı → Registry'de unknown kalır (zero-trust, dürüst boşluk; gerçek probe'lar ayrı PR). Event Bus/Deep Scan/Platform Kernel/tüketici migrasyonu YOK; yeni timer/polling YOK; import yan etkisiz; tek-instance guard; dedup; O(1); Registry PAYLAŞILAN singleton → cleanup dispose ETMEZ. **HOST-DOĞRULANDI:** 32 yeni test yeşil, tam suite 3603 yeşil (202 dosya), tsc temiz, lint 0 error, build OK. | `platform/system/platformCoreCapabilityWiring.ts` (yeni), `platform/system/SystemBoot.ts` (Wave 2 kayıt), `__tests__/platformCoreCapabilityWiring.test.ts` (yeni, 32 test) | Gerçek cihazda (Xiaomi 23090RA98I + en az bir low-tier K24/Mali-400/T507): (1) boot'ta `getPlatformCoreCapabilityWiringStatus()` `started=true`, `providerCount>0`, `resolvedCount>0` (teşhis raporuna bounded alan eklenince ADB'siz okunabilir — ayrı küçük PR); (2) `device.gps`/`navigation.gps` Registry'de `available/degraded` çözülüyor; (3) probe'suz capability'ler (ai.*, platform.*) `unknown` KALIYOR (sahte available YOK); (4) boot/CPU/RAM/FPS regresyonu yok; (5) idle'da yeni sürekli yük yok (timer/polling yok); (6) background/foreground geçişinde çökme yok. Cihaz kanıtı olmadan 🟢 YAPMA. ⚠️ Not: `resolvedCount` şu an teşhis raporuna EKLENMEDİ (diagnosticSections bu PR kapsamı dışı — W4E precedent'i gibi ayrı) → cihazda okumak için küçük teşhis PR'ı gerekir. | 2026-07-12 |
| 56 | **🔴 Device Validation Pending — Source Health → HAL Fail-Closed Consumption (PR-2)** — PR-1'in taşıdığı `halStatusStore.sourceHealth` (worker 1 Hz watchdog: CAN 3s/OBD 5s/GPS 5s) artık HAL veri akışında TÜKETİLİYOR. Zincir: worker → SOURCE_HEALTH → halStatusStore → **UnifiedVehicleStoreProvider snapshot** → adapter → HAL → bridge → appEventBus. **Bu, SESSİZ ECU FRAME KAYBI blokajını kapatır:** kablo takılı/port açık olsa bile (native `canStatus` `connected:true` der) worker frame sessizliğini görür → CAN-özel sinyaller fail-closed olur. **MODEL:** provider snapshot'a bounded `sourceHealth {can,obd,gps,ts}` eklendi (yapısal DI — provider/adapter store'ları DOĞRUDAN import ETMEZ); **araç + sağlık store abonelikleri TEK unsub'a sarılır** → teşhisteki `activeSubscriptionCount=1` invaryantı KORUNUR. **UNKNOWN ≠ DEAD:** `null` (worker hiç bildirmedi) → SAHTE disconnect YOK, davranış W4B ile aynı; `false` → kaynak KESİN ölü. **SİNYAL-KAYNAK MATRİSİ (testle kilitli):** CAN-ÖZEL (`coolant_temp · oil_temp · throttle · battery_voltage · gear · ambient_temp · tpms · door_state · parking_brake`) → `can=false` ise `source:none`/`supported=false`/`value=null`/`quality=unknown`/`confidence=0`; FÜZYON (`speed · rpm · fuel_level · odometer · reverse`) → CAN ölse de OBD/GPS/worker beslerse SUPPORTED kalır (yalnız tüm kaynaklar gidince düşer). `rpm`'in CAN fallback'i (`canRpm`) CAN ölüyken **bayat sayılır ve KULLANILMAZ**. `ignition` hâlâ ÜRETİLMEZ. **DİRİLTME YASAĞI:** `can=true` TEK BAŞINA eski değeri supported yapmaz — ölüm anındaki değer saklanır; sinyal ancak **değer DEĞİŞİNCE** (= taze frame kanıtı) gerçek source/quality/confidence ile geri döner. **BATCH:** bir geçişte düşen/dönen tüm sinyaller TEK `hal.ingest(batch)` → TEK HAL emit (W4A korunur); aynı durum duplicate ingest/emit üretmez. **BRIDGE:** tüm destek kalkınca `vehicle.connection.changed=false`, kısmi kayıpta üretilmez, reconnect'te (gerçek veriyle) `true`. **TEŞHİS:** support_snapshot'a bounded `sourceHealth {can,obd,gps,lastChangeAt}` eklendi (null≠false ayrımı korunur; sinyal değeri/CAN frame/VIN/koordinat/payload YOK). **YENİ TIMER YOK.** ✅ **BACKGROUND SAHTE-ÖLÜ KAPISI (bu PR'da EKLENDİ — merge blokajı kaldırıldı):** kök neden — watchdog canlılığı monotonik saat farkıyla ölçer (`_alive()`, `VehicleCompute.worker.ts:409-411`); bu saat arka planda İLERLER ama WebView timer'ı kısılır ve adaptör frame'leri durur → foreground dönüşündeki ilk tikte `now - lastSeen ≫ timeout` → kaynak SAĞLAMKEN `false`. Düzeltme: yeni bounded `VISIBILITY {visible:boolean}` worker kontrol mesajı (ana thread `visibilitychange` → `VehicleSignalResolver`, **TEK dinleyici, stop()'ta sökülür, duplicate durum POSTLANMAZ**) + saf `sourceHealthGate` (timer/DOM/store YOK, zaman enjekte edilir): **hidden → sağlık GEÇİŞİ DONDURULUR** (`lastSeen` BOZULMAZ), **foreground → timeout SAATİ yeniden tabanlanır** (arka planda geçen süre timeout'a YAZILMAZ): dönüşten sonra taze frame → `true`; görünür zamanda gerçek timeout dolarsa → `false` (**gerçek foreground kaybı MASKELENMEZ**); henüz ikisi de değilse KARAR YOK → son bilinen değer korunur (yoksa unknown). Kapı YALNIZ SOURCE_HEALTH kararını etkiler — fusion/reverse/SAB ham `_alive()` kullanmaya devam eder; **timer sayısı DEĞİŞMEDİ (4)**; ölçümsüz "N ardışık tur" eşiği YOK. | `platform/vehicleDataLayer/sourceHealthGate.ts` (yeni, saf), `platform/vehicleDataLayer/VehicleCompute.worker.ts` (VISIBILITY + kapı), `platform/vehicleDataLayer/VehicleSignalResolver.ts` (visibilitychange kanalı), `__tests__/sourceHealthVisibilityGating.test.ts` (yeni, 27 test), `platform/vehicleHal/providers/unifiedVehicleStoreProvider.ts`, `platform/vehicleHal/vehicleHalProviderAdapter.ts`, `platform/system/platformCoreVehicleHalWiring.ts` (health store DI), `platform/vehicleHal/providers/index.ts` (tip export), `platform/diagnosticSections.ts` (bounded sourceHealth), `__tests__/vehicleHalSourceHealthConsumption.test.ts` (yeni, 25 test), `__tests__/platformRuntimeDiagnostics.test.ts` + `__tests__/workerSourceHealthTransport.test.ts` (kilitler güncellendi) | Gerçek cihazda/araçta: (1) worker sourceHealth adapter'a ULAŞIYOR (Tanı raporunda `sourceHealth.gps=true` gibi gerçek değer); (2) **CAN canlıyken health=true**; (3) **ECU/kontak kapatılıp kablo bağlı kalınca ≤3-5 sn içinde `can=false`** ve **yalnız CAN-özel sinyaller** unsupported; (4) GPS/OBD fallback sinyalleri SUPPORTED kalıyor; (5) tüm destek kalkarsa `connection.changed=false`; (6) frame dönünce `can=true` ve **yalnız yeni veriyle** supported/`connection=true`; (7) disconnect TEK batch/TEK emit, duplicate event yok, `halBridge.droppedCount=0`; (8) UNKNOWN'da sahte disconnect yok; (9) **background/foreground geçişinde yanlış-pozitif `can=false` ölçülür**; (10) Xiaomi'de boot/CPU/RAM/FPS regresyonu yok; (11) **en az bir low-tier cihazda (K24/Mali-400 veya T507)** teşhis raporundan health + bridge sayaçları doğrulanır. Araç ve low-tier doğrulanmadan 🟢 YAPMA. ⚠️ **KISMİ CİHAZ KANITI (2026-07-12, Xiaomi 23090RA98I — ARAÇ/OBD YOK; ölçüm mevcut "Tanı Gönder" akışıyla, uygulama koduna enstrümantasyon EKLENMEDEN):** 🟢 **Worker sağlığı adapter/teşhis zincirine ULAŞIYOR:** raporda `sourceHealth{can:false, obd:false, gps:true, lastChangeAt:3171}` (araç yok → CAN/OBD ölü, GPS canlı — doğru). 🟢 **Sahte disconnect YOK:** `halWiring.ingestedSignalCount=3` (DEĞİŞMEDİ) → `can=false` hiçbir sinyali düşürmedi (CAN-özel sinyaller zaten hiç aktarılmamıştı = unknown; fail-closed yalnız DAHA ÖNCE aktarılmışa uygulanır). 🟢 **TEK abonelik korunuyor:** `activeSubscriptionCount=1` (altta araç + sağlık store'u iki abonelik olmasına rağmen). 🟢 `halBridge{present:true, started:true, droppedCount:0}`, teşhis bölümü 698-700 bayt, **sızıntı izi YOK**. 🟢 Boot crash/ANR yok (3 cold start: 1361/1128/1011 ms), idle CPU 26-65% / RSS 565-585 MB → önceki build'lerle aynı bantta. 🟡 **BACKGROUND TESTİ (45 sn arka plan → geri dön):** dönüşte `gps:true` KALDI (kalıcı yanlış-pozitif YOK) ve HAL'e hiçbir sinyal düşüşü yansımadı; **ancak** `lastChangeAt` 3171 → 699171 ms'e taşındı → oturumda bir sağlık GEÇİŞİ oldu, yani **arka planda geçici bir `gps:false` DIŞLANAMADI** (rapor yalnız son değişimi taşır; geçmiş yok). Bu, araç bağlıyken `can` için de geçici yanlış-pozitif riskinin sürdüğü anlamına gelir. 🟢 **BACKGROUND KAPISI — CİHAZDA DOĞRULANDI (2026-07-12, Xiaomi 23090RA98I, düzeltme sonrası build; ölçüm gerçek "Tanı Gönder" rapor gövdesinden, uygulama koduna enstrümantasyon EKLENMEDEN):** ① **Premise cihazda teyit:** uygulama arka plandayken/kilit ekranındayken WebView JS yaşıyor ve `document.visibilityState === 'hidden'` dönüyor → görünürlük kanalı gerçek Android WebView'da tetikleniyor. ② **SAHTE DEAD YOK:** boot raporu `sourceHealth{can:false,obd:false,gps:true, lastChangeAt:1026.6}`; **~20 dk arka plan** (60 sn kasıtlı + ekran kapanması/doze) sonrasında arka plandan alınan rapor **AYNI `lastChangeAt:1026.6`** → arka planda **SIFIR sağlık geçişi** üretildi. (Düzeltme ÖNCESİ aynı cihazda 45 sn arka plan `lastChangeAt`'i 3171 → 699171'e taşıyordu.) `activeSubscriptionCount=1`, `halBridge.droppedCount=0`. 🟡 **FOREGROUND ARTIĞI (dürüst kayıt):** foreground dönüşünden hemen sonraki raporda `lastChangeAt` 1438027.8'e taşındı — dönüş civarında bir `gps:false→true` çifti yaşandı. Bu, **arka plan saatinden DEĞİL**, tasarım gereği GÖRÜNÜR zamandaki 5 sn GPS sessizliğinden kaynaklanıyor (OS konum akışı dönüşte geç başlıyor); ölçümsüz "grace/N-tur" eşiği EKLENMEDİ. Kalıcı hasar YOK: son durum `{can:false,obd:false,gps:true}` (arka plan öncesiyle AYNI), `ingestedSignalCount` 3'e geri döndü, `droppedCount=0`, sahte `connection.changed=false` YOK. 🟡 Arka plandayken `ingestedSignalCount` 3→1 düştü (W4B **değer kaybı** yolu — sağlık kapısı değil; health donmuş durumdaydı) → dönüşte 3'e döndü. **HÂLÂ DOĞRULANMADI (🔴):** (a) gerçek araçta CAN canlı→`can=true`, ECU/kontak kapatınca ≤3-5 sn `can=false`, yalnız CAN-özel sinyallerin düşmesi, fallback'in korunması, `connection.changed=false`, frame dönünce yalnız TAZE veriyle supported/`connection=true`; (b) **low-tier cihaz (K24/Mali-400 veya T507)**; (c) araç bağlıyken foreground dönüşündeki görünür-zaman timeout'unun CAN'de de geçici bir `false` üretip üretmediği (CAN frame akışı dönüşte ≤3 sn içinde başlıyor mu) ölçülmeli. | 2026-07-12 |
| 55 | **🔴 Device Validation Pending — Worker Source Health Transport (PR-1)** — Worker'ın 1 Hz watchdog'unda **ZATEN hesapladığı** per-kaynak canlılık (`_alive(_canLastSeen, SRC_TIMEOUT_CAN_MS=3s)` · OBD 5s · GPS 5s — `VehicleCompute.worker.ts:92-96, 1010-1013`) ana thread'e taşınır: yeni `SOURCE_HEALTH` mesajı → `VehicleSignalResolver` → `halStatusStore.sourceHealth`. **NEDEN:** sessiz ECU frame kaybında native `canStatus` yalnız TRANSPORT durumunu bildiriyor (`CarLauncherPlugin.java:3748-3756` — port açıkken `connected:true`), dolayısıyla frame kaybını **yalnız worker** görüyor; bu bilgi bugüne dek ana thread'e HİÇ ulaşmıyordu. **DAVRANIŞ:** can/obd/gps **AYRI** tutulur (tek global `halConnected` yetersiz — CAN ölüp GPS yaşarken toptan kapatma yapılamaz); `null` = **BİLİNMİYOR** ve `false` (ölü) ile KARIŞTIRILMAZ; mesaj **YALNIZ DEĞİŞİMDE** postlanır (kenar-tetikli, duplicate durum mesaj üretmez; store de idempotent → gereksiz abone uyanışı yok); payload **yalnız 3 boolean + monotonik ts** (araç verisi/hız/RPM/CAN frame/VIN/PII YOK); pre-allocated envelope (hot-path allocation yok). **YENİ TIMER YOK** (worker'daki `setInterval` sayısı 4'te sabit: speed/fuel/coolant/watchdog — testle kilitli). **BU PR YALNIZ BİLGİ TAŞIR:** hiçbir sinyali unsupported YAPMAZ; HAL · adapter · Event Bus · bridge davranışı DEĞİŞMEZ (adapter hâlâ `halStatusStore` okumaz — kilitli). Fail-closed tüketim **AYRI PR (PR-2)**. ⚠️ **Background/visibility politikası EKLENMEDİ** (risk: uygulama arka plandayken worker timer'ı kısılır veya plugin event teslimi gecikirse yanlış-pozitif "CAN öldü" üretebilir → PR-2'de tüketimden önce ölçülmeli). | `platform/vehicleDataLayer/VehicleCompute.worker.ts` (WorkerOutMessage + kenar-tetikli post), `platform/vehicleDataLayer/VehicleSignalResolver.ts` (SOURCE_HEALTH kanalı), `platform/vehicleDataLayer/halStatusStore.ts` (bounded `sourceHealth` + setter), `__tests__/workerSourceHealthTransport.test.ts` (yeni, 20 test) | Gerçek cihazda/araçta: (1) **geçiş başına TEK mesaj** ve mesaj SPAM'i yok (store yazımı yalnız değişimde); (2) yeni timer/polling YOK; (3) **sahte disconnect YOK** — sinyaller hâlâ supported (Tanı raporu: `halWiring.ingestedSignalCount` düşmüyor, `halBridge.droppedCount=0`); (4) **gerçek araçta CAN frame kesilince `canAlive=false`** (≤3 sn) ve (5) **reconnect'te `canAlive=true`**; (6) OBD/GPS bağımsız kalıyor; (7) Xiaomi 23090RA98I'de boot/CPU/RAM regresyonu YOK; (8) **en az bir low-tier cihazda (K24/Mali-400 veya T507)** doğrulama; (9) background/foreground geçişinde yanlış-pozitif ölçülür. Araç ve low-tier doğrulanmadan 🟢 YAPMA. ⚠️ **KISMİ CİHAZ KANITI (2026-07-12, Xiaomi 23090RA98I — ARAÇ/OBD YOK):** 🟢 **Sahte disconnect YOK:** gerçek "Tanı Gönder" raporunda `halWiring{ingestedSignalCount:3 (değişmedi), activeSubscriptionCount:1, lastErrorCode:null}` ve `halBridge{present:true, started:true, droppedCount:0}` → PR-1 bilgi taşıması hiçbir sinyali düşürmedi, HAL/bridge davranışı DEĞİŞMEDİ. 🟢 Boot crash/ANR YOK (3 cold start: 1287/1027/1058 ms), idle CPU 13-60% / RSS 558-580 MB → önceki build'lerle aynı bantta (regresyon gözlenmedi). **HÂLÂ DOĞRULANMADI (bu yüzden 🔴):** (a) **`canAlive=false` / reconnect `true` — gerçek araç/OBD gerektirir** (CAN frame kesintisi üretilemiyor); (b) **low-tier cihaz (K24/T507) doğrulaması yok**; (c) background/foreground yanlış-pozitif ölçümü yapılmadı. ⚠️ **AÇIK BLOKAJ:** `sourceHealth` alanları TEŞHİS RAPORUNA henüz eklenmedi (diagnosticSections bu PR'ın izinli kapsamında değildi) → `canAlive`'ı cihazda ADB'siz okumak için PR-2'de (veya küçük bir teşhis PR'ında) bounded alan eklenmeli. | 2026-07-12 |
| 54 | **🔴 Device Validation Pending — Vehicle HAL → Event Bus Bridge Runtime Wiring (W4C)** — Zincir TAMAMLANDI: `UnifiedVehicleStore → VehicleHalProviderAdapter → vehicleHal → VehicleHalEventBridge → appEventBus`. Bridge foundation'ı (PR #54) gerçek `vehicleHal` singleton'ına ve W3'ün TEK aktif bus'ına bağlandı. **SAHİPLİK:** bridge bu wiring'in → cleanup YALNIZ bridge'i dispose eder; **HAL ve Bus DISPOSE EDİLMEZ**. **TEK INSTANCE:** aktif bridge varken ikinci start YENİ HAL aboneliği AÇMAZ; bayat kayıt serbest; eski cleanup yeni bridge'i düşüremez. **SystemBoot:** Wave 2'de HAL wiring'den SONRA, SystemOrchestrator'dan ÖNCE `_reg` ile → LIFO shutdown: bridge → HAL wiring → VDL → … → Event Bus (en son) ⇒ bridge kapanırken HAL ve Bus HÂLÂ AYAKTA. Bus yoksa wiring sessizce no-op (fail-soft, boot sürer). **EVENT:** `vehicle.signal.changed` (transient, supported sinyal değişimi; W4A sayesinde tek HAL snapshot → değişen sinyal başına 1 event), `vehicle.connection.changed` (retained; W4B unsupported geçişi TÜM destek kalkınca false üretir, kısmi kayıpta üretmez, reconnect'te true), `vehicle.identity.changed` (retained, ham VIN YOK — yalnız fingerprint). **`vehicle.ignition.changed` ÜRETİLMEZ** (kaynak yok → supported=false). TPMS payload bounded (≤8). Duplicate değer → duplicate event YOK (bridge imza-dedupe). **TEŞHİS:** support_snapshot'a bounded `halBridge{present,started,disposed,publishedCount,droppedCount,lastPublishAt}` eklendi — "bridge yok" (present:false, null) ile "0 event" AYRI. **BİLİNÇLİ KARARLAR:** throttle/debounce/coalescing/async-queue **YOK** → publish hızı = gerçek sinyal değişim hızı; **W4D kararı bu PR'ın CİHAZ ÖLÇÜMÜNDEN sonra** verilecek. **RİSK:** `PlatformEventBus.publish()` SENKRON dispatch eder → ileride eklenecek consumer'lar HAL/store hot-path stack'inde çalışır (bu PR'da ABONE YOK → yalnız publish maliyeti). **TİP NOTU:** bridge'in publish sözleşmesi geniş (`string`) domain/source alır, Bus dar birlik tipleri bekler → foundation'a dokunmadan wiring'de daraltıcı publish hedefi kullanıldı (doğrulama zayıflamaz; Bus zaten beyaz liste uygular). **KAPSAM DIŞI:** consumer/abone · Capability bridge/Registry · Deep Scan · Kernel servis kaydı · yeni catalog girdisi · HAL stale sweeper · **native `canStatus` wiring / worker source-health** · throttle (W4D). | `platform/system/platformCoreVehicleHalBridgeWiring.ts` (yeni), `platform/system/SystemBoot.ts` (additive Wave 2), `platform/diagnosticSections.ts` (halBridge bölümü), `__tests__/platformCoreVehicleHalBridgeWiring.test.ts` (yeni, 35 test), `__tests__/platformRuntimeDiagnostics.test.ts` (kilit güncellendi) | Gerçek cihazda/araçta: (1) TEK aktif bridge ve DOĞRU app Event Bus (Tanı raporu: `halBridge.present=true`, `started=true`); (2) HAL sinyal değişimleri doğru topic'lere publish (araçta speed/rpm/coolant/gear/reverse); (3) **publish/sn ölçülür** (park + sürüş); (4) duplicate sinyal duplicate event ÜRETMİYOR; (5) cleanup sonrası publish YOK; (6) bridge HAL/Bus'ı dispose ETMİYOR (restart sonrası bus sayaçları sıfırlanmıyor/çift bus yok); (7) `vehicle.ignition.changed` HİÇ yok; (8) TPMS payload bounded; (9) raporda VIN/ham CAN/koordinat/secret YOK; (10) `droppedCount=0` (veya gerekçeli); (11) CAN/OBD disconnect sonrası mümkün olan sinyaller unsupported + `connection.changed=false` YALNIZ tüm destek kalkınca; (12) reconnect sonrası `connection.changed=true`; (13) Xiaomi'de boot/idle CPU/RSS/FPS regresyonu YOK; (14) **en az bir low-tier cihazda (K24/Mali-400 veya T507)** sayaçlar raporda görünüyor, ANR/donma yok, publish yükü CPU/FPS bütçesini bozmuyor. ⚠️ **AÇIK BLOKAJ (ayrı iş):** ECU sessizce frame kesip store değerlerini null'a çekmezse disconnect TESPİT EDİLEMEZ (native `canStatus` hiçbir store'a yazılmıyor) → `connection.changed=false` o senaryoda üretilemez. ⚠️ **throttle/coalescing W4D'ye bırakıldı** (ölçüm sonrası). Masaüstü/araçsız Xiaomi testleriyle 🟢 YAPMA. ⚠️ **KISMİ CİHAZ KANITI (2026-07-12, Xiaomi 23090RA98I — ARAÇ/OBD YOK; ölçüm PR #68 teşhis yüzeyinden, uygulama koduna HİÇ enstrümantasyon eklenmeden):** 🟢 Bridge CANLI: gerçek "Tanı Gönder" raporunda `halBridge{present:true, started:true, droppedCount:0}`. 🟢 **PUBLISH/SN ÖLÇÜLDÜ (park, yalnız GPS):** iki rapor arası 440 sn — bridge `publishedCount` 33→197 (Δ=164) ⇒ **≈0,37 publish/sn**; bus `publishedCount` 34→198 (aynı Δ ⇒ yalnız bridge yayınlıyor), `droppedCount=0`, **`deliveredCount=0` ve `activeListenerCount=0`** (tasarım gereği abone YOK). 🟢 `halWiring.refreshCount` 56→487 (≈1 store update/sn) ve **`activeSubscriptionCount=1`** (tek HAL aboneliği). 🟢 `historyCount` 6'da SABİT → `vehicle.signal.changed` transient olduğu için history/RAM ŞİŞMİYOR; `retainedEventCount=2` (runtime.started + connection.changed). 🟢 Teşhis bölümü 612-615 bayt, **sızıntı izi YOK**. 🟢 Boot crash/ANR YOK (3 cold start: 1474/1112/1077 ms), idle CPU 17-27% / RSS 545-576 MB (önceki build'lerle aynı bantta → regresyon gözlenmedi). **HÂLÂ DOĞRULANMADI (bu yüzden 🔴): gerçek araç/OBD** — speed/rpm/coolant/gear/reverse event'leri, **sürüşteki publish/sn** (CAN frame hızı kodda yok → W4D throttle kararının girdisi), CAN/OBD disconnect sonrası `connection.changed=false`, reconnect, CPU/FPS bütçesi; ve **low-tier cihaz (K24/Mali-400 veya T507) doğrulaması YAPILMADI.** | 2026-07-12 |
| 53 | **🔴 Device Validation Pending — Vehicle HAL Source Disconnect / Fail-Closed (W4B)** — Kaynak kopunca HAL'in eski değerleri sonsuza kadar `supported=true`+`stale=false` tutması KAPATILDI. **KOD GERÇEĞİ:** `VehicleHal.refresh()` (tek stale süpürücü) üretimde HİÇ çağrılmıyor; `canStatus` native olayı hiçbir store'a yazılmıyor (yalnız `console.info` — CanAdapter.ts:57-59); `canPhase` yalnız İLK-FRAME timeout'unu temsil ediyor. **Gerçek kopma kanıtı:** worker kaynak-timeout'unda `null` yayar (SRC_TIMEOUT_CAN/OBD/GPS) ve `resetCanData()` CAN alanlarını null'lar. **SEÇİLEN MODEL (A-refined, timersız, yeni abonelik/DI YOK):** adapter, DAHA ÖNCE aktarılmış bir sinyalin değeri kaybolunca `source:'none'` ingest eder → HAL `supported=false` + `value=null` (eski değer taşınmaz; quality `unknown`, confidence 0 — uydurma YOK; timestamp = GEÇİŞ anı). **unknown ≠ disconnected:** hiç aktarılmamış sinyal için SAHTE disconnect üretilmez. **Toptan kapatma YOK:** yalnız kaynağı kaybolan sinyal düşer; füzyon `speed` GPS/OBD ile canlıysa supported KALIR. **CAN-özel boolean'lar** (door_state/parking_brake) artık CAN canlılığına bağlı → `resetCanData()`'nın `false` artığı "gerçek veri" sayılmaz (reverse füzyon olduğu için genel canlılıkta kalır). **Batch korunur (W4A):** bir geçişte düşen tüm sinyaller TEK batch → TEK HAL emit; aynı disconnected durum TEKRAR ingest edilmez (duplicate emit yok); kaynak dönünce değer normal yoldan yeniden ingest edilir (supported=true + gerçek source). **ignition:** kaynak yok → hâlâ ASLA üretilmez. **KAPSAM DIŞI:** zaman-tabanlı stale sweeper / yeni timer · Event Bus/bridge (W4C) · throttle (W4D) · native CAN state machine refactor. | `platform/vehicleHal/vehicleHalProviderAdapter.ts`, `__tests__/vehicleHalSourceDisconnect.test.ts` (yeni, 25 test) | Gerçek araçta: (1) CAN/OBD bağlıyken ilgili sinyaller supported; (2) adaptör/CAN kesilince **yalnız o kaynağın sinyalleri** `source=none`/`supported=false` oluyor; (3) diğer canlı kaynakların sinyalleri (GPS füzyon hızı) supported KALIYOR; (4) bir kopma geçişi TEK batch/TEK HAL emit üretiyor; (5) aynı disconnected durum duplicate emit ÜRETMİYOR; (6) yeniden bağlanınca supported/source/değer doğru geri dönüyor; (7) `vehicle.ignition` unsupported KALIYOR; (8) yeni timer/polling YOK ve tek subscription korunuyor (Tanı raporu: `halWiring.activeSubscriptionCount=1`, kopma/geri gelme sırasında `ingestedSignalCount` düşüp yükseliyor); (9) Xiaomi'de CPU/RAM/boot regresyonu YOK; (10) **en az bir low-tier cihazda (K24/Mali-400 veya T507)** doğrulama. ⚠️ **BİLİNEN SINIRLAMA (kapsam dışı):** CAN kablosu takılıyken ECU sessizce frame göndermeyi keserse store'daki CAN değerleri null'a DÖNMEZ (native `canStatus` hiçbir store'a yazılmıyor) → bu senaryoda kopma HÂLÂ tespit edilemez; ayrı iş (native canStatus wiring veya worker CAN-sağlık expose). Test edilmeden 🟢 yapma. ⚠️ **KISMİ CİHAZ KANITI (2026-07-12, Xiaomi 23090RA98I — ARAÇ/OBD YOK; ölçüm W4E teşhis yüzeyiyle, uygulama koduna HİÇ enstrümantasyon eklenmeden):** 🟢 Cihazın gerçek "Tanı Gönder" raporunda `halWiring{started:true, **activeSubscriptionCount:1**, refreshCount:169, ingestedSignalCount:3, lastErrorCode:null}` → tek abonelik korunuyor, hata yok. 🟢 **CAN-boolean kapısı cihazda GÖZLEMLENDİ:** aynı cihazda W4B ÖNCESİ build `ingestedSignalCount=5` veriyordu, W4B ile **3** → CAN yokken `door_state`+`parking_brake` artık HAL'e aktarılmıyor (resetCanData'nın `false` artığı "gerçek veri" sayılmıyor). 🟢 Boot/CPU/RAM regresyonu YOK (aynı cihaz durumunda A/B): baseline (main eşdeğeri) cold start 3026/3014/3014 ms · CPU 0-26% · RSS 360-365 MB → W4B 3017/3012/3010 ms · CPU 3-28% · RSS ~332 MB. 🟢 Crash/ANR yok (3 cold start + background/foreground + rapor gönderimi). **HÂLÂ DOĞRULANMADI (bu yüzden 🔴): gerçek CAN/OBD kopar-bağla senaryosu** (supported→unsupported geçişi, fallback, reconnect, tek-emit) — **araç/OBD gerektirir**; ve **low-tier cihaz (K24/Mali-400 veya T507) doğrulaması yapılmadı.** | 2026-07-12 |
| 52 | **🔴 Device Validation Pending — Platform Runtime Diagnostics (W4E, kalıcı bounded teşhis yüzeyi)** — Event Bus + Vehicle HAL wiring'in BOUNDED durum sayaçları mevcut kullanıcı-tetikli teşhis raporuna (`triggerSupportSnapshot` → `reportSupportSnapshot` → `_buildSupportSnapshotPayload` → `_deepSanitize` → `pushVehicleEvent('support_snapshot')` → /admin/tani) **kalıcı** olarak eklendi. **AMAÇ:** "tek instance / tek abonelik / event sayacı" invaryantları cihazda **ADB, CDP veya geçici debug expose OLMADAN** okunabilsin (T507 gibi PC-adb'siz cihazların TEK ölçüm kanalı). **KULLANILAN API'ler (yalnız mevcut, salt-okunur):** `getEventBusStatus()` ve `getVehicleHalWiringStatus()`. **WHITELIST — Event Bus:** present · disposed · publishedCount · deliveredCount · droppedCount · listenerErrorCount · duplicateSubscriptionCount · recursionDropCount · activeListenerCount · retainedEventCount · historyCount · lastEventAt · runtimeStartedPublished · runtimeStoppedPublished. **HAL wiring:** started · lastRefreshAt · refreshCount · ingestedSignalCount · activeSubscriptionCount · lastErrorCode (SABİT kod: init_failed/cleanup_failed; serbest metin/stack REDDEDİLİR). **`disposed` HAL wiring status'ünde YOK → uydurulmadı.** **Bridge bölümü ÜRETİLMEZ** (üretimde bağlı değil, status accessor'ı yok → sahte present/started yerine W4C'ye bırakıldı). **PRIVACY:** runtime status nesnesi ASLA spread edilmez (alan-alan whitelist); event payload · history içeriği · topic detayı · correlation/causation · araç sinyal DEĞERLERİ (hız/RPM) · VIN · fingerprint · koordinat · MAC · CAN frame · ham hex · secret · stack trace **GİRMEZ**; çıktı yalnız sayı/bool/null. **"0" ≠ "ölçülemiyor":** wiring yoksa sayaçlar **null** (0 değil). **FAIL-SOFT:** accessor throw ederse bölüm boş döner ve raporun DİĞER bölümleri üretilmeye devam eder (`_safeSection`); NaN/Infinity → null. **YALIN:** yeni timer/polling YOK · yeni publisher/subscriber/bridge/wiring YOK · runtime instance YARATMAZ (teşhis okuma bus/adapter oluşturmaz) · yeni otomatik/yüksek frekanslı gönderim YOK (kullanıcı tetikli akış korunur) · yeni bağımlılık YOK. | `platform/diagnosticSections.ts` (+`buildPlatformRuntimeSnapshot`), `platform/remoteLogService.ts` (payload'a `platform: _safeSection(...)`), `__tests__/platformRuntimeDiagnostics.test.ts` (yeni, 17 test) | Gerçek cihazda: (1) "Tanı Gönder" raporunda **bounded Event Bus sayaçları** görünüyor; (2) raporda **bounded Vehicle HAL wiring sayaçları** görünüyor (`activeSubscriptionCount=1` → tek abonelik ADB'siz doğrulanabiliyor); (3) raporda **hiçbir event payload'ı veya araç sinyal değeri YOK**; (4) VIN/koordinat/CAN/ham hex/secret YOK; (5) wiring yokken rapor fail-soft (sayaçlar null, rapor yine gönderiliyor); (6) teşhis üretimi yeni runtime instance YARATMIYOR; (7) yeni timer/polling YOK; (8) rapor boyutunda anlamlı şişme YOK (önce/sonra ölçülür); (9) Xiaomi 23090RA98I'de ADB/global expose olmadan sayaçlar okunabiliyor; (10) **T507 veya K24/Mali-400'de mevcut teşhis kanalıyla sayaçlar okunabiliyor** ve rapor gönderimi başarılı, UI donması/ANR yok; (11) CPU/RAM/boot regresyonu YOK. Masaüstü testleriyle 🟢 YAPMA; low-tier cihazda gerçek rapor doğrulanmadan 🔴 kalır. ⚠️ **CİHAZ KANITI (2026-07-12, Xiaomi 23090RA98I — UYGULAMA KODUNA HİÇ ENSTRÜMANTASYON EKLENMEDEN):** 🟢 Cihazda **gerçek "Tanı Gönder" butonu** ile rapor tetiklendi; giden `push_vehicle_event` (support_snapshot) isteğinin GÖVDESİ (CDP Network ile, yalnız gözlem) `platform` bölümünü İÇERİYOR: `eventBus{present:true, publishedCount:1, deliveredCount:0, droppedCount:0, listenerErrorCount:0, duplicateSubscriptionCount:0, recursionDropCount:0, activeListenerCount:0, retainedEventCount:1, historyCount:1, runtimeStartedPublished:true, runtimeStoppedPublished:false}` + `halWiring{started:true, **activeSubscriptionCount:1**, refreshCount:289, ingestedSignalCount:5, lastErrorCode:null}` → **tek bus + tek HAL aboneliği + runtime.started bir kez, ADB/global expose OLMADAN raporda okunabiliyor.** 🟢 Gizlilik: platform bölümünde VIN/koordinat/sinyal değeri/correlation/payload/history izi **YOK** (otomatik tarama: sızıntı izi yok). 🟢 Boyut: platform bölümü **484 bayt**, toplam rapor ~14,2 KB (≈%3,4 → anlamlı şişme yok). 🟢 Kararlılık: 3 cold start (1059/1085/1325 ms) + background/foreground + rapor gönderimi sırasında **crash/ANR YOK**, UI donmadı; idle CPU 25-39% / RSS 577-600 MB (önceki build ile aynı bantta → regresyon yok). **HÂLÂ DOĞRULANMADI (bu yüzden 🔴): low-tier cihaz (T507 veya K24/Mali-400) üzerinde rapor gönderimi ve /admin/tani'de platform sayaçlarının görünmesi DOĞRULANMADI** (T507'de PC-adb yok — bu PR'ın asıl hedef senaryosu); ayrıca alınan raporun admin panelinde görüntülenmesi kullanıcı tarafından teyit edilmeli. | 2026-07-12 |
| 51 | **🔴 Device Validation Pending — Vehicle HAL Provider Adapter Batch Ingest (W4A, perf)** — Adapter tek `refresh()` içinde değişen TÜM sinyalleri artık **tek `hal.ingest(batch)`** ile veriyor. **KÖK PROBLEM:** eskiden sinyal başına `hal.ingestSignal()` çağrılıyordu; `VehicleHal.ingestSignal` her değişimde revision artırıp **15 sinyallik tam snapshot** üretiyor (kopya + sort + freeze) ve abonelere emit ediyordu → tek store tick'inde **N değişim = N emit + N snapshot**; CAN yolu tek `updateCanExtras` çağrısında 10'a kadar HAL sinyalini birlikte değiştirebiliyor (worker yoluyla toplam 14). **YENİ:** N değişim = **1 ingest = 1 emit**. **KORUNAN:** 14 eşleme ve SIGNAL_MAP sırası · ignition eşlenmez · TPMS yalnız gerçek 4-tuple · source/quality/confidence · refresh başına ORTAK timestamp · değer normalizasyonu · duplicate ingest yok · start/stop/dispose idempotency · abonelik sayısı · cleanup · fail-soft · bounded `_last` · timer/polling YOK · HAL sahipliği adapter'a GEÇMEZ. **DEDUPE ATOMİKLİĞİ:** `_last` YALNIZ başarılı `ingest()` sonrası güncellenir — `VehicleHal.ingest()` sinyal-bazlı catch İÇERMEZ (tümden throw edebilir), bu yüzden batch düşerse sinyaller "aktarılmış" sayılmaz ve sonraki refresh yeniden dener (HAL idempotent: aynı değer → değişim yok → emit yok). **BİLİNÇLİ DAVRANIŞ FARKLARI:** (1) HAL `revision` sinyal başına değil **batch başına 1** artar; (2) HAL aboneleri **ara/kısmi snapshot görmez**, yalnız nihai tutarlı snapshot'ı görür; (3) yayınlanan nihai sinyal kümesi + değerler + metadata DEĞİŞMEZ; (4) üretimde HAL snapshot/revision tüketicisi olmadığı için **kullanıcı-gözlemlenebilir davranış değişmez**. **KAPSAM DIŞI:** Event Bus bridge wiring (W4C) · stale/ disconnect semantiği (W4B) · throttle/coalescing (W4D) · kalıcı teşhis yüzeyi (W4E). Bu PR bridge'i BAĞLAMAZ — W4C'nin performans ÖN KOŞULUDUR. | `platform/vehicleHal/vehicleHalProviderAdapter.ts` (refresh + `VehicleHalIngestTarget` sözleşmesi `ingest`'e geçti), `__tests__/vehicleHalBatchIngest.test.ts` (yeni, 24 test), `__tests__/vehicleHalProviderAdapter.test.ts` · `platformCoreVehicleHalWiring.test.ts` · `unifiedVehicleStoreProvider.test.ts` (fake HAL'ler yeni sözleşmeye güncellendi) | Gerçek cihazda: (1) tek refresh içindeki N değişiklik **yalnız 1 HAL emit** üretir (deterministik sayaçla; testte kilitli, cihazda gerçek CAN tick'i ile doğrulanmalı); (2) nihai HAL snapshot sinyal kümesi ve değerleri değişmedi; (3) source/quality/confidence/timestamp korunuyor; (4) duplicate ingest davranışı korunuyor; (5) **gerçek araçta emit amplifikasyonunun azaldığı sayaçla ölçülür** (çok-sinyalli CAN tick'i araç/OBD olmadan üretilemez); (6) Xiaomi 23090RA98I'de CPU/RAM/FPS regresyonu YOK; (7) **en az bir low-tier cihazda (tercihen K24/Mali-400) CPU/RAM/FPS regresyonu YOK**; (8) boot süresi regresyonu YOK; (9) Event Bus bridge hâlâ BAĞLI DEĞİL. Test edilmeden 🟢 yapma. ⚠️ **KISMİ CİHAZ KANITI (2026-07-12, Xiaomi 23090RA98I — ARAÇ/OBD YOK; geçici, commit-EDİLMEYEN bounded sayaç + CDP-over-adb, ölçüm sonrası kaldırıldı ve temiz ağaçtan yeniden build/kurulum yapıldı):** 🟢 boot crash/ANR YOK (3 cold start + background/foreground); cold start WaitTime baseline (main, W4A yok) 1218/1060/1041 ms → W4A 1082/1035/1138 ms (**boot regresyonu YOK**); idle CPU baseline 109/62/58% → W4A 23/30/26% ve RSS 572-622 MB → 559-570 MB (⚠️ **her iki ölçüm de gürültülü; W4A lehine kesin iyileşme İDDİA EDİLMİYOR**, yalnız regresyon yok). 🟢 Adapter/HAL sayaçları (30 sn, park, yalnız GPS akışı): `subs=1 · refreshCount≈56 (~1.9 store update/sn) · emits=23 (~0.77 HAL emit/sn) · ingestedSignals=5 · maxBatchDelta=5` → **emit sayısı refresh sayısından KÜÇÜK** (değişiklik yoksa ingest/emit yok) ve tek abonelik korunuyor. **HÂLÂ DOĞRULANMADI (bu yüzden 🔴):** park hâlinde tick başına genelde TEK sinyal değiştiği için **N→1 emit azalması cihazda GÖSTERİLEMEDİ** — çok-sinyalli gerçek CAN tick'i **araç/OBD gerektirir**; ayrıca **low-tier cihaz (K24/Mali-400 veya T507) ölçümü YAPILMADI** ve uzun-süreli CPU/termal ölçülmedi. | 2026-07-12 |
| 50 | **🔴 Device Validation Pending — Platform Event Bus Runtime Ownership (W3)** — Uygulamanın TEK `appEventBus` örneğinin SAHİPLİĞİ: oluşturma · kontrollü erişim · cleanup · boot/shutdown runtime olayları · bounded teşhis. **KOD GERÇEĞİ:** `PlatformEventBus`in start/stop API'si YOK (yalnız publish/subscribe/reset/dispose) → "lifecycle" = OLUŞTUR + DISPOSE + tek-instance guard. `eventBus` barrel'ı bilinçli olarak global singleton export ETMEZ; bu modül o invaryantı KORUR (import edilmesi bus YARATMAZ; bus yalnız `start...()` ile). **Sahiplik:** bus bu wiring modülünündür → cleanup bus'ı DISPOSE EDER; cleanup YALNIZ kendi kaydını siler (bayat/HMR kaydı otomatik serbest; eski cleanup yeni bus'ı düşüremez) → boot→shutdown→boot güvenli. **Kernel sahibi DEĞİL:** `PlatformKernel` bus'ı `publisher` (KernelEventPublisher) DI ile TÜKETİR → bus Kernel'in BAĞIMLILIĞIDIR, servisi değil; Kernel bu PR'da HİÇ import edilmez. **Duplicate koruması kritik:** `publish()` throw etmez, null döner → yanlış/disposed bus'a yayın SESSİZ event kaybıdır; ikinci `start` yeni bus YARATMAZ. **SystemBoot:** Wave 1'in EN BAŞINDA `_reg` ile (İSİMSİZ → `restartService` adayı DEĞİL, restart = sessiz abonelik ölümü) → LIFO shutdown'da EN SON dispose olur (bridge/publisher'lar kapanırken bus ayakta). Wave sırası (1→2→3→4) DEĞİŞMEZ; legacy healthMonitor/worker restart/circuit breaker/LIMP_HOME DOKUNULMAZ. **Runtime olayları (yalnız MEVCUT katalog, yeni ad uydurulmadı):** `platform.runtime.started` (retained/critical) tüm dalgalar başarıyla bitince BİR KEZ; `platform.runtime.stopped` shutdown'da bus DISPOSE EDİLMEDEN ÖNCE BİR KEZ ve yalnız `started` yayınlandıysa → hiç başlamamış/yarım boot SAHTE stopped üretmez; tekrar stop duplicate üretmez. Payload UYDURULMAZ (katalogda payload sözleşmesi yok); `source` verilmez (beyaz listede SystemBoot karşılığı yok → `unknown`). **Kapsam DIŞI:** HAL→Bus bridge (W4) · Capability bridge/Registry · Deep Scan · Kernel servis kaydı · consumer aboneliği · throttle/debounce · HAL stale timer · katalog genişletme. **Publisher/consumer YOK → boot başına 2 publish, 0 subscriber.** Yeni timer/polling YOK; kalıcı `window.__` debug expose YOK. | `platform/system/platformCoreEventBusWiring.ts` (yeni), `platform/system/SystemBoot.ts` (additive: Wave 1 kayıt + started/stopped), `__tests__/platformCoreEventBusWiring.test.ts` (yeni, 35 test) | Gerçek cihazda: (1) cold boot'ta crash/ANR YOK; (2) boot başına TEK aktif bus (`getEventBusStatus().present=true`, ikinci instance yok); (3) `platform.runtime.started` boot başına BİR KEZ (`publishedCount`/`retainedEventCount`=1); (4) shutdown başına `platform.runtime.stopped` BİR KEZ ve bus dispose EDİLMEDEN ÖNCE; (5) boot→shutdown→boot sonrası YENİ ve kullanılabilir bus (sayaçlar sıfırdan, eski bus'a event kaçmıyor); (6) listener hatası bus'ı çökertmiyor (`listenerErrorCount` artar, diğer listener teslim alır); (7) `droppedCount`/`recursionDropCount` bounded ve erişilebilir; (8) yeni timer/polling YOK; (9) boot süresi regresyonu <50 ms (öncesi/sonrası ölçülür); (10) Xiaomi 23090RA98I'de idle CPU/RAM regresyonu YOK; (11) **en az bir low-tier cihazda (K24/Mali-400 veya T507) doğrulama**. ⚠️ **AÇIK BLOKAJ:** sayaçlar (2,3,4,6,7) cihazda kalıcı bir teşhis yüzeyi olmadan OKUNAMAZ — `diagnosticSections` yalnız `remoteLogService` üzerinden rapora girer ve o dosya W3'ün izinli kapsamında DEĞİLDİR; ölçüm ya geçici (commit edilmeyen) instrumentation ile ya da ayrı bir "teşhis yüzeyi" PR'ı ile yapılmalıdır. Masaüstü testleriyle 🟢 YAPMA. ⚠️ **KISMİ CİHAZ KANITI (2026-07-12, Xiaomi 23090RA98I — geçici/commit-EDİLMEYEN instrumentation + CDP-over-adb; ölçüm sonrası kaldırıldı, temiz ağaçtan yeniden build/kurulum yapıldı):** 🟢 boot crash/ANR YOK (3 cold start + background/foreground + 2 stop/restart döngüsü; logcat FATAL/ANR/eventBusWiring temiz). 🟢 İKİ ardışık boot'ta da sayaçlar: `present=true · publishedCount=1 · retainedEventCount=1 · droppedCount=0 · listenerErrorCount=0 · recursionDropCount=0 · activeListenerCount=0 · runtimeStartedPublished=true` → **boot başına TEK aktif bus, `platform.runtime.started` TAM BİR KEZ (retained), publisher/consumer yok, sayaç birikmesi yok.** 🟢 cold start WaitTime: baseline (main+#65, bus YOK) 1061/1062/1115 ms → W3 989/1091/1237 ms (**delta ~+27 ms < 50 ms hedefi**). 🟡 idle CPU/RSS: her iki build de 26–68% / ~535–588 MB aralığında dalgalı → ayırt edici regresyon gözlenmedi (gürültülü ölçüm, kesin iddia YOK). **HÂLÂ DOĞRULANMADI (bu yüzden 🔴):** `platform.runtime.stopped` cihazda tetiklenemedi (üretimde yalnız React unmount'ta çalışır; testlerde doğrulandı), listener-hatası izolasyonu cihazda gözlenemedi (consumer yok), uzun-süreli idle CPU/termal ölçülmedi ve **low-tier cihaz (K24/Mali-400 veya T507) doğrulaması YAPILMADI.** 🔴 **AÇIK BLOKAJ:** sayaçlar ancak geçici instrumentation ile okunabildi — kalıcı teşhis yüzeyi (diagnosticSections→remoteLogService) ayrı PR gerektirir. | 2026-07-12 |
| 49 | **🔴 Device Validation Pending — Vehicle HAL Runtime Wiring** — Mevcut `useUnifiedVehicleStore` (fused worker/CAN/OBD/GPS) veri akışını, hazır SALT-OKUNUR provider + adapter foundation üzerinden gerçek `vehicleHal` singleton'ına AYNA modunda bağlar. Zincir: `useUnifiedVehicleStore` → `createUnifiedVehicleStoreProvider` → `VehicleHalProviderAdapter` → `vehicleHal`. **Yalnız HAL veri girişi aktive edilir** — Event Bus wiring YOK · Capability Registry wiring YOK · Platform Kernel servis kaydı YOK · Deep Scan wiring YOK · tüketici migrasyonu YOK · UI/native/OBD/CAN/poll frekansı DEĞİŞMEZ. **SystemBoot:** Wave 2'de VehicleDataLayer'dan SONRA, SystemOrchestrator'dan ÖNCE `_reg` ile kaydedilir → LIFO shutdown'da store hâlâ ayaktayken adapter/provider dispose olur; Wave sırası (1→2→3→4) DEĞİŞMEZ. **Tek instance:** modül-düzeyi aktif-kayıt guard → duplicate `start` ikinci provider/adapter/subscription AÇMAZ (no-op cleanup); HMR/restart bayat kaydı otomatik serbest bırakılır; cleanup yalnız KENDİ kaydını siler → boot→shutdown→boot güvenli. **Sahiplik:** provider+adapter wiring'e ait (cleanup dispose eder); `vehicleHal` PAYLAŞILAN singleton → cleanup HAL'i DISPOSE ETMEZ (çift-dispose yok). **Sinyaller (store'da gerçekten var):** speed · rpm · fuel_level · odometer · reverse · coolant_temp · oil_temp · throttle · battery_voltage · gear · ambient_temp · door_state · parking_brake; **TPMS yalnız gerçek 4-tuple veri varsa**; **ignition kaynağı YOK → ingest EDİLMEZ (supported=false, fail-closed)**; metadata UYDURULMAZ. **Duplicate ingest yok** (adapter O(1) son-değer dedupe); yeni timer/polling YOK (mevcut store subscribe akışı); hot-path'te JSON stringify/deep clone YOK. **Fail-soft:** store yoksa/bozuksa boot ÇÖKMEZ (tek `logError` + no-op cleanup, wiring kilitlenmez); provider/adapter/HAL-ingest hatası izole (tek sinyal bozuksa diğerleri sürer); cleanup hatası shutdown'ı engellemez; hata hâlinde legacy store tüketicileri AYNEN çalışır. **Gözlemlenebilirlik:** bounded `getVehicleHalWiringStatus()` (started/lastRefreshAt/ingestedSignalCount/refreshCount/activeSubscriptionCount/lastErrorCode) — ham telemetri/log spam YOK. | `platform/system/platformCoreVehicleHalWiring.ts` (yeni), `platform/system/SystemBoot.ts` (+13 satır, additive), `__tests__/platformCoreVehicleHalWiring.test.ts` (yeni, 43 test) | Gerçek cihazda: (1) boot'ta crash/ANR YOK; (2) TEK HAL/provider/adapter instance ve TEK store aboneliği (`getVehicleHalWiringStatus().activeSubscriptionCount === 1`); (3) araç bağlıyken hız/RPM/soğutma suyu/akü voltajı/vites-reverse HAL'e DOĞRU ulaşıyor; (4) aynı veri duplicate ingest ÜRETMİYOR; (5) OBD reconnect/uygulama restart sonrası subscription ÇOĞALMIYOR (hâlâ 1); (6) kaynağı olmayan `vehicle.ignition` supported=false KALIYOR; (7) TPMS kaynağı olmayan araçta supported GÖRÜNMÜYOR; (8) boot süresi regresyonu <50 ms (öncesi/sonrası ölçülür); (9) Xiaomi'de steady-state idle CPU regresyonu YOK; (10) Mali-400/K24'te CPU/RAM/FPS ayrıca ölçülür; (11) stale davranışı: veri kesilince HAL sinyalleri stale'e düşüyor. ⚠️ **Event Bus · Capability Registry · Platform Kernel servis kaydı · tüketici migrasyonu AYRI PR'lar.** Test edilmeden 🟢 yapma. ⚠️ **KISMİ CİHAZ KANITI (2026-07-12, Xiaomi 23090RA98I — ARAÇ/OBD YOK):** boot crash/ANR YOK (4 cold start + background/foreground + restart, logcat FATAL/ANR/vehicleHalWiring temiz); cold start WaitTime baseline (wiring'siz main) 3013 ms → wiring 3011/3015/3046/3078 ms (ort. ~3037, **delta ~+24 ms < 50 ms hedefi**); idle CPU: her iki build de 3–30% arası dalgalı, ayırt edici regresyon gözlenmedi. **HÂLÂ DOĞRULANMADI** (bu yüzden 🔴): tek subscription/instance sayacı (in-page teşhis kancası yok), HAL sinyal akışı (hız/RPM/soğutma/voltaj), ignition & TPMS fail-closed, reconnect/stale davranışı — GERÇEK ARAÇ/OBD gerektirir. Mali-400/K24 ölçülmedi. | 2026-07-12 |
| 48 | **🔴 Device Validation Pending — Platform Kernel / Service Lifecycle Foundation** — Platform servislerinin (Vehicle HAL · Capability Registry · Event Bus · Deep Scan · Navigation · AI · Fleet · Remote · Health · Vision) yaşam döngüsünü, bağımlılıklarını, başlatma/durdurma SIRASINI, sağlık durumunu ve HATA İZOLASYONUNU yöneten merkezi çekirdek. ⚠️ **KOD GERÇEĞİ (salt-okunur analiz):** lifecycle ≥3 desende parçalı — SystemBoot "Wave 1-4"+LIFO cleanup+named restart (MAX_RESTARTS=2, BACKOFF_BASE 5s, COOLOFF 5dk); modül-düzeyi startX/stopX; servis-içi _listeners/_reg. ORTAK descriptor/bağımlılık/health/circuit modeli YOK → Kernel doldurur (gerçek servisleri BAĞLAMADAN, yalnız DI). **Model:** 11 state (registered→initializing→ready→starting→running/degraded/paused→stopping→stopped/failed/disposed) · 6 criticality · 6 startPolicy · 4 restartPolicy · 4 health. **Descriptor:** id/version/criticality/dependencies/optionalDependencies/requiredCapabilities/minimumDeviceTier/startPolicy/restartPolicy/init·start·stopTimeoutMs/owner/experimental. **Service contract:** init/start/pause/resume/stop/health/dispose HEPSİ opsiyonel (eksik→no-op). **API:** register/unregister/has/getServiceStatus/list/resolveStartOrder/initialize/start/startAll/pause/resume/stop/stopAll/restart/markDegraded/runHealthCheck/getKernelSnapshot/subscribe/reset/dispose. **Dependency:** directed-acyclic; **cycle kayıt anında reddedilir**; topolojik başlatma (tie-break=id), **ters topolojik durdurma**; eksik required→başlamaz; eksik optional→degraded başlar; dependency failed→dependent bloke. **Gate (DI):** Capability Registry DOĞRUDAN import EDİLMEZ (isAvailable/getStatus/subscribe contract); required unknown/unavailable→başlamaz; degraded→degraded başlar; **minimumDeviceTier sağlanmazsa başlamaz** (Mali-400/low'da optional ağır servis GATE). **Timeout:** proje 5s konvansiyonundan türetildi (magic yok); timer YALNIZ aktif lifecycle çağrısı boyunca (settle'da temizlenir); **steady-state timer/polling YOK**. **Restart/circuit:** never/manual/on_failure/bounded_auto; bounded (safety=1, diğer=2); max aşımında **circuit-open**; **retry timer döngüsü YOK** (backoff yalnız saf `computeKernelBackoffMs` contract). **Health:** service.health() varsa çağrılır (fail-soft), yoksa state'ten türetilir; result state'i MUTATE ETMEZ. **Event (DI opsiyonel):** publishName ile platform.runtime.started/stopped/degraded · platform.service.started/stopped/failed; **Event Bus yoksa lifecycle sürer**, publish hatası izole. **Fail-soft:** servis exception izole, bir servis düşse bağımsızlar sürer, public API throw etmez, unknown servis güvenli, dispose sonrası no-op, partial-start+stopAll güvenli. **Immutable** snapshot/status (nested dahil), descriptor girdi mutate edilmez, iç Map/graph dışa verilmez. **Privacy:** degradedReasons/lastErrorCode sanitize (VIN/MAC/koordinat/secret sızmaz). **YALIN:** SystemBoot'a BAĞLANMAZ · gerçek servisleri auto-start ETMEZ · HAL/Registry/Bus'ı KENDİSİ YARATMAZ (DI) · persistence/SQL/cloud YOK · global singleton YOK · yeni bağımlılık YOK · import yan etkisiz (yalnız DeviceTier TYPE). Kernel servis iş mantığını BİLMEZ. Bounded (servis 128 · listener 32). **Gerçek servis wiring · SystemBoot wiring AYRI PR.** | `platform/kernel/platformKernel.ts` (yeni), `platform/kernel/index.ts` (yeni), `__tests__/platformKernel.test.ts` (yeni, ~53 test) | Gerçek cihazda (gerçek servis wiring PR'ı bağlandıktan SONRA): (1) servisler dependency SIRASIYLA başlıyor; (2) stop sırasında TERS sırayla kapanıyor; (3) bir servis çökünce bağımsız servisler çalışmaya devam ediyor; (4) capability/DeviceTier olmayan servis başlatılmıyor; (5) Mali-400 cihazda ağır optional servisler gate ediliyor; (6) lifecycle timeout ve cleanup sonrası timer/listener sızıntısı yok; (7) kernel snapshot servis durumlarını doğru gösteriyor; (8) BASIC_JS/Mali-400'de sürekli CPU/RAM yükü yok (steady-state timer yok, pasifken sıfır yük). ⚠️ **Gerçek servis + SystemBoot wiring AYRI PR.** Test edilmeden 🟢 yapma. | 2026-07-11 |
| 45 | **🟢 FullMapView Idle Render Optimization (device-verified 2026-07-12, +WAKE-tarafı park tutucusu)** — Cihaz QA'sının FAIL sebebini kapatır: tam ekran harita **boşta, kullanıcı dokunmadan** 1.5–2.1 CPU çekirdeği yakıyordu (+30sn %64 · +60sn %96 · +2dk %106 · **+3dk %210** · +6dk %167; harita kapanınca 45sn'de %20). **KÖK NEDEN (iki katman):** (1) idle kapısı **GPS gürültüsünü hareket sanıyordu** — park hâlinde ±3 m doğrulukla gelen fix'ler "3 m yer değiştirme ≈ 10 km/h" üretip `isIdleNow`'u sürekli false yapıyor, rAF döngüsü **hiç uyumuyordu**; (2) döngü uyanıkken tick **değişiklik olmasa da** `updateUserMarker()` çağırıyor, o da **koşulsuz `source.setData()`** yapıyordu (`MapLayerManager.ts:371`) → her 60 ms'de bir MapLibre repaint + kamera 150–500 ms'de yeniden hesap → sürekli GPU/compositor yükü (`mali-event-hand` %24 + `Chrome_InProcGp` %17). **ÇÖZÜM (yalnız FullMapView.tsx):** (a) **yapılan-iş kapısı** — marker/kamera `NO_WORK_IDLE_MS=2500` boyunca hiç güncellenmediyse harita gerçekten sabittir → döngü uyur (gürültüye dayalı hız/yer-değiştirme sezgilerinin yerine **ground truth**); (b) **marker + kamera dedup** (eşikler: 0.3 m / 0.5° / 0.5 km/h; kamera 0.5 m / 0.5°) → değişmeyen kare repaint üretmez; (c) **park gürültü tutucusu** — hız < 1.5 km/h ve nav/sürüş yokken fix'in kendi doğruluk yarıçapı (≥5 m) altındaki kayma hareket sayılmaz; (d) **force kapıları** — kullanıcı pan'inden sonra kamera MUTLAKA geri merkezler, stil/katman düşerse marker MUTLAKA yeniden çizilir (self-healing korunur); (e) DR yolu ve tek-nokta dalı (her karede setData ediyordu) da dedup'a bağlandı. **Wake tetikleyicileri ve hareket semantiği DEĞİŞMEDİ** → nav/sürüş/takip davranışı aynı. | `components/map/FullMapView.tsx` (tek dosya), `__tests__/fullMapViewIdleRender.test.ts` (yeni, 25 test) | **🔴 Xiaomi zircon (bekliyor):** (1) tam ekran harita açık + dokunulmadan 3 dk → process CPU **%210 → tek haneli/düşük onlu** seviyeye insin (önceki QA baseline: `docs-local/qa-runs/2026-07-11T15-52-52Z-deep-device-analysis/`); (2) `dumpsys gfxinfo` boşta kare üretimi ~56 fps'ten **~0–2 fps**'e insin; (3) harita **kapatılınca** timer/rAF tamamen dursun; (4) 30 dk soak: PSS/Graphics düz, crash/ANR 0; (5) **navigasyon davranışı DEĞİŞMESİN** — rota başlat/reroute/takip/marker/pan-sonrası-recenter aynı; (6) pan/zoom/rotate akıcılığı bozulmasın. **🔴 Mali-400 (K24) — bekliyor:** aynı ölçümler; asıl kazanç orada kanıtlanmalı. Cihazda görülmeden 🟢 YAPILMAZ. **✅ DOĞRULAMA (2026-07-12, gerçek Xiaomi 23090RA98I, main+#64+#61, tam ekran harita idle/park, dokunma yok, 12s settle, CDP rAF-attribution + `/proc` jiffies — screencap YOK):** İLK ÖLÇÜM #61'i **DÜŞÜRDÜ** — idle CPU 108% (main+#64 106% ile aynı), rAF 137/sn, idle-gate döngüsü `L` 278/3sn (HİÇ uyumuyor). **KÖK NEDEN (cihaz CDP tanısı, geçici `window.__ib`/`__wc` instrumentation):** `isIdleNow` %100 `hysteresis(wake <2500ms)` bloğunda takılıyordu; wake **2.8/sn spam** — `onGPSLocation` displacement-wake (satır ~318) **accuracy-BLIND**'dı → park GPS gürültüsü (accuracy 3m, ~3m zıplama) "≥3m/≥5km/h" eşiğini geçip wake tetikliyordu. #61'in park tutucusu yalnız `isIdleNow` (döngü ÇIKIŞI) tarafındaydı, wake GİRİŞİ korunmamıştı → **yarım fix**. **DÜZELTME (wake-tarafı park tutucusu, `isIdleNow` ile tutarlı semantik):** park (nav/sürüş yok + hız<STANDSTILL_KMH) hâlinde fix'in kendi doğruluk yarıçapı (`Math.max(STANDSTILL_HOLD_M, accuracy)`) altındaki kayma wake ETMEZ; gerçek hareket (hız≥1.5 **veya** yarıçap-üstü displacement) wake eder → takip/fail-soft korunur. **DÜZELTME SONRASI (aynı cihaz/yöntem):** idle CPU **108%→21%** (~5x, Ayarlar tabanı ~19% seviyesine), toplam rAF **137→3.3/sn** (%97.6↓), idle-gate `L` **278→0** (döngü uyudu), MapLibre native repaint **114→0**, VisionOverlay(#64) 0. Ölçütler (1)(2)(3) KARŞILANDI (following AÇIK'ken uyudu → gerçek ardışık-fix jitter < CAM_EPS). **Testler:** `fullMapViewIdleRender.test.ts` 25→**30** (yeni `feedGps` modeli GPS-wake yolunu simüle eder — eski 25 test wake'i yalnız manuel çağırdığından bu bug'ı KAÇIRMIŞTI; R1-R5 wake-tarafı tutucuyu kilitler). ⚠️ Test edilmeyen: gerçek araçta hareket/nav-başlat/reroute/pan-recenter (ofis testi, araç yok); 30dk soak; **Mali-400 (K24) hâlâ 🔴**; olası kamera-tarafı park tutucusu (cihazda gerekmedi — jitter<CAM_EPS). | 2026-07-12 |
| 47 | **🟢 Device-Verified (2026-07-12, CDP rAF-attribution) — VisionOverlay AR-kapalı idle loop gate** — ✅ Xiaomi 23090RA98I, tam ekran harita idle: renderAR 3sn'de 1 çağrı = ~0.3fps (fix'siz ~60fps, %99.4↓); #61 wake-fix build'inde de 0 rAF. Restart mekanizması sağlam. Araçta AR/HYBRID çizim geri-gelme testi + Mali-400 hâlâ bekliyor. — GÖREV 2 kök-neden araştırmasının (docs-local/device-runs/20260712T000500-map-idle-root-cause) bulduğu **birincil** idle-CPU kaynağını kapatır. `VisionOverlay.renderAR` (AR kamera/şerit katmanı) 60 fps rAF döngüsü, **AR/kamera KAPALIYKEN bile** (`!frame \|\| !isHybrid \|\| confidenceLevel==='off'`) `requestAnimationFrame(renderAR)` ile KOŞULSUZ yeniden planlanıyordu → VisionOverlay FullMapView'de koşulsuz mount olduğundan, harita açıkken (çizim yapmasa da) her kare `getContext('2d',{willReadFrequently:true})` (CPU-backed canvas) + `getBoundingClientRect` (layout) + `clearRect` yapıp ana thread'i ve compositor'ı boşa yüküyordu. **KANIT (gerçek cihaz, DevTools + /proc, A/B):** rAF çağrılarının %84'ü VisionOverlay (~138/sn); VisionOverlay rAF canlı düşürülünce (settled idle harita) **process 35%→20%, main-thread 11%→6%, gfx 5.2→0.3 fps** — Ayarlar tabanına + ~0-2fps hedefine ulaştı. FullMapView tick (PR #61) zaten settled'da 0/sn (idle-gate çalışıyor); sorun ayrı bileşendeydi. **FIX (tek dosya, 1 satır davranış):** off-branch'te `requestAnimationFrame(renderAR)` yerine `rafRef.current = null` → döngü DURUR. Yeniden başlatma OTOMATİK: renderAR deps'i (isHybrid/confidenceLevel/vision.frame/currentLat/currentLon) değişince renderAR yeniden yaratılır → mevcut `useEffect([renderAR])` döngüyü tekrar başlatır. **Aktif AR çizim yolu (satır 623 reschedule) DEĞİŞMEZ** → AR/navigasyon işlevi korunur. Aynı "reschedule etmeden dur" deseni satır ~550 (`if(!canvas) return;`) zaten kullanıyor. Zero-leak: unmount cleanup + rafRef=null. YENİ timer/rAF/bağımlılık YOK. Kapsam-dışı dokunulmadı: FullMapView PR #61, GPS PR #62, native OBD/CAN, SQL, Platform Kernel #55. | `components/map/VisionOverlay.tsx` (renderAR off-branch), `__tests__/visionOverlayIdleLoopGate.test.ts` (yeni, 5 kaynak-kilidi test — bu repo SSR render kullandığından rAF davranışsal testi mümkün değil, kilit off-branch'e requestAnimationFrame geri konmasını engeller) | Gerçek Xiaomi cihazda (önce/sonra, tam ekran harita idle, park, dokunma yok, `/proc` + DevTools rAF attribution — **screencap ölçüm penceresinde YOK**): (1) VisionOverlay rAF idle'da **~0/sn** (önce ~60-138/sn); (2) idle harita process CPU **≤ ~20-22%** (Ayarlar tabanına yakın, önce ~35%); (3) gfx idle kare **≤ ~2 fps** (önce ~5.2); (4) **AR/HYBRID navigasyon açılınca** şerit/ok/tabela çizimi ÇALIŞIYOR (döngü yeniden başlıyor); (5) background/cold-warm start/30dk soak regresyonsuz, crash/ANR 0; (6) **Mali-400 (K24)**: CPU-canvas maliyeti orada oransal daha ağır → kazanç muhtemelen daha büyük ama ölçümsüz "çalışır" denmez. Test edilmeden 🟢 yapma. | 2026-07-12 |
| 43 | **🔴 Device Validation Pending — Compass Foreground Demand Gating** — PR #42'nin (Ledger #42) bilinçli bıraktığı "Compass Settings ekranında KAPATILMADI" sınırlamasını KAPATIR. Önce `gpsService._startCompassListener()` `startGPSTracking()` ile KOŞULSUZ açılıyor, uygulama ön plandayken (Ayarlar/profil/medya/haritasız ekran dahil) HİÇ kapanmıyordu → cihaz QA'sında boşta ana ekranda `rot_vec`+`game_rotvec` 60 Hz açık. **BU PR:** küçük, ref-count'lu, owner-bazlı **compass demand contract** (`compassDemand.ts`) — `acquireCompassDemand/releaseCompassDemand/hasCompassDemand/getCompassDemandCount` + `subscribeCompassDemand` (0↔1 geçişlerinde bildirir). gpsService bu talebi dinler: **ilk acquire → gate compass aboneliği AÇILIR; son release → KAPANIR** (yalnız GPS tracking açıkken). **Tüketiciler:** `FullMapView` mount'ta `map:full` acquire / unmount'ta release (heading-up harita gerçekten heading kullanır); `MiniMapWidget` YALNIZ kendi mevcut `isDriving` histerezisinde (giriş >5 km/h, çıkış <3 km/h) `map:mini` acquire — park/kuzey-yukarı statikte talep YOK (yeni eşik eklenmedi). **KORUNAN:** GPS konum takibi (talep yokken watch aynen sürer, yalnız compass kapanır), blend/smoothing (`computeBlendedHeading`/`applyCompassSmoothing`/`COMPASS_THROTTLE_MS`), izin ve fallback yolu, background/foreground gate (visibility) davranışı — HİÇBİRİ değişmez. **YALIN:** owner bounded(32), duplicate acquire yok, release idempotent, timer/polling YOK, import yan etkisiz, gate'in sahibi değil, zero-leak (stopGPSTracking demand unsub'ı bırakır; MiniMap unmount cleanup'ı release eder). ⚠️ **Native samplingPeriod düşüşü İDDİA EDİLMEZ** (Chromium DeviceOrientation 60 Hz sabit); Generic Sensor API EKLENMEZ. Kapsam-dışı dokunulmadı: blackBox, smartDriving, FullMapView PR #61, GPS PR #62, Platform Kernel PR #55, SystemBoot, OBD/CAN, SQL. | `platform/gps/compassDemand.ts` (yeni), `platform/gpsService.ts` (compass talep-güdümlü), `components/map/FullMapView.tsx` (acquire/release), `components/map/MiniMapWidget.tsx` (isDriving'e bağlı acquire/release), `__tests__/compassForegroundDemand.test.ts` (yeni, 29 test), `__tests__/orientationDemandGating.test.ts` (PR #58 1/3/5/6/7 kilitleri yeni talep-güdümlü davranışa güncellendi) | Gerçek Xiaomi cihazda (önce/sonra, `dumpsys sensorservice` + idle CPU/`SensorsHandlerThread`): (1) **Ayarlar/haritasız ekranda** uygulama adına `rot_vec`/`game_rotvec` (rotation-vector/game-rotation) sensörleri AÇIK OLMAMASI; (2) harita/navigasyon açılınca compass sensörlerinin GERİ GELMESİ; (3) heading/pusula yönünün DOĞRU olması; (4) harita kapanınca compass sensörlerinin SÖKÜLMESİ; (5) background davranışının BOZULMAMASI; (6) duplicate listener OLMAMASI; (7) Xiaomi'de idle CPU ve `SensorsHandlerThread`'in önce/sonra ölçülmesi (düşüş beklenir); (8) Mali-400'de regresyon yok. ⚠️ **Mali-400 cihaz doğrulaması bekliyor.** Test edilmeden 🟢 yapma. | 2026-07-11 |
| 42 | **🔴 Device Validation Pending — Orientation Sensor Demand Gating (PR 3/3)** — İki always-on tüketiciyi (gpsService compass + smartDrivingEngine accelerometer) talep-güdümlü hale getirir. **ACCEL (gerçek foreground demand-gate):** `_accelMagnitude` YALNIZ `detectDrivingMode` Kademe 4'te (OBD+GPS+decayed hepsi yok/stale) okunuyor → yardımcı son-çare. `_evaluateAccelDemand()` yalnız TAZE güvenilir hız kaynağı YOKKEN gate'ten `subscribeMotion` ile abone olur; taze kaynak (OBD **veya** GPS — `recordSpeed` ile beslenen `_lastSpeedEstimate`, yaş>DECAY_MAX_SEC=stale) varken release. `attachAccelerometer` fail-safe (kaynak yoksa acquire), `recordSpeed` her çağrıda re-eval, `detachAccelerometer` release. İvmeölçer TEK BAŞINA safety-critical/kesin hareket üretmez (yalnız Kademe 4 ipucu). Magic number YOK (DECAY_MAX_SEC mevcut sabit). **COMPASS (gpsService):** `_startCompassListener` ham `window.addEventListener` → gate `subscribeOrientationAbsolute/subscribeOrientation`; mevcut JS throttle (COMPASS_THROTTLE_MS, mevcut) + heading/smoothing/blend davranışı KORUNUR. Kazanım: **background auto-pause** (gate visibility hidden→fiziksel listener söker) + dedup. **YALIN:** yalnız 2 dosya değişti; yeni timer/rAF/polling YOK, Generic Sensor API YOK, yeni JS throttle YOK, yeni bağımlılık yok, import yan etkisiz; gate'in sahibi değil. ⚠️ **DÜRÜST SINIRLAMALAR:** (1) **Compass Settings ekranında KAPATILMADI** — foreground Settings-gating "harita ekranda mı" tüketici sinyali gerektirir (global görünüm store'u yok, MainLayout lokal `drawer` state'i; tüketiciye dokunmak KAPSAM DIŞI). Compass foreground davranışı bilinçli KORUNDU (map-heading-while-not-navigating bozulmasın). (2) **Accel mid-drive kaynak kaybı** (fresh sonra stale) re-acquire'ı sonraki `recordSpeed`/attach-döngüsünde olur (timer yok) — accel non-critical fallback + decay(20s)+Kademe5 kapsar. ⚠️ **Bu PR native samplingPeriod düşüşü İDDİA ETMEZ.** | `platform/gpsService.ts` (compass→gate), `platform/smartDrivingEngine.ts` (accel→gate+demand), `__tests__/orientationDemandGating.test.ts` (yeni, 26 test), `__tests__/orientationGateConsumerMigration.test.ts` (PR2 #21/#22 scope-kilidi yeni davranışa güncellendi) | Gerçek Xiaomi cihazda (önce/sonra): (1) sürüş sırasında (GPS/OBD taze) `dumpsys sensorservice` altında smartDriving accel `devicemotion` kaydı KALMAMASI; (2) GPS fix yok + OBD yok senaryosunda accel fallback'in ÇALIŞMASI (mod tespiti sürüyor); (3) background'da (app hidden) compass+accel fiziksel listener'ın SÖKÜLMESİ; (4) foreground dönüşünde geri bağlanması; (5) heading/pusula/navigation bearing DEĞİŞMEMESİ; (6) duplicate listener/cleanup leak OLMAMASI; (7) idle CPU (SensorsHandlerThread+CrRendererMain) sürüş+background senaryolarında düşüşü; (8) BASIC_JS/Mali-400'de regresyon yok. ⚠️ **Compass Settings-gating bu PR'da YOK (tüketici demand sinyali gerektirir — ayrı iş). Mali-400 cihaz doğrulaması bekliyor.** Test edilmeden 🟢 yapma. | 2026-07-11 |
| 41 | **🔴 Device Validation Pending — Orientation Sensor Gate On-Demand Consumer Migration (PR 2/3)** — Yaşam-döngüsüyle açılıp kapanan DÖRT sensör tüketicisini ham `window.addEventListener` yerine Orientation Sensor Gate (#40) üzerinden çalıştırır. **Taşınanlar:** `arAlignmentService` (deviceorientationabsolute+deviceorientation+devicemotion → `subscribeOrientationAbsolute/subscribeOrientation/subscribeMotion`, 3 release ref, capture-fazı kaldırıldı=pencere-hedefli device event teslimatı faz-bağımsız), `dashcamService` (devicemotion → `subscribeMotion`, `_motionRelease`), `blackBoxService._startAccelerometer` (gate release aynen döner→`_accelUnsub` sözleşmesi değişmez), `deviceApi.subscribeToAccelerometer` (gate release döner→sentryEngine `_unsubAccel` yolu değişmez). **Model:** her tüketici gate release'i kendi lifecycle alanında tutar; start/stop İDEMPOTENT (mevcut guard'lar korundu); duplicate acquire yok; stop/dispose'ta release; gate'in SAHİBİ DEĞİL, gate'i dispose ETMEZ. **Davranış korundu:** handler'lar/callback sırası/işaretleri/smoothing/filtering/permission (feature-detect `'DeviceMotionEvent' in window` korundu, iOS requestPermission EKLENMEDİ) aynı; event payload aynı callback'e ulaşır. **Visibility:** tüketiciler kendi visibilitychange listener'ını EKLEMEZ (gate yönetir; çift listener yok). **Kazanım:** aynı ham event için birden fazla fiziksel JS listener yerine tek gate listener + background'da otomatik pause + temiz ownership/cleanup. **YALIN:** event başına yeni allocation yok, yeni timer/rAF/polling yok, callback gövdesi büyümedi, yeni bağımlılık yok, import yan etkisiz. ⚠️ **gpsService ve smartDrivingEngine bu PR'da DEĞİŞMEDİ** (hâlâ always-on) → **Settings ekranında native sensör kaydı ve native 60 Hz sampling DEVAM EDEBİLİR; bu PR native samplingPeriod'u düşürdüğünü İDDİA ETMEZ ve Settings CPU sorununu ÇÖZMEZ** — bunlar PR 3 kapsamı (always-on talep-güdümlü acquire). | `platform/arAlignmentService.ts`, `platform/dashcamService.ts`, `platform/security/blackBoxService.ts`, `platform/deviceApi.ts`, `__tests__/orientationGateConsumerMigration.test.ts` (yeni, 30 test) | Gerçek cihazda (AR navigasyon/dashcam/black-box/sentry aktifken): (1) AR/Dashcam/BlackBox/Sentry aktifken sensör olayları çalışıyor (heading/G-force/crash-detect aynı); (2) özellik durduğunda ilgili consumer release oluyor; (3) birden fazla consumer tek fiziksel listener paylaşıyor; (4) background'da (visibility hidden) fiziksel listener sökülüyor; (5) foreground dönüşünde yalnız aktif consumer geri bağlanıyor; (6) duplicate listener/cleanup leak yok; (7) BASIC_JS/Mali-400'de regresyon yok. ⚠️ **gpsService ve smartDrivingEngine bu PR KAPSAMI DIŞI; Settings ve native 60 Hz sorunu PR 3'te çözülür. Mali-400 cihaz doğrulaması bekliyor.** Test edilmeden 🟢 yapma. | 2026-07-11 |
| 40 | **🔴 Device Validation Pending — Orientation Sensor Subscription Gate Foundation (PR 1/3)** — Cihaz yön/hareket Web event'leri (`deviceorientationabsolute`/`deviceorientation`/`devicemotion`) için MERKEZİ, ref-count'lu abonelik kapısı (multiplexer). Bugün bu event'lere ≥6 servis (gpsService compass, smartDrivingEngine, arAlignmentService, dashcamService, blackBoxService, deviceApi) AYRI `addEventListener` ile abone; ikisi (compass + accelerometer) kök layout'ta app-ömrü bağlı → Ayarlar dahil her ekranda sensör aktif (QA: `dumpsys sensorservice` samplingPeriod 16.67ms=60Hz, SensorsHandlerThread ~%24 + CrRendererMain ~%21). Kapı: **event türü başına TEK fiziksel window listener** (dedup); callback Set ile ref-count (ilk consumer→bağla, son→sök); `visibilityState!=='visible'`→fiziksel listener sökülür, görünür+aktif consumer→yeniden bağlanır; deterministik fan-out (insertion order, kararlı dizi snapshot'ı); **event başına YENİ array/object allocation YOK** (index'li döngü); callback exception İZOLE (fail-soft, diğerleri sürer); public API throw ETMEZ; bounded (kanal başı 64, aşım fail-soft no-op+droppedCount); dispose zero-leak + sonrası güvenli no-op; reset yeniden kullanılabilir. API: subscribeOrientationAbsolute/subscribeOrientation/subscribeMotion (idempotent release), getSubscriberCounts, getStatus, reset, dispose. **YALIN:** HİÇ import YOK (tam bağımsız→import yan etkisiz); modül seviyesinde listener kurulmaz (ilk abonelikte `visibilitychange` bağlanır); timer/rAF/polling YOK; Generic Sensor API YOK; JS throttle YOK; permission tüketicide kalır. ⚠️ **KAPSAM: foundation — HİÇBİR tüketici bu kapıya bağlanmadı (wiring YOK).** ⚠️ **Bu PR tek başına CPU veya samplingPeriod DÜŞÜRMEZ.** Gerçek kazanım PR 2 (on-demand tüketiciler) ve özellikle PR 3 (always-on compass + smartDriving talep-güdümlü acquire + background pause) wiring'inden SONRA ölçülebilir. **Legacy event API aktifken native sampling 60 Hz OLABİLİR** (frekans parametresi yok — kanıt: gpsService zaten JS'te 10Hz throttle'lı ama dumpsys 60Hz). | `platform/sensors/orientationSensorGate.ts` (yeni), `platform/sensors/index.ts` (yeni), `__tests__/orientationSensorGate.test.ts` (yeni, 30 test), `__tests__/regression.guards.test.ts` (gate zero-leak + bağımsızlık kilidi) | Gerçek cihazda (tüketici wiring PR 2/3 bağlandıktan SONRA): (1) Ayarlar ekranında `dumpsys sensorservice` altında CockpitOS için orientation/motion 60 Hz kaydı kalmaması; (2) background'da (visibility hidden) sensör kaydı kalmaması; (3) harita/navigation açılınca sensörün yeniden çalışması; (4) heading/pusula davranışının korunması; (5) duplicate listener ve cleanup sızıntısı olmaması; (6) idle CPU'nun (SensorsHandlerThread + CrRendererMain) Ayarlar ve background senaryolarında belirgin düşmesi; (7) BASIC_JS/Mali-400'de regresyon olmaması. ⚠️ **PR 1 tek başına ölçülebilir kazanç VERMEZ — foundation. Mali-400 cihaz doğrulaması bekliyor.** Test edilmeden 🟢 yapma. | 2026-07-11 |
| 39 | **🔴 Device Validation Pending — Platform Core Event Bridge Foundation** — Vehicle HAL ve Capability Registry'deki GERÇEK değişiklikleri tip-güvenli, bounded, SALT-OKUNUR biçimde Platform Event Bus'a aktaran İKİ köprü. **Vehicle:** `createVehicleHalEventBridge({hal,bus})` — HAL `subscribe` TAM snapshot verir (per-signal delta yok); köprü snapshot'ı kendi son-değer haritasıyla DIFF'leyip yalnız değişen supported sinyalleri yayınlar. `vehicle.signal.changed` **TRANSIENT** (history dışı, küçük payload: signalId/value özeti/quality/confidence/source/stale/supported/timestamp; **full HAL snapshot/raw CAN TAŞINMAZ**; O(1) dedup, hot-path allocation yok). `vehicle.ignition.changed` (retained) yalnız ignition supported+boolean değişince — **foundation'da ignition kaynağı YOK→supported=false→ASLA yayınlanmaz**. `vehicle.connection.changed` (retained) "en az bir supported sinyal" geçişinde (HAL'e dayalı gerçek olgu; veri yoksa false→SESSİZ). `vehicle.identity.changed` (retained) yalnız identity.supported (fingerprint) değişince (ham fingerprint yalnız metadata, payloadta DEĞİL). `vehicle.health.changed` YAYINLANMAZ (HAL'de health kaynağı yok→uydurulmaz). **Capability:** `createCapabilityEventBridge({registry,bus})` — Registry change event yalnız `{type,id,revision,at}` verir; köprü record'u `getCapability(id)` ile çeker. `capability.record.registered/changed/removed` (changed imza-dedup: aynıysa event yok); `capability.snapshot.changed` **RETAINED, küçük payload** {revision, counts, changedIds(bounded)} — **full registry TAŞINMAZ**; sayımlar start'ta BİR KEZ `createSnapshot()` ile tohumlanır, sonra INCREMENTAL O(1) (**değişiklik başına snapshot allocation YOK**); record payload whitelist (id/domain/status/available/quality/confidence/source/stale/reason/limitations bounded, Registry'ce zaten sanitize). **Ortak:** start/stop/dispose İDEMPOTENT; tek abonelik; **kaynakların SAHİBİ DEĞİL→dispose HAL/Registry/Bus'ı dispose ETMEZ**; dispose sonrası callback no-op; publishedCount/droppedCount (Bus reddi/publish hatası→droppedCount, kaynak servis etkilenmez); fail-soft (HAL/Registry subscribe hatası, tek bozuk signal/record izole, public API throw etmez); privacy (VIN/MAC/koordinat/ham CAN/anahtar event'e girmez); immutable payload; katalog event isimleri; HAL/Registry/Bus'ı DOĞRUDAN import ETMEZ (yalnız TYPE→import yan etkisiz); **timer/polling YOK, yeni bağımlılık YOK, global singleton YOK**. Güvenlik KARARI üretmez (yalnız taşır); Event Bus recursion/priority/bounded kuralları DEĞİŞMEZ. **SystemBoot wiring · provider adapter auto-start · Platform Kernel wiring YOK — AYRI PR.** | `platform/eventBus/bridges/vehicleHalEventBridge.ts` (yeni), `platform/eventBus/bridges/capabilityEventBridge.ts` (yeni), `platform/eventBus/bridges/index.ts` (yeni), `platform/eventBus/index.ts` (export), `__tests__/vehicleHalEventBridge.test.ts` (yeni, ~24 test), `__tests__/capabilityEventBridge.test.ts` (yeni, ~24 test) | Gerçek cihazda (SystemBoot/Platform Kernel wiring PR'ı köprüleri gerçek HAL/Registry/Bus'a bağladıktan SONRA): (1) gerçek araç sinyal değişiklikleri Event Bus'a **duplicate olmadan** ulaşıyor; (2) 3 Hz sinyaller (vehicle.signal.changed transient) history/RAM ŞİŞİRMİYOR; (3) capability değişiklikleri doğru event'lerle (registered/changed/removed/snapshot) yayınlanıyor; (4) kaynağı olmayan ignition/connection event'i UYDURULMUYOR (foundation'da sessiz); (5) listener/publish hatası HAL veya Registry'yi ETKİLEMİYOR (droppedCount artar); (6) stop/dispose sonrası abonelik sızıntısı yok; (7) BASIC_JS/Mali-400'de CPU/FPS regresyonu yok (O(1) dedup, timer yok, full snapshot allocation yok, pasifken sıfır yük). ⚠️ **SystemBoot/Platform Kernel wiring AYRI PR.** Test edilmeden 🟢 yapma. | 2026-07-11 |
| 38 | **🔴 Device Validation Pending — Platform Core Real Providers Foundation** — Vehicle HAL Provider Adapter ve Capability Provider Adapter için İLK gerçek, SALT-OKUNUR provider implementasyonları. **Vehicle:** `createUnifiedVehicleStoreProvider({store})` mevcut `useUnifiedVehicleStore`'u (fused worker/CAN/OBD/GPS) adapter'ın `VehicleStoreSource`'ına (getSnapshot/subscribe) uyarlar — store SALT-OKUNUR, her snapshot YENİ dondurulmuş alt-küme (speed/rpm/fuel/odometer/reverse + CAN extras), `rpm undefined→null`, **TPMS yalnız gerçek 4-elemanlı numerik veri varsa**, **ignition EXPOSE EDİLMEZ** (kaynak yok), metadata UYDURULMAZ; store DOĞRUDAN import EDİLMEZ (yapısal DI→import yan etkisiz); provider oluşturulmadan abone OLMAZ; subscribe idempotent unsub + dispose zero-leak. **Capability:** `createRuntimeCapabilityProviders({env,probes})` yalnız KANITLANABİLİR provider üretir. **Kanıt sınıfı:** browser-API presence (geolocation/mediaDevices/bluetooth) → `degraded`/düşük conf (donanım KESİNLEŞMEZ), API yok→unknown; `navigator.connection.type` kesin wifi/cellular→available, aksi unknown; mikrofon izin granted olmadıkça available DEĞİL (denied→restricted); native secure storage authoritative→available/yüksek conf; modül runtime (deep_scan/vehicle_learning/assistant_context/ota/safety_kernel/offline_commands/offline_conversation/push/cloud_commands): **modül var ama runtime bağlı değil→degraded/experimental**, runtime hazır→available, yok→unknown; AI BYOK (gemini/groq/claude/cloud): configured+usable→available, configured→degraded, değil→unavailable; offline map/routing gerçek paket/graf yoksa→unavailable. **KAYNAK YOK:** `ai.grok`(xAI entegrasyonu yok→provider HİÇ ÜRETİLMEZ), `ai.local_model`(gerçek yerel LLM yok→yalnız high-tier+probe, low/mid'de OLUŞTURULMAZ). **Runtime readiness ≠ module existence** (dosya/export var tek başına available DEĞİL). Fabrikalar DI; **global singleton YOK**; probe/navigator yalnız `read()`'te (fabrika yan etkisiz); ağır modül IMPORT YOK (yalnız TYPE); timer/polling YOK; her read try/catch (throw etmez); sonuçlar dondurulmuş; privacy (VIN/MAC/anahtar probe payload'ından sızmaz). **SystemBoot wiring · Event Bus publish · Registry auto-start · UI · Assistant Context değişikliği YOK — hepsi AYRI PR.** | `platform/vehicleHal/providers/unifiedVehicleStoreProvider.ts` (yeni), `platform/vehicleHal/providers/index.ts` (yeni), `platform/vehicleHal/index.ts` (export), `platform/capability/providers/runtimeCapabilityProviders.ts` (yeni), `platform/capability/providers/index.ts` (yeni), `platform/capability/index.ts` (export), `__tests__/unifiedVehicleStoreProvider.test.ts` (yeni, ~16 test), `__tests__/runtimeCapabilityProviders.test.ts` (yeni, ~38 test) | Gerçek cihazda (SystemBoot/Registry/HAL wiring PR'ı bağlandıktan SONRA): (1) gerçek araç state'i (hız/RPM/soğutma/yakıt/voltaj) Vehicle HAL Provider Adapter'a doğru aktarılıyor; (2) GPS/Bluetooth/Wi-Fi/cellular capability sonuçları gerçek cihaz API'leriyle uyumlu; (3) izni olmayan özellik (mikrofon) available GÖRÜNMÜYOR; (4) secure storage yalnız gerçek native kanıtla available; (5) Grok/local model/offline map olmayan cihazda available GÖRÜNMÜYOR (grok provider bile yok); (6) provider stop/dispose sonrası subscription sızıntısı yok; (7) BASIC_JS/Mali-400'de CPU/RAM regresyonu yok (fabrika yan etkisiz, timer yok, pasifken sıfır yük). ⚠️ **SystemBoot/Event Bus wiring AYRI PR.** Test edilmeden 🟢 yapma. | 2026-07-11 |
| 37 | **🔴 Device Validation Pending — Capability Provider Adapter Foundation** — Gerçek runtime/cihaz/platform kanıtlarını SALT-OKUNUR biçimde Capability Registry kayıtlarına aktaran provider adapter köprüsü (gerçek kanıt → adapter → Registry). ⚠️ **KOD GERÇEĞİ (salt-okunur analiz):** doğrulanabilir kaynaklar sınırlı — `deviceCapabilities`(WebGL/CSS/SAB/cores browser probe→device/platform) · `vehicleHal.getCapability/hasSignal`(araç sinyali, foundation'da hepsi false) · browser API(gps/mic/bluetooth/storage → yalnız "API var", donanım erişimini KESİNLEŞTİRMEZ) · config/lisans(oem.*/ai-provider BYOK anahtarı). **Runtime kaynağı HİÇ YOK:** `vehicle.ignition`(kontak kaynağı yok)·`ai.grok`(xAI yok)·`navigation.offline_map`(tile paketi yoksa) → unknown/unavailable KALIR. UI/dosya/modül varlığı kanıt DEĞİL. **Adapter:** donanımı DOĞRUDAN taramaz — yalnız DI `providers`'ı okur (foundation'da YERLEŞİK gerçek provider YOK; gerçek prob'lar ayrı wiring PR'ı). **Provider sözleşmesi:** `{id, domain, source, authoritative?, refreshPolicy?, read()}` (read sync/async, kaynak yoksa null). Provider sonucu→Registry evidence'ına normalize→`resolveCapability(id, evidence, domain)` (status'ü REGISTRY çözer — karar kuralları DEĞİŞMEZ). **YALNIZ değişeni günceller** (imza-tabanlı dedup→duplicate Registry update yok, O(1)). **API:** start/refresh/refreshProvider/stop/getStatus/getLastRefreshAt/getProviderResults/reset/dispose. **Refresh:** start'ta ilk refresh + açık çağrı; **steady-state timer/polling YOK**; provider'lar PARALEL (`Promise.allSettled` → biri diğerini bloke etmez); opt-in DI timeout (settle'da temizlenir→leak yok), timeout/hata/null→güvenli **unknown**. **DeviceTier:** varlık uydurmaz — Registry tier engelini uygular (low'da ai.local_model restricted); high tier kanıtsız açmaz. **Fail-soft:** provider throw/Registry update hatası izole, public API throw etmez, dispose sonrası no-op. **Gizlilik:** provider/details/reason sanitize (VIN/MAC/koordinat/secret/API-key sızmaz). **Immutable:** getProviderResults dondurulmuş kopya (nested dahil), girdi mutate edilmez. **YALIN:** Registry'yi DOĞRUDAN import ETMEZ (yapısal DI + type-only import→import yan etkisiz); Event Bus'a yayınlamaz; SystemBoot'a bağlanmaz; persistence/cloud yok; native tarama/OBD-CAN sorgusu yok; yeni bağımlılık yok; provider bounded 128. Registry'nin SAHİBİ DEĞİL→dispose Registry'yi dispose etmez. | `platform/capability/capabilityProviderAdapter.ts` (yeni), `platform/capability/index.ts` (export), `__tests__/capabilityProviderAdapter.test.ts` (yeni, 41 test) | Gerçek cihazda (gerçek provider wiring PR'ı bağlandıktan SONRA): (1) GPS/Bluetooth/Wi-Fi/kamera capability'leri doğru doğrulanıyor (native kanıtla available); (2) donanımı olmayan özellik available GÖRÜNMÜYOR; (3) gerçek kontak kaynağı olmayan cihazda `vehicle.ignition` unknown kalıyor; (4) TPMS kaynağı olmayan araçta available görünmüyor; (5) provider hatası Registry'yi/diğer provider'ları çökertmiyor; (6) aynı kanıt tekrarında duplicate capability update yok; (7) BASIC_JS/Mali-400'de CPU/RAM regresyonu yok (O(1) dedup, timer yok, paralel bounded read). ⚠️ **Gerçek provider implementasyonları · Event Bus · SystemBoot wiring · persistence AYRI PR'lar.** Test edilmeden 🟢 yapma. | 2026-07-11 |
| 36 | **🔴 Device Validation Pending — Vehicle HAL Provider Adapter Foundation** — Mevcut normalize araç durumunu (UnifiedVehicleStore fused worker/CAN/OBD/GPS) Vehicle HAL sinyal modeline aktaran ilk resmi SALT-OKUNUR provider adapter köprüsü. ⚠️ **KOD GERÇEĞİ (salt-okunur analiz):** UnifiedVehicleStore şu alanları taşır — `speed`(km/h,null=yok)·`rpm`(fused/`canRpm`)·`fuel`(%)·`odometer`·`reverse` + CAN extras `canCoolantTemp`/`canOilTemp`/`canThrottle`/`canBatteryVolt`/`canGearPos`/`canAmbientTemp`/`canTpmsKpa`/`canDoorOpen`/`canParkingBrake`; **per-signal source/quality/confidence metadata YOK** (fused nativeSource bile store'da yok); **gerçek ignition kaynağı YOK.** Store zaten dirty-check ile dedupe eder. **Aktarılan sinyaller:** speed/rpm/fuel_level/odometer(>0)/coolant_temp/oil_temp/throttle/battery_voltage/gear/ambient_temp/tpms + reverse/door_state/parking_brake (yalnız aktif araç varken=en az bir numerik sinyal mevcut). **supported=false KALAN:** `vehicle.ignition` (kaynak yok, ASLA ingest edilmez), TPMS (canTpmsKpa null iken). **Metadata yok → dürüst eşleme:** CAN-adlı store alanları `source=can`(quality high, confidence 0.9, VAL CAN base ~0.92 gerekçeli), füzyon alanları `source=inferred`(quality medium, 0.6); **magic confidence uydurulmadı** (VAL SIGNAL_SOURCE_DEFAULTS taban gerekçesi). Nullable numerik null→ingest YOK; boolean sinyaller default false'u kaynak sanmamak için aktif-araç kapısına tabi. **Değişiklik modeli:** adapter kendi son-değer haritasıyla YALNIZ değişeni ingest eder (duplicate yok, O(1), tam snapshot clone YOK, hot-path allocation yok). **Lifecycle:** start/stop/refresh/dispose İDEMPOTENT; duplicate subscription yok; store aboneliğini kullanır (yeni timer/polling YOK); dispose sonrası callback no-op; **HAL'in SAHİBİ DEĞİL → dispose HAL'i dispose ETMEZ.** **Fail-soft:** store okunamazsa/HAL ingest hatasında/tek sinyal bozukken diğerleri sürer, public API throw etmez. **Gizlilik:** yalnız numerik/boolean/array değer aktarılır (VIN/MAC/koordinat/ham/kimlik YOK). **YALIN:** store'u DOĞRUDAN import ETMEZ (yapısal `source` DI → import yan etkisiz); Event Bus'a yayınlamaz; Capability Registry'yi güncellemez; SystemBoot'a bağlanmaz; native/OBD/CAN sorgusu başlatmaz; poll frekansı değiştirmez; yeni bağımlılık yok. | `platform/vehicleHal/vehicleHalProviderAdapter.ts` (yeni), `platform/vehicleHal/index.ts` (export), `__tests__/vehicleHalProviderAdapter.test.ts` (yeni, 32 test) | Gerçek araçta (wiring PR'ı `useUnifiedVehicleStore.getState/subscribe`'ı bağladıktan SONRA): (1) hız/RPM/soğutma/yakıt/voltaj HAL'de kaynakla uyumlu (`getSpeed()` vb. gerçek değer); (2) aynı sinyal tekrarında duplicate update yok; (3) kaynağı olmayan ignition ve (verisiz) TPMS destekleniyor GÖRÜNMEZ; (4) reconnect sonrası abonelik duplicate olmuyor (start idempotent); (5) adapter stop/dispose sonrası callback kalmıyor; (6) BASIC_JS/Mali-400'de CPU/FPS regresyonu yok (O(1) değişiklik kontrolü, timer yok, mevcut subscribe akışı). ⚠️ **Event Bus · Capability Registry · SystemBoot wiring AYRI PR'lar.** Test edilmeden 🟢 yapma. | 2026-07-11 |
| 35 | **🔴 Device Validation Pending — Platform Event Bus Foundation** — Platform modüllerinin birbirini DOĞRUDAN çağırması yerine tip-güvenli, bounded, fail-soft, izole bir olay omurgası üzerinden haberleşmesini sağlayan foundation. Bugün olay/abonelik ≥5 desende parçalı (≥40 servis kendi `_listeners=new Set`'ini tutuyor; DOM CustomEvent; Zustand subscribe; mini-bus errorBus/VehicleEventHub; native listener), ORTAK event-adı standardı YOK. `PlatformEventBus` (createPlatformEventBus DI) bunu birleştirir. **Model:** 6 priority (safety/critical/high/normal/low/background) · 16 domain · 15 source · PlatformEvent(id/name/domain/source/priority/timestamp/**sequence**/payload/correlation/causation/vehicleFingerprintHash/replayable/retained/experimental). **İsim standardı** `domain.entity.action` (regex zorunlu). **~43 event katalog** (platform/vehicle/capability/deep_scan/health/navigation/eagle_eye/assistant/remote/fleet) — GERÇEK modüle BAĞLI DEĞİL (yalnız isim+contract). **API:** publish/publishName/subscribe/subscribeDomain/once/unsubscribe(idempotent)/hasSubscribers/listenerCount/getStats/getRecentEvents/clearRecentEvents/reset/dispose. **Dispatch:** listener'lar subscription-priority (yüksek→düşük) sonra kayıt sırası; **safety event re-entrant kuyrukta ÖNCE** dispatch (öncelikli drain); listener exception İZOLE (safety dahil engellenmez). **Sequence** monotonik (saat-geri'de bile artar). **Retained** (vehicle.connection/ignition/health.thermal/capability.snapshot) son değer tutar, yeni abone `replayLast` OPT-IN ile alır (varsayılan replay YOK), bounded. **History** bounded (256, BASIC_JS düşük config), **transient** (vehicle.signal.changed 3Hz) history DIŞI, priority-aware eviction (background önce, **safety KORUNUR**), yalnız sanitize özet. **Recursion** bounded depth (16) → aşım drop+recursionDropCount. **Gizlilik:** VIN/MAC/koordinat/ham hex/secret event'e giremez (vehicleFingerprintHash yalnız anonim 8-64 hex, VIN reddedilir; history payload özeti sanitize). **YALIN:** güvenlik KARARI ÜRETMEZ (yalnız taşır); SystemBoot'a BAĞLANMAZ; GLOBAL SINGLETON YOK (yalnız factory/class); **HİÇ import YOK** (tamamen bağımsız); timer YOK; polling YOK; import yan etkisiz; Map/Set O(1); hot-path'te JSON stringify YOK; yeni bağımlılık yok. Bounded (event-adı 256/listener 512/event-başına 64/domain 32/history 256/retained 32); immutable (event+payload deep-freeze, stats/history frozen); fail-soft (bozuk event reddedilir, dispose sonrası no-op, listener içinde unsubscribe/dispose güvenli). Gerçek modül davranışı DEĞİŞMEZ. | `platform/eventBus/platformEventBus.ts` (yeni), `platform/eventBus/index.ts` (yeni), `__tests__/platformEventBus.test.ts` (yeni, 46 test) | Gerçek cihazda (modül wiring PR'ları bağlandıktan SONRA): (1) event sırası deterministik; (2) duplicate listener oluşmuyor; (3) bir listener exception diğer modülleri etkilemiyor; (4) safety event normal event'ten önce teslim; (5) yüksek frekanslı transient event (vehicle.signal) history/RAM şişirmiyor; (6) dispose sonrası listener/timer sızıntısı yok; (7) BASIC_JS/Mali-400'de publish CPU/FPS regresyonu yok (O(1), timer yok, hot-path'te stringify yok). ⚠️ **Capability Registry/Vehicle HAL/Deep Scan wiring + event migration + persistence AYRI PR'lar.** Test edilmeden 🟢 yapma. | 2026-07-11 |
| 34 | **🔴 Device Validation Pending — Vehicle HAL Foundation** — CarOS Pro'daki TÜM modüllerin araçla YALNIZ bu katman üzerinden konuştuğu tek resmi Hardware Abstraction Layer. Bugün araç verisi Native VHAL/CAN(Hiworld/K24/NWD)/OBD/GPS → SignalNormalizer → VAL → VehicleCompute.worker → VehicleState → `useUnifiedVehicleStore` (**~40 dosyada** okunuyor) + Assistant Context + Battery Service + gauge SAB ile AYRI okunuyor; per-signal quality/confidence/source/stale/supported/unit veren ORTAK API yoktu. `VehicleHal` bu boşluğu doldurur. **15 sinyal kataloğu** (speed/rpm/coolant_temp/oil_temp/fuel_level/battery_voltage/throttle/ambient_temp/odometer/gear/reverse/ignition/door_state/parking_brake/tpms), her biri `supported=false` tohumlu (kaynak yok). **Signal modeli:** id·value·quality(low/medium/high/unknown)·confidence(0-1)·source(native/can/obd/deep_scan/inferred/none)·timestamp·stale·unit·supported. **Snapshot:** revision·updatedAt·signals(deep-freeze, id-sıralı). **API:** getSnapshot/getSignal(id)/hasSignal/subscribe/unsubscribe/refresh/reset/dispose + tipli erişimciler (getSpeed/getRPM/getCoolantTemp/getFuelLevel/getBatteryVoltage/getIgnition/getGear/getDoorState/getReverse/getThrottle/getOdometer/getTpms) + getVehicleIdentity + getCapability(HAL-yerel). **HAL ilkesi:** donanımdan bağımsız, KAYNAĞI GİZLER, aynı API her araçta çalışır, veri yoksa null/supported=false (fail-closed), hiçbir tüketici CAN/ELM327/Hiworld bilmez. **Veri ÜRETMEZ/YORUMLAMAZ** — yalnız tip doğrulama (number sinyale boolean/NaN→null), magic sanity eşiği YOK. **Gizlilik:** HAM VIN reddedilir (getVehicleIdentity yalnız fingerprint hash 8-64 hex, 17-karakter=VIN reddedilir), protokol sanitize, ham CAN frame/OBD response SAKLANMAZ. **YALIN:** SystemBoot/native/VAL/UnifiedVehicleStore'a BAĞLANMAZ (import YOK — tamamen bağımsız foundation); timer YOK; polling YOK; import yan etkisiz; yeni bağımlılık yok; Map O(1). Bounded (katalog sabit 15 · listener 32 · tpms 6); immutable (snapshot/signal deep-freeze); fail-soft (provider/listener izole, throw yok); duplicate event önleme (değişmezse emit yok); stale (yaş>5000ms veya saat-geri). ⚠️ **Gerçek kaynak wiring (VAL/UnifiedVehicleStore → HAL.ingest) + modüllerin HAL'e taşınması AYRI PR'lar.** | `platform/vehicleHal/vehicleHal.ts` (yeni), `platform/vehicleHal/index.ts` (yeni), `__tests__/vehicleHal.test.ts` (yeni, 35 test) | Gerçek araçta (kaynak wiring PR'ı bağlandıktan SONRA): (1) `getSpeed()/getRPM()/getBatteryVoltage()` gerçek değer döner (CAN/OBD/HAL kaynağından, tüketici kaynağı BİLMEDEN); (2) kaynağı olmayan sinyal (ör. `getIgnition()` şu an) null/supported=false; (3) sinyal eskirse stale=true (taze gösterilmez); (4) bir provider düşse HAL çökmez, diğer sinyaller okunur; (5) HAM VIN hiçbir yere sızmaz (yalnız fingerprint hash); (6) BASIC_JS/Mali-400'de HAL CPU/RAM regresyonu yok (O(1) lookup, timer yok, pasifken sıfır yük); (7) tüm modüller CAN/OBD/Hiworld yerine yalnız HAL kullanır. ⚠️ **Gerçek kaynak wiring + modül taşıma SONRAKİ PR KAPSAMI.** Test edilmeden 🟢 yapma. | 2026-07-11 |
| 33 | **🔴 Device Validation Pending — Capability Registry Foundation** — CarOS Pro'nun cihaz/araç/yazılım/servis yeteneklerini TEK merkezden, KANITA DAYALI, confidence içeren, DeviceTier-uyumlu yöneten registry. İleride Deep Scan · Vehicle HAL · Navigation · Eagle Eye · Companion AI · Assistant Context · Health/Remote/Fleet/Vision/Media OS · OEM/White-label · Plugin Runtime ORTAK KAYNAK olarak kullanacak. `CapabilityRegistry` Map-tabanlı O(1); 13 domain · 6 status (available/unavailable/unknown/degraded/restricted/unsupported) · 12 source (native/can/obd/deep_scan/config/license/runtime/network/user/oem/inferred/none) · quality(low/medium/high/unknown) · confidence(0–1 clamp). **~63 ID'lik başlangıç kataloğu** (device/vehicle/navigation/ai/vision/remote/platform/oem/fleet) `unknown` olarak tohumlu (KANIT YOK). **Karar kuralları (zero-trust/fail-closed):** available YALNIZ yeterli kanıtla; unknown≠available; **config tek başına fiziksel donanımı (device/vehicle/vision) AÇMAZ** → unknown; UI varlığı kaynak DEĞİL; stale kanıt available tutmaz→unknown; **çelişkili authoritative kaynak→unknown**; authoritative(native/can/obd/deep_scan/license/oem) yardımcıdan üstün; **DeviceTier minimumu karşılanmazsa→restricted** (Mali-400/low'da advanced GPU + local model + ağır vision restricted); **safety-critical writable (vehicle.coding/adaptation/bidirectional) yalnız authoritative + confidence≥0.85**; tek zayıf inferred safety-critical açamaz; confidence<0.5/quality low→degraded. Confidence kaynak-tabanlı (native 0.98…config 0.3, politika sabiti). **Güçlü cihaz varlık UYDURMAZ** — tier yalnız minimum engelini kaldırır, kanıt yine şart. API: register/update/remove/get/has/isAvailable/getStatus/getConfidence/list/listByDomain/resolveCapability/createSnapshot/evaluateRequirements/explainUnavailable/subscribe/reset/dispose. **YALIN:** SystemBoot/provider/UI'ya BAĞLANMAZ; import yan etkisiz (yalnız `DeviceTier` TYPE import, donanım probu/timer/native YOK); kalıcı storage YOK (runtime-memory); yeni bağımlılık yok. Bounded (256 capability · 32 listener · details16/limitations8); immutable (record/snapshot deep-freeze, nested dahil); privacy (VIN/MAC/koordinat/ham/secret sanitize); fail-soft (bozuk kayıt/provider/listener izole, throw yok); duplicate merge deterministik; değişmezse event YOK. | `platform/capability/capabilityRegistry.ts` (yeni), `platform/capability/index.ts` (yeni), `__tests__/capabilityRegistry.test.ts` (yeni, 40 test) | Gerçek cihazda (hardware provider wiring PR'ı bağlandıktan SONRA): (1) GPS/kamera/Bluetooth/Wi-Fi capability durumları doğru (native kanıtla available); (2) donanımı olmayan özellik available GÖRÜNMEZ (unknown/unavailable); (3) TPMS kaynağı olmayan araçta `vehicle.tpms` aktifleşmez; (4) Deep Scan keşifleri araç capability'lerine (`vehicle.live_pid`/`did_discovery`…) doğru yansır; (5) BASIC_JS/Mali-400'de registry CPU/RAM regresyonu yok (O(1) lookup, timer yok, pasifken sıfır yük); (6) capability değişiklikleri duplicate event üretmez; (7) unknown capability güvenli şekilde kapalı davranır (isAvailable=false). ⚠️ **Provider wiring · persistence · OEM config · SystemBoot wiring AYRI PR KAPSAMI.** Test edilmeden 🟢 yapma. | 2026-07-11 |
| 32 | **🔴 Device Validation Pending — Deep Scan Orchestration Foundation** — Deep Scan'in tüm katmanlarını (Runtime state machine · Persistence · Ignition Source) yöneten TEK koordinatör. `DeepScanOrchestrator` deterministik 12-faz sırasını yürütür: kontak doğrula → mod seç (Persistence `resolveMode`) → identity → protocol → ECU/PID/DID/firmware keşif → capability → fingerprint → knowledge → evidence(+pattern+persistence checkpoint) → change_detection → report → completed. **Fazların GERÇEK işi enjekte edilen `handlers` ile yapılır** (foundation'da yok → tümü `skipped`); ileride gerçek discovery servisleri buraya BAĞLANIR. **Faz çıktısı** (success/skipped/error/timeout/cancelled) runtime'a yansıtılır: keşifler `recordEcu/Pid/Did/Firmware`, hata `reportPhaseFailure` (KRİTİK identity/protocol→failed; kritik-değil→skip+warn FAIL-SOFT devam). **Fail-soft zinciri:** fingerprint düşse knowledge çalışır · knowledge düşse persistence yazılır · persistence düşse report yine üretilir (runtime.completeScan ÖNCE, persistence.completeScan try/catch SONRA). **Kontak güvenliği:** her aktif faz öncesi Ignition Source'tan `getConfirmedValue()` okunur→runtime'a beslenir; doğrulanmazsa `waiting_for_ignition`'da DURUR (index ilerlemez, aktif sorgu yok), kontak gelince aynı fazdan devam. **Mod:** Persistence'tan tamamlanmış full_scan varsa `change_check`. **Progress** gerçek fazlara göre (runtime PHASE_PROGRESS_FLOOR + ölçülmüş updateProgress), sahte yüzde YOK, 100'de biter. **Event:** scan_started/phase_started/phase_completed/phase_failed/progress_changed/report_ready/scan_completed/scan_cancelled/scan_failed. **YALIN:** fazlar ÇAĞRILABİLİR API (`runNextPhase()` primitif + `run()` sonlu sıra); **timer YOK · yeni thread YOK · sürekli döngü YOK · hot-path YOK · OBD polling'e dokunmaz**; SystemBoot/Assistant/Dashboard wiring YOK; import yan etkisiz (runtime/persistence/ignition yalnız start/run'da kullanılır); yeni bağımlılık yok. Bounded (keşif birikimi ECU128/PID512/DID512/FW128 · listener32 · uyarı16); immutable (snapshot+event deep-freeze); privacy (sanitizeText, ham/VIN/MAC girmez); zero-leak (dispose yalnız kendi dinleyicileri; enjekte deps çağıranındır). Gerçek OBD komutu/PID/DID/Discovery algoritması/native DEĞİŞMEZ. | `platform/deepScan/deepScanOrchestrator.ts` (yeni), `platform/deepScan/index.ts` (export), `__tests__/deepScanOrchestrator.test.ts` (yeni, 21 test) | Gerçek araçta (gerçek discovery handler'ları bağlandıktan SONRA): (1) araç bağlanınca kontak doğrulanır → mod (full/change) seçilir; (2) faz sırası deterministik yürür, her faz tamamlandıkça progress artar (100'de biter); (3) kontak doğrulanmadan aktif sorgu BAŞLAMAZ (`waiting_for_ignition`); (4) tek modül düşse (firmware/fingerprint/knowledge/persistence) Deep Scan ÇÖKMEZ, uygun noktadan devam eder, report yine üretilir; (5) cancel/timeout doğru işlenir; (6) tarama sonu Persistence'a kayıt yazılır (sonraki bağlantı change_check); (7) BASIC_JS/Mali-400'de CPU/RAM regresyonu yok (timer yok, pasifken sıfır yük). ⚠️ **Gerçek discovery/OBD entegrasyonu + SystemBoot wiring + Assistant Context yansıtma SONRAKİ PR KAPSAMI.** Test edilmeden 🟢 yapma. | 2026-07-11 |
| 31 | **🔴 Device Validation Pending — Deep Scan Ignition Source Foundation** — Deep Scan Runtime'ın üç durumlu `ignitionConfirmed`'ını GÜVENLİ + deterministik besleyecek çözümleyici katman. ⚠️ **KOD GERÇEĞİ (salt-okunur denetim):** depoda DOĞRUDAN kontak/ACC/key-state yayan HİÇBİR kaynak YOK — VAL `NormalizedVehicleData`, Hiworld CAN protokolü (RPM/BATT_VOLT/GEAR/REVERSE var, ACC/IGN komutu YOK), NativeHAL (AAOS VHAL) ve `useAssistantContextStore` (`status.ignition=null` "sinyal YOK") teyit etti; `commandExecutor.ignitionOn` yalnız remote-command parametresi. `resolveIgnitionState(evidence, now)` SAF çözümleyici: yalnız **authoritative** kaynaklar (`native_acc`/`can_ignition`/`engine_running`/`manual`) onay verir; **yardımcı** kaynaklar (`rpm`/`alternator`/`battery_voltage`/`obd_transport`) tek başına ASLA ON üretmez → `unknown`. **Fail-closed:** kesin doğrulanamazsa `unknown`→`confirmed=null`. Tehlikeli tekil varsayım YOK (hız>0/RPM>0/voltaj/OBD bağlı → ON yasak). **Fizik magic number UYDURULMADI** (kodda motor-çalışıyor RPM eşiği / alternatör voltaj eşiği tanımlı değil → ham sayıya eşik uygulanmaz; yardımcı sinyaller çağıran tarafından bool'a yorumlanıp `engine_running` authoritative kanıtı olur = zero-trust). **Stale/zaman:** her kanıt `observedAt` (Date.now ms, VAL ile aynı taban); yaş>5000ms (VAL SIGNAL_SOURCE_DEFAULTS timeout gerekçesi) veya saat-geri (age<0) → stale, onay için kullanılmaz. **Çelişki:** iki güvenilir authoritative kaynak zıtsa → `unknown` (fail-closed). **Confidence kapısı** 0.7 (VAL base 0.85–0.98 gerekçesi, politika sabiti). **Adapter API:** `getConfirmedValue():boolean\|null`. **YALIN:** `deepScanRuntimeService`'e/SystemBoot'a OTOMATİK BAĞLANMAZ; polling timer YOK (pull `refresh()`/push `submitEvidence()`); import yan etkisiz; yeni bağımlılık yok. Manual override PROD varsayılan KAPALI (dev/test). Bounded (kanıt 16, listener 32); immutable (deep-freeze); privacy (reason `sanitizeText`, VIN/MAC/koordinat/ham/secret snapshot'a girmez); fail-soft (kaynak/listener hatası izole, throw yok). | `platform/deepScan/deepScanIgnitionSource.ts` (yeni), `platform/deepScan/index.ts` (export), `__tests__/deepScanIgnitionSource.test.ts` (yeni, 31 test) | Gerçek araçta (orchestration PR'ı bağlandıktan SONRA): (1) ACC/ON durumu doğru algılanır (gerçek native/CAN ignition kaynağı eklenince); (2) kontak kapalıyken `false` veya `unknown`; (3) RPM tek başına yanlış ON ÜRETMEZ; (4) OBD adaptörü takılı ama kontak kapalıyken ON üretmez; (5) kaynaklar çelişince `unknown`/null; (6) Deep Scan aktif sorguları yalnız doğrulanmış ON ile başlar; (7) BASIC_JS/Mali-400'de CPU/RAM regresyonu yok (saf çözümleyici, timer yok); (8) farklı MCU/CAN kutularında kaynak desteği AYRI raporlanır (şu an hiçbirinde doğrudan kaynak yok → daima `unknown`, fail-closed). ⚠️ **Runtime wiring + gerçek orchestration + doğrudan kaynak entegrasyonu SONRAKİ PR KAPSAMI.** Test edilmeden 🟢 yapma. | 2026-07-11 |
| 30 | **🔴 Device Validation Pending — Deep Scan Persistence Foundation** — Deep Scan sonuçlarını araç PARMAK İZİ HASH'ine bağlı, kalıcı, bounded ve fail-soft saklayan katman. `DeepScanPersistenceStore` (safeStorage anahtarı `car-deep-scan-history`, şema v1) araç başına bir `DeepScanRecord` tutar: mod/durum/zaman damgaları · `hasCompletedFullScan` · `completedScanCount`/`changeCheckCount` · keşif listeleri (ECU/PID/DID/firmware, normalize+unique+sort+dedup) · `newDiscoveriesCount`/`changedFirmware`/`changedEcu` · uyarılar · rapor özeti. **Mod kararı:** `resolveMode(hash)` — kayıt yok / tam tarama tamamlanmamış → `full_scan`; tam tarama tamamlanmış → `change_check` (runtime `resolveScanMode` ile UYUMLU). Sayaçlar yalnız GERÇEK tamamlanmada artar (`completeScan`, idempotent: aynı scanId iki kez saymaz; failed/cancelled artırmaz). **Bounded:** 16 araç LRU (tamamlanmamış/en eski önce evict, deterministik tie-break=hash, sıra-bağımsız) · ECU 128 · PID 512 · DID 512 · firmware 128 · uyarı 16. **Yazma politikası:** 5 sn debounce (progress burst'ları tek yazma), complete/dispose'da flush, eMMC dostu. **Gizlilik:** VIN(17-karakter kapısı)/MAC/koordinat/ham response/secret SAKLANMAZ; firmware yalnız ECU + normalize sürüm KİMLİĞİ (MAC/VIN kalıbı reddedilir); serbest metin `sanitizeText`. **Fail-soft:** bozuk JSON/eski şema → boş; tek bozuk kayıt diğerlerini bozmaz; hiçbir public API throw etmez; güvenli varsayılan `full_scan`. **Immutable:** çıktı deep-freeze, girdi mutate edilmez. **YALIN:** runtime servise/SystemBoot'a/Assistant Context'e OTOMATİK BAĞLANMAZ (yalnız ileride orchestration PR'ının kullanacağı temiz API); import yan etkisiz (disk yalnız ilk API çağrısında okunur); yeni bağımlılık yok; timer yalnız dirty durumda. ⚠️ **Gerçek scan orchestration + ignition wiring BU PR KAPSAMI DIŞI.** | `platform/deepScan/deepScanPersistence.ts` (yeni), `platform/deepScan/index.ts` (export), `utils/safeStorage.ts` (`car-deep-scan-history` → LRU_PROTECTED, additive), `__tests__/deepScanPersistence.test.ts` (yeni, 36 test) | Gerçek araçta (orchestration PR'ı bağlandıktan SONRA): (1) tam tarama tamamlanınca araç kaydı OLUŞUR; (2) uygulama restart sonrası kayıt GERİ GELİR (kalıcı); (3) aynı araç tekrar bağlanınca `change_check` seçilir; (4) farklı/yeni araç `full_scan` seçer; (5) bozuk kayıt Deep Scan'i ÇÖKERTMEZ (fail-soft); (6) disk yazımları debounce ile sınırlı (eMMC write-amplification yok); (7) 16 araç sınırı ve eviction doğru (tamamlanmamış/en eski önce); (8) BASIC_JS/Mali-400'de CPU/RAM/eMMC regresyonu yok (pasifken sıfır yük). ⚠️ **Kalıcılık BU PR'da; gerçek orchestration ve ignition wiring AYRI PR.** Test edilmeden 🟢 yapma. | 2026-07-11 |
| 29 | **🔴 Device Validation Pending — Deep Scan Runtime Service Foundation** — Yeni araç ilk kez bağlandığında çalışacak Deep Scan sürecinin merkezi runtime FOUNDATION katmanı: durum · faz · ilerleme · keşif sayaçları · olaylar · güvenli çalışma koşulları. `DeepScanRuntimeService` saf bir durum makinesidir — **araca TEK BİR sorgu bile göndermez**, hiçbir OBD/discovery/fingerprint/learning servisini import etmez (kaynak-metin kilidi: tek import `./deepScanModel`), timer/abonelik açmaz → **import edilmesi yan etki üretmez**. 9 durum (idle · waiting_for_ignition · preparing · scanning · analyzing · completed · paused · cancelled · failed) + 12 faz; fazlar AKTİF (araca sorgu: kimlik/protokol/ECU/PID/DID/firmware) ve OFFLINE (analiz/fingerprint/knowledge/evidence/change/rapor) olarak ayrılır. **Mod kararı:** önceki tamamlanmış tarama yoksa `FULL_SCAN`, varsa `CHANGE_CHECK` (aynı araca her bağlantıda tam tarama YOK). **KONTAK GÜVENLİĞİ (fail-closed):** depoda kontak/ACC yayan gerçek kaynak YOK (`IGNITION_OFF` yalnız mesaj sabiti, hiç emit edilmiyor) → `ignitionConfirmed` üç durumlu (`true`/`false`/`null=bilinmiyor`), yalnız `setIgnitionConfirmed()` ile dışarıdan beslenir, servis kontağı ASLA tahmin etmez; `true` değilse aktif faz açılmaz + aktif keşif kaydı reddedilir, yalnız offline fazlar çalışır. Progress deterministik + monotonik (0–100 clamp, geriye düşmez, complete→100, fail/cancel'da korunur, sahte ilerleme yok). Bounded: warnings(16) · listeners(32) · dedup anahtarları(4096). Snapshot + rapor + olay zarfı `Object.freeze`'li; VIN(17-karakter kapısı) · MAC · koordinat · ham CAN hex · API key snapshot'a giremez. **SystemBoot wiring YOK** (kilitli test) — gerçek tarama orchestration ve kalıcı geçmiş deposu AYRI PR kapsamı. | `platform/deepScan/deepScanModel.ts` (yeni), `platform/deepScan/deepScanRuntimeService.ts` (yeni), `platform/deepScan/index.ts` (yeni), `__tests__/deepScanRuntimeService.test.ts` (yeni, 57 test) | Gerçek araçta (orchestration PR'ı bağlandıktan SONRA): (1) **yeni araçta** `FULL_SCAN` + `isFirstScan=true` durumu oluşur; (2) **daha önce öğrenilmiş araçta** `CHANGE_CHECK` seçilir, tam tarama tekrarlanmaz; (3) kontak doğrulanmadan **aktif sorgu başlamaz** (status `waiting_for_ignition`'da kalır, ELM327 trafiği sıfır); (4) kontak kapalıyken araca sorgu **gönderilmez** (adb/ELM log'unda istek yok), yalnız analyzing/report fazları koşar; (5) progress monotonik — geri düşüş gözlenmez; (6) discovery sayaçları gerçek gözlemlerle **birebir** eşleşir (duplicate şişirmez); (7) cancel/pause/resume çalışır; (8) BASIC_JS tier'da CPU/FPS regresyonu yok (servis pasifken sıfır yük). ⚠️ **Restart sonrası kalıcılık BU PR'IN KAPSAMI DEĞİL** — `hasCompletedScanBefore` çağırandan gelir; süreç yeniden başlayınca durum `idle`'a döner. Test edilmeden 🟢 yapma. | 2026-07-10 |
| 28 | **🔴 Device Validation Pending — Unified Assistant Context (OfflineAssistantContext)** — Voice Assistant · Companion AI · Safety Kernel · Offline Conversation · Vehicle Learning · Diagnostic AI · Maintenance AI katmanlarının ayrı ayrı okuduğu kaynakları TEK merkezde toplayan, periyodik + `Object.freeze`'li snapshot yayınlayan store. SAF kurucu (`buildSnapshot`, throw etmez, girdiyi mutate etmez) + fail-soft canlı adaptör (`readLiveSources`, her kaynak izole try/catch). Periyot 5 sn · DeviceTier=low → 10 sn; görev `runtimeManager.scheduleTask` üstünde `criticality:'NORMAL' + deferIdle:true` → hot-path'e (3Hz hız/RPM) GİRMEZ. Dirty-flag ile park hâlinde sıfır maliyet; ağır soğuk-yol bölümleri (identity/learning) 30 sn (low: 60 sn) aralıkla okunur. Gizlilik: VIN/MAC/GPS/ham PID-DID-CAN snapshot'a ASLA girmez (whitelist kurucu; navigasyon hedefi yalnız ad, ≤64 karakter). Hiçbir servise YAZMAZ; severity/driveSafe/diagnostic kararı DEĞİŞMEZ. **Additive: henüz hiçbir tüketiciye ve SystemBoot'a bağlı DEĞİL** → bu PR'da çalışma zamanı davranışı değişmez. Kaynağı olmayan alanlar dürüstçe `null`: `deepScan.*` (genel Deep Scan servisi YOK), `driver.fatigueScore`, `status.ignition`, `media.ducking`, `network.wifi/mobile` (yalnız `navigator.connection.type` kesinse dolar). | `src/store/useAssistantContextStore.ts` (yeni), `src/__tests__/assistantContextStore.test.ts` (yeni, 25 test) | Gerçek araçta (tüketiciye bağlandıktan SONRA): (1) snapshot mid/high tier'da ~5 sn, BASIC_JS/low'da ~10 sn (veya mod çarpanıyla daha seyrek) aralıkla yenilenir; (2) park hâlinde (OBD sessiz) tik sıfır ayırma yapar — CPU profilinde snapshot işi görünmez; (3) BASIC_JS tier'da FPS/CPU regresyonu YOK, hız/RPM göstergesi 3Hz akıcılığını korur; (4) snapshot içeriğinde VIN/MAC/koordinat/ham hex GÖRÜNMEZ (Expert Mode / log denetimi); (5) uzun sürüşte (>1 sa) bellek büyümesi gözlenmez (yalnız son snapshot tutulur); (6) OBD/DTC/tanı servislerinden biri düşse bile snapshot üretilmeye devam eder. ⚠️ Test edilmeden 🟢'a çekme. | 2026-07-10 |
| 27 | **🔴 Device Validation Pending — Companion Safety Kernel & Response Verifier (PR-A)** — Asistanın online AI'dan BAĞIMSIZ, yerel + deterministik güvenlik katmanı. `assistantSafetyKernel` iki SAF fonksiyon (enjekte `SafetyContext`, throw etmez) + bir fail-soft canlı adaptör: **PRE-GATE** (`evaluatePreGate`) kritik durumda (motor aşırı ısı ≥118°C · yağ basıncı · fren · şanzıman · şarj/düşük voltaj <11.8V · tanı driveSafe=false veya severity=critical · uykululuk · reverse · cognitive≥PROTECTION) online çağrıyı ENGELLER → aktif fiziksel arızada doğrulanmış yerel şablon (`SAFETY_TEMPLATES`, mevcut `safety-overheat`/`safety-battery-oil` klipleri), cognitive/kernel-hatada offline'a düşer (fail-closed). **POST-GATE** (`verifyResponse`) online CHAT cevabını doğrular: kritik bağlamda tamamen atıp deterministic ile değiştirir · driveSafe=false'ta yanlış rahatlatma/telemetri çelişkisini reddeder · VIN/koordinat/JSON sızıntısını temizler · sürüşte kısaltır (≤200) parkta uzun (≤400). severity/driveSafe/diagnostic kararı DEĞİŞMEZ (yalnız okunur); online model kararı YÜKSELTEMEZ. `companionChatProvider`'a iki kanca (PRE/POST), **sağlayıcı sırası (Gemini→Groq→Haiku) ve grounding DEĞİŞMEZ**; xAI Grok/router/offline sohbet YOK. Girdi mutate edilmez, fail-soft, zero-alloc dostu. | `assistant/assistantSafetyKernel.ts` (yeni), `companion/companionChatProvider.ts` (PRE/POST wrapper + `companion_safety` route; iç zincir `runCompanionBrain`/`runCompanionChat`'e `allowOnline` geçidi), `assistantSafetyKernel.test.ts` (yeni, 30 test) | Gerçek araçta: (1) kritik hararet durumunda asistana soru sorulunca **online çağrı YAPILMAZ** (ağ log'unda istek yok) + deterministic güvenlik mesajı gelir; (2) online model "önemli değil/güvenle devam" dese bile kullanıcıya GEÇMEZ; (3) driveSafe/severity kararı değişmez; (4) TTS güvenlik mesajı gecikmeden çalar; (5) BASIC_JS tier'da FPS/CPU regresyonu yok; (6) kritik olmayan normal sohbet eskisi gibi online'a gider (sağlayıcı sırası korunur). ⚠️ Test edilmeden 🟢'a çekme. | 2026-07-10 |
| 26 | **🔴 Device Validation Pending — Evidence Write Bridge (P2-6)** — P2-1→P2-5 öğrenme katmanları kuruldu ama Evidence Store'a runtime YAZICI yoktu (Integration Service yalnız `list()` okuyordu) → learning rozetleri / Expert özeti / diagnostic learning context BOŞ kalıyordu. Bu köprü eksik write-path'i bağlar: VKB güncellenince (discovery gözlemi + VID kimlik değişimi abonelikleri, debounce'lu cold-path) `vehicleLearningEngine.computeEvidence()` çağrılır → üretilen kanıtlar Evidence Store'a `save()` (idempotent reproject — createdAt korunur, observationCount ŞİŞMEZ, vehicleCount distinct) ile yazılır → Pattern/Integration Service `list()` ile okuyabilir. 3Hz OBD hot-path'e GİRMEZ; yalnız anlamlı kimlik/discovery değişiminde tetiklenir; BALANCED/HIGH 8sn, BASIC_JS(low) 20sn debounce; kapanışta flush + dispose; fail-soft (köprü hatası OBD/Discovery'ye SIZMAZ); bounded(store 512 LRU) + throttle'lı yazma + zero-leak. Confidence/decay/pattern-promotion FORMÜLLERİ · Discovery capture/queue · Fingerprint · Auto Learning · VKB · Manufacturer Intelligence · Diagnostic safety/severity/driveSafe · PID/DID registry DEĞİŞMEZ. Cloud/SQL/LLM/Native YOK. | `vehicleLearningEvidenceBridge.ts` (yeni), `system/SystemBoot.ts` (VKB'den SONRA cold-path wiring, `_reg` cleanup), `vehicleLearningEvidenceBridge.test.ts` (yeni, 12 test) | Gerçek araçta: (1) discovery sonrası Evidence Store DOLUYOR (list() boş değil); (2) uygulama restart sonrası evidence geri geliyor (kalıcı); (3) Dashboard learning rozeti görünüyor; (4) Expert "Araç Öğrenme" sayıları >0; (5) Diagnostic learning context doluyor; (6) duplicate gözlem vehicleCount'i ŞİŞİRMİYOR (distinct fingerprint); (7) BASIC_JS tier'da FPS/CPU regresyonu YOK (seyrek debounce + cold-path). ⚠️ Test edilmeden 🟢'a çekme. | 2026-07-10 |
| 25 | **🔴 Device Validation Pending — Learning Integration Layer (P2-5)** — P2-1→P2-4 öğrenme katmanları (Evidence Store · Decay · Pattern Engine) uygulamanın mevcut ekran/tanı akışlarına SALT-OKUNUR bağlandı. `vehicleLearningIntegrationService` Evidence Store'dan okur → decay uygular → pattern üretir → (a) Discovery Dashboard'a WEAK/CANDIDATE/STRONG/MANUAL_REVIEW/STALE/CONFLICT rozetleri + confidence/decay/araç/gözlem/ECU alanları + Strong/Candidate/Manual/Conflict filtreleri ekler, (b) Expert Mode'a "Araç Öğrenme" özet bölümü (toplam/weak/candidate/strong/manual/stale/prune + güçlü PID/DID adayları + marka cluster + son öğrenme) ekler, (c) Diagnostic Insight için `enrichInsight` ile öğrenme bağlamı üretir. Hiçbir registry/profile/store'a OTOMATİK YAZMAZ; safety/severity/driveSafe DEĞİŞMEZ; memoized + bounded(512) + fail-soft + zero-leak. BASIC_JS(low) tier yalnız özet istatistik (ağır pattern/conflict detayı kapalı). | `vehicleLearningIntegrationService.ts` (yeni), `discoveryDashboardModel.ts` (öğrenme filtreleri + AnnotationResolver), `DiscoveryDashboard.tsx` (rozet/alan/filtre), `settings/expert/VehicleLearningInspector.tsx` (yeni), `ExpertModePanel.tsx` (bölüm wiring) | Gerçek araçta: (1) yeni PID/DID discovery oluşur → Evidence Store kaydı oluşur → Dashboard'da doğru WEAK/CANDIDATE/STRONG rozeti görünür; (2) duplicate gözlem vehicleCount'i şişirmez (distinct fingerprint); (3) Expert "Araç Öğrenme" özeti doğru sayıları gösterir; (4) Diagnostic Insight öğrenme bağlamını (learnedEvidenceCount/relatedStrongPids/requiresManualReview) gösterir; (5) düşük cihaz tier'ında (BASIC_JS) FPS/CPU regresyonu oluşmaz (ağır pattern detayı kapalı). ⚠️ Salt-okunur/additive → alt katman davranışı değişmez (tsc+test+build yeşil). | 2026-07-10 |
| 24 | **🔴 Device Validation Pending — P0-Güvenlik #4: production log hijyeni** (commit 049e290). terser `drop_console:true` (error+warn dahil TÜM console'u siliyor → üretimde hata gözlemlenebilirliği yoktu + logGate ölü kod) yerine dev-aware kesim: release'te yalnız `log/info/debug/trace` çıkarılır, `error`+`warn` KORUNUR (logGate runtime'da adaptif susturur). `NODE_ENV=development` build hiçbir console'ı çıkarmaz. Build-analiz kanıtı: release bundle'da `console.error(`/`warn(` var, `console.log(`/`info(`/`debug(` **çağrı = 0**; dev bundle'da log/info çağrıları korunur. | `vite.config.ts` (`_isDevBuild` + `drop_console: _isDevBuild ? false : ['log','info','debug','trace']`) | **Release APK** cihaz-üstü (debug build ile Chrome inspector): bir hata durumunda `console.error` GÖRÜNÜR (üretim gözlemlenebilirliği geri geldi); debug seviye log GÖRÜNMEZ. Debug APK'da tüm console akar (on-device debug korundu). Hot-path'te debug log kaynaklı ek CPU/IO gözlenmez. | 2026-07-08 |
| 23 | **🔴 Device Validation Pending — P0-Güvenlik #2: cleartext trafik kısıtlama** (commit 77e5292). Global `android:usesCleartextTraffic="true"` kaldırıldı; `network_security_config` base-config ile tüm cleartext reddedilir, yalnız loopback (`localhost`/`127.0.0.1`) whitelist'te. Ölü dev IP `192.168.1.12` çıkarıldı. Ham `ServerSocket`'ler (passenger 8765 / diag 8899) bu politikadan etkilenmez. | `AndroidManifest.xml`, `res/xml/network_security_config.xml` | **Release APK**'da dış **https** API'ler (hava/Nominatim/Supabase/AI) çalışmaya devam eder; kasıtlı bir `http://` dış çağrı NSC ile REDDEDİLİR; cihaz-içi OSRM (`http://localhost:5000`) offline routing + yolcu paneli (ayrı telefonun tarayıcısı) çalışır. | 2026-07-08 |
| 22 | **🔴 Device Validation Pending — P0-Güvenlik #1: teşhis HTTP sunucusu + ADB uçları release'te kapalı** (commit 8786764). `startDiagServer` release'te güvenli no-op; `/enable-adb` (root ADB) + `/root-check` uçları `BuildConfig.DEBUG` guard'ı altında → release'te `DEBUG=false` → R8 blokları **ve** yalnız onlarda kullanılan `runRootShell`'i APK'dan siler. DTCPanel çağrısı `import.meta.env.DEV` ile ikinci katman. BuildConfig üretimi build.gradle'de açıldı (debug=true / release=false doğrulandı). | `build.gradle` (buildConfig), `CarLauncherPlugin.java` (no-op + 2 endpoint guard), `DTCPanel.tsx` (DEV guard) | **Release APK**'da DTC paneli açıkken PC'den `http://<ip>:8899/` → **bağlantı reddi** (soket açılmaz); `strings`/apktool ile `enable-adb` / `service.adb.tcp.port` literal'i APK'da **bulunamaz**. **Debug APK**'da aynı uçlar ÇALIŞIR (saha teşhis workflow'u korundu). | 2026-07-08 |
| 21 | **OBD tanı raporu — socket-level hata kategorisi (C)** — saha ihtiyacı (2026-07-07): `OBD_CONNECT_FAIL "Her iki transport ile bağlantı başarısız"` raporu, NEDENİ (busy/refused/timeout…) taşımadığından "tekel mi, kontak mı, gerçek bug mı" adb olmadan anlaşılamıyordu. FIX: `classifyObdErrorReason(err)` — native soket hatasını (OBDManager `e.getMessage()` → `call.reject(code,msg)` → JS `.message`) PII-GÜVENLİ sabit kategoriye çevirir: `resource_busy`(başka app tutuyor)/`no_vehicle_response`(kontak/ECU uyku)/`connection_refused`/`socket_closed`/`broken_pipe`/`read_failed`/`permission_denied`/`timeout`/`bt_disabled`/`device_not_found`/`other`. `OBD_CONNECT_FAIL`+`OBD_HANDSHAKE_FAIL` emit'lerine `reason` alanı + msg'e `· <reason>` eklenir. Ham mesaj (cihaz adı/MAC) ASLA yayılmaz. | `obdDiagEmitter.ts` (`classifyObdErrorReason` + `reason` alanı + msg zenginleştirme), `obdService.ts` (3 emit noktası: connect-tcp/connect-classic+ble/handshake), `obdErrorClassify.test.ts` (yeni, 13: kategori eşleme + **PII sızıntı kilidi**), `obdDiagEvents.test.ts` (reason güncel) | Sideload sonrası: bağlantı başarısız olunca tanı raporunda `OBD_CONNECT_FAIL` yanında **neden kategorisi** görünür (ör. `· resource_busy` → Car Scanner açık; `· no_vehicle_response` → kontak/ECU; `· socket_closed` → transport düştü). Kategori doğru + rapor cihaz adı/MAC içermez. ⚠️ head unit adb'siz → sideload gerekli. | 2026-07-07 |
| 20 | **OBD ilk-bağlantı hızlandırma (A) — verified-transport-persist** — saha kanıtı (2026-07-07, Renault head unit, BT ELM327, fotoğraf: OBD bağlı + canlı PID akıyor): bağlantı ÇALIŞIYOR ama her boot `classic+ble` denenip ~15s BLE-first turu boşa harcanıyor (`OBD_CONNECT_FAIL` logları). Kök (kod-kanıtlı): `_transportConfirmed` **oturum-içi** bayrağı her boot `false` → diskteki persisted `classic` yok sayılıp BLE ÖNCE deneniyor. FIX: yeni `obd:transportVerified` persist bayrağı — YALNIZ canlı PID verisi (data-gate `_dataGatePassed`) geçilince '1' yazılır → boot'ta persisted+verified transport DOĞRUDAN primary denenir (BLE turu atlanır). Doğrulanmamış (dual-mod tahmin, veri akmamış) → mevcut güvenli BLE-first fallback KORUNUR (yanlış yönlendirme yok). `saveObdTransport` verified'ı sıfırlar, `clearObdTransport` (adaptör değişimi) ikisini de siler. | `obdStorage.ts` (verified persist API + saveObdTransport reset + clear), `obdService.ts` (`_lastTransportVerified` init + `_trustPersisted`/`_directPrimary` connect-flow + data-gate write + transport-değişimi/clear reset), `obdCoreV2.patch10.tcp.test.ts` (+3: verified round-trip/reset/clear), `obdDiagEvents.test.ts` (mock) | Gerçek araçta (BT ELM327, kontak açık): İLK boot yine öğrenir (classic+ble). **2. boottan itibaren** log `classic+ble` yerine doğrudan `classic` + `elapsedMs` ~15s'ten <birkaç saniyeye düşer (BLE turu YOK). Adaptör değişince (RFCOMM hiç açılmaz) verified silinir → tam BLE-first fallback'e döner. ⚠️ head unit adb'siz → CİHAZDA DOĞRULANMADI (tsc+238 OBD testi+build yeşil). | 2026-07-07 |
| 19 | **Öncelikli bulgu triyajı** — tanı raporu artık ~15 ham bölüm taşıyor; insan hepsini taramasın diye cihazda çalışan bir kural motoru (`diagnosticTriage.ts`, 13 kural — health/boot+termal(çapraz)/bellek trendi/güç/füzyon+GPS(çapraz)/transport+obdDeep(çapraz)/netAi/selfTest/uiActivite/storageQueue+netAi(çapraz)/geofence/GPS/DTC) bölümleri OKUYUP "EN KRİTİK BULGU" listesi (severity+başlık+NEDEN+AKSİYON, en fazla 8, critical>warning>info sıralı) üretir; `support_snapshot` payload'ına `triage` alanı olarak gömülür (selfTest/inspector dahil TÜM bölümler toplandıktan SONRA hesaplanır). Panel raporun EN TEPESİNDE renkli gösterir, bulgu yoksa "kritik bulgu yok ✓". | `diagnosticTriage.ts` (yeni, 13 kural + 4 çapraz-korelasyon), `remoteLogService.ts` (`_attachTriage`, 3 gönderim noktası: support/dev_inspector/self_test), `IncidentCenter.tsx` (`TriageSection`, raporun ilk bölümü), testler (diagnosticWidth +7: health-critical, boot+termal çapraz, boş-veri-sahte-bulgu-yok, PII kilidi, sıralama, tavan) | Taze APK'da gerçek raporun tepesinde bulgular doğru önceliklenmiş görünür (critical üstte), en az bir çapraz-korelasyon kuralı (ör. yavaş boot+termal L2, ya da sık OBD reconnect+düşük bağlantı kalitesi) gerçek veride tetiklenip 2 kaynak birlikte gösterir, veri eksikken (çoğu bölüm boş) "kritik bulgu yok" dürüst görünür (sahte bulgu yok). **🟢 KISMEN (2026-07-07 taze APK, CDP payload yakalama):** triage payload'da EN TEPEDE, `scanned:15` (15 bölüm okundu), topSeverity 'critical', 2 bulgu doğru önceliklenmiş (SELFTEST_FAIL critical > UI_UNTIMELY_SURFACE info) — robot ham bölümleri okuyup kök-neden+aksiyon üretiyor, sahte bulgu yok ✓. ANCAK çapraz-korelasyon kuralı tetiklenmedi (sağlıklı/araçsız ortamda ikili-arıza koşulu yok) → KALAN: gerçek dual-fault (yavaş boot+termal veya transport+obdDeep) ile ≥1 çapraz-korelasyon 2-kaynaklı bulgu doğrulanmalı. ⚠️ ROBOT BULGUSU (ayrı inceleme): zamansız-modal FAIL (`div z99999 %100 kullanıcı-dokunmadan`) muhtemelen FALSE-POSITIVE — pasif `pointer-events-none` z-100000 güvenlik katmanı / resume re-render'ı modal sayıyor olabilir (#12/#13 'kurt geldi' sınıfı); boşta-render WARN ~27fps maplibre = mevcut ekranda canlı harita (bağlam-bağımlı). | 2026-07-07 |
| 18 | **Tanı genişlik turu 3 — GÜÇ/AKÜ + SENSÖR FÜZYON + BOOT ZAMAN ÇİZELGESİ + TRANSPORT** (additive, mevcut #9/#11/#15 desenin devamı). (a) **GÜÇ/AKÜ**: 12V voltaj (CAN `canBatteryVolt` öncelikli, yoksa OBD PID 0x42 `batteryVoltage`) + rozet (kritik<11.8V / düşük<12.2V / normal / şarj>13.2V) + son 10sn min/max (BatteryProtectionService `_samples` yeniden kullanıldı — ani düşüş=marş işareti). (b) **SENSÖR FÜZYON**: worker'ın seçtiği aktif hız kaynağı (`useHALStatusStore.activeSource`) + GPS Doppler vs OBD ham hız farkı + zero-trust güven rozeti (fark≤5 yüksek/≤15 orta/üstü düşük, tek kaynakta 'unknown') + DR aktif mi. (c) **BOOT ZAMAN ÇİZELGESİ**: yeni `bootTimingRecorder.ts` — `SystemBoot.start()`'a yalnız ölçüm eklendi (Wave sırası/mantığı DEĞİŞMEDİ), her Wave süresi + toplam cold-start + en yavaş dalga. (d) **TRANSPORT**: aktif transport (CAN > classic/ble/tcp > none) + oturum reconnect sayısı + son kopma nedeni (`obdDiagEmitter` yeni `getLastObdDiagReason` — suppress penceresinden BAĞIMSIZ her zaman günceller). | `diagnosticSections.ts` (4 toplayıcı), `bootTimingRecorder.ts` (yeni), `power/BatteryProtectionService.ts` (`getVoltageStats`), `obdService.ts` (`getTransportStats`), `obdDiagEmitter.ts` (`getLastObdDiagReason`), `system/SystemBoot.ts` (Wave timing), `remoteLogService.ts` (payload), `IncidentCenter.tsx` (4 bölüm: PowerSection/FusionSection/BootTimingSection/TransportSection), testler (diagnosticWidth +34) | Taze APK'da tanı raporunda: (a) GÜÇ/AKÜ bölümü CAN veya OBD'den gerçek voltaj gösterir (araç kontağı açıkken ~12.6-14.5V, marş sırasında min/max'ta gözle görülür düşüş), (b) SENSÖR FÜZYON aktif kaynağı doğru gösterir (CAN/OBD bağlıyken HAL/CAN/OBD, yalnız telefon GPS'i varken GPS) + tünelde/GPS zayıfken DR aktif görünür, (c) BOOT ZAMAN ÇİZELGESİ 4 Wave'i gerçek ms değerleriyle listeler (toplam ~boot süresine yakın), en yavaş dalga işaretli, (d) TRANSPORT bağlıyken `connected:true` + doğru transport adı, adaptör koptuğunda reconnect sayısı artar + son kopma nedeni (ör. `OBD_STALE_DATA`) görünür. **🟢 DOĞRULANDI (2026-07-07 taze APK, CDP payload yakalama):** `self_test` payload'ında 4 bölüm de GERÇEK içerikle geldi — BOOT: Wave1 198ms / Wave2 13ms / Wave3 2088ms / Wave4 2695ms, toplam 4994ms, en yavaş Wave4 ✓; FÜZYON: gpsSpeedKmh 1.7 (GPS dolu), güven 'high', DR pasif ✓; GÜÇ: source 'none' / voltaj null — dürüst (araç yok) ✓; TRANSPORT: 'none', lastDisconnectReason 'OBD_NO_DEVICE' — dürüst ✓. Bekleyen (araç şart): gerçek voltaj + marş min/max düşüşü, bağlı transport `connected:true`, canlı reconnect artışı. | 2026-07-07 |
| 17 | **Sahte "Yolculuk Tamamlandı" banner (resume artefaktı) fix** — CANLI CİHAZDA tekrar-üretildi (Xiaomi, CDP+adb, 2026-07-07): app HOME/uykudan foreground'a dönünce park halde (0 km/h) sahte "Yolculuk Tamamlandı" banner açılıyordu (0.1km/2dk, tutarsız metrik — avgSpeed 11km/s ile mesafe uyuşmaz). Kök: dönüşte birikmiş GPS tek tick'te işlenip hız spike'ı ≥`DRIVE_ON_KMH`(5) üretiyor → worker `_handleEventSpeed(raw)` (ham hız, ekrandaki EMA-filtreli `display` değil) → sahte `DRIVING_STARTED`→`DRIVING_STOPPED` → Orchestrator `setTripSummary`. 60s stabil ön-planda 0 sahte trip → yalnız resume anı. UIFreeze(#16) ile aynı kök aile. FIX: Orchestrator resume-guard — `visibilitychange` visible → `_lastResumeAt`; `setTripSummary` öncesi `Date.now()-_lastResumeAt < RESUME_TRIP_GRACE_MS(5s)` ise banner bastır (trip yine kaydedilir). Zero-leak: listener teardown'da sökülür. | `system/SystemOrchestrator.ts` (`_lastResumeAt`+`_onOrchVisibility`+guard+teardown) + regresyon kilidi (guard 110) | Taze APK'da: aracı park et → app'i arka plana al / ekranı kilitle 30+sn → geri dön → park halde sahte "Yolculuk Tamamlandı" banner **AÇILMAZ** (eskiden dönüşte açılıyordu). Gerçek yolculuk (>5s süren sürüş sonrası duruş) banner'ı HÂLÂ açar. **🟡 KISMEN (2026-07-07, Xiaomi zircon telefon, CDP-over-adb):** taze APK'da HOME→30s→foreground + HOME→90s→foreground iki döngü — banner **AÇILMADI** (0 sahte banner) + visibility olayları teyitli (hidden→visible), REGRESYON YOK ✅. ANCAK sahte trip HİÇ oluşmadı (trip-log 0 kayıt) → guard fiilen tetiklenmedi: füzyon GPS ~1.7 km/h jitter < `DRIVE_ON_KMH`(5). Sabit/kapalı-mekan telefon + güvenli kilit (uyku→PIN, kimlik yok) → asıl fault üretilemedi, #16/UIFreeze ile aynı ortam-kısıtı. KALAN: hareketli araç / gerçek GPS ile resume-fault üretilip guard'ın banner'ı bastırdığı (trip yine kaydedilir) doğrulanmalı. | 2026-07-07 |
| 16 | **HealthMonitor:UIFreeze arka-plan/uyku YANLIŞ POZİTİF fix** — saha raporu (2026-07-07) `UI thread frozen for 210.3s` bastı. Kök: UI watchdog `setInterval` DRIFT ölçüyor; yorumda "arka planda sahte donma üretmez" yazıyordu ama Android WebView app backgrounded/ekran-kapalı olunca setInterval'i de ASKIYA alır → app dönünce tek tick 210s boşluk ölçüp sahte donma + `capturePanicSnapshot` + thermal panic marker + escalation Step 0 tetikliyordu (temiz 210s+devam = askı, gerçek foreground donması ANR'ye giderdi). FIX: `visibilitychange` (iki yönde) → sonraki ölçümü ATLA + taban sıfırla; ayrıca tick'te `document.hidden` guard. Gerçek foreground donması görünürlüğü değiştirmez → yakalanmaya devam. Zero-leak: listener stop()'ta sökülür. | `system/SystemHealthMonitor.ts` (`_uiVisibilitySuspect`+`_onUiVisibility`+tick guard+teardown) | Taze APK'da: uygulamayı arka plana al / ekranı kapat 30+sn → geri dön → OLAY İZİ'nde `UIFreeze` **BASILMAZ** (eskiden askı süresi kadar sahte "frozen Xs"). Gerçek foreground donması (varsa) hâlâ yakalanır. ⚠️ regresyon testi yazılmadı (timer+visibility flake riski) — cihaz kanıtı şart. **🟡 KISMEN (2026-07-07, Xiaomi zircon telefon, CDP-over-adb):** 2 tur ölçüldü (HOME+45s, EKRAN-KAPAT+45s) — enjekte 8100ms gap-probe max ~9.0s (normal peryot) → **WebView HİÇ askıya ALINMADI** çünkü app `SCREEN_BRIGHT_WAKE_LOCK` (pkg=com.cockpitos.pro) tutuyor → ekran uyanık kalıyor, timer donmuyor. Sonuç: **0 sahte-freeze** (regresyon YOK ✅) AMA fix'in ASIL dalı (askı→devasa gap→atla) bu cihazda ÜRETİLEMEDİ. Asıl 210s bug'ı T507 head unit sahasından; oradaki OEM WebView'i wake-lock'a rağmen donduruyor. **KALAN: T507'de gerçek kullanım (park→uyku→dönüş) sonrası Tanı raporunda `UIFreeze` basılmaması.** | 2026-07-07 |
| 15 | **Tanı raporu 4 yeni bölüm + boşta-render prob self-teşhis + GPS-health çelişki fix** — (a) boşta-render probu döngü tespitinde rAF çağıranın stack karesi + MapLibre canvas sayısını rapora ekler (`kaynak≈tick@FullMapView.tsx:814 [maplibre×1]`) → 47fps gizli döngünün suçlusu TAHMİN edilmeden cihaz raporundan okunur. (b) 4 yeni bölüm: **GPS DERİN** (izin/fix-yaşı/doğruluk/DR — KOORDİNAT YOK), **SESLİ/STT** (vosk hazır/wake/son-STT — transkript YOK), **GEOFENCE** (readState/zone sayısı/cloudSync), **DEPOLAMA+KUYRUK** (kota%/bekleyen telemetri). (c) 🐛 **GPS-health çelişkisi fix**: sağlık monitörü izin reddedilse bile `GPS healthy:true` gösteriyordu (saha raporu çelişkisi) → STARTUP_GRACE (45s) sonrası izin 'denied' ise `healthy:false`+`unhealthyReason:'gps_permission_denied'`. | `selfTestEngine.ts` (prob stack örnekleme), `diagnosticSections.ts` (4 toplayıcı), `security/geofenceService.ts` (getGeofenceStatus), `wakeWordService.ts` (isVoskModelReady), `voiceService.ts` (getLastSttOutcome), `remoteLogService.ts` (payload), `system/SystemHealthMonitor.ts` (GPS-denied fix), `IncidentCenter.tsx` (4 bölüm), testler (diagnosticWidth + appVersion, +35) | Taze APK'da: (a) 47fps raporunda `kaynak≈...` görünür → suçlu isimlenir, (b) 4 yeni renkli bölüm panelde dolu gelir, (c) GPS izni reddedilince SAĞLIK'ta GPS `healthy:false` (artık `healthy:true` çelişkisi YOK) + mahremiyet: GPS bölümünde koordinat YOK. **🟢 KISMEN (2026-07-07 taze APK): 4 bölüm de panelde DOLU geldi** — GPS DERİN (izin:granted, fix 166ms, 3m, koordinat YOK ✓), SESLİ/STT (vosk:hazır), GEOFENCE (readState:ok), DEPOLAMA+KUYRUK (0/0). Bekleyen: (a) idle-render `kaynak≈` satırı bu raporda 47fps olmadığı için tetiklenmedi (boşta ~2fps — iyi), (b) GPS-health-denied fix bu raporda izin GRANTED olduğundan test edilemedi. | 2026-07-07 |
| 13 | **Modal avcısı opacity:0 sızıntısı (fix-2) + geofence sonsuz-retry (robotun bulduğu 2 bulgu)** — (a) #12'deki modal fix'i opacity guard'ı `parseFloat(op)\|\|1` yazmıştı; op=0 falsy→1 olduğu için **opacity:0 KAPALI drawer'lar** (DrawerShell dış div inset:0 tam-ekran opacity:0) hâlâ "açık modal" sayılıyordu → FIX: `Number.isFinite(op)&&op<0.05`. (b) Robotun `[object Object]` fix'i geofence'in GERÇEK hatasını gösterdi: `vehicle_geofences` tablosu DEPLOY EDİLMEMİŞ (migration'da yok) → `PGRST205` her 60sn sonsuz retry (ağ+log spam) → FIX: kalıcı şema hatasında (PGRST205/PGRST202/42P01) retry ETME (servis canlı kalır, beyhude döngü durur) | `uiActivityRecorder.ts` (opacity Number.isFinite), `security/geofenceService.ts` (permanent-error no-retry) + testler (uiActivity +1 opacity regresyon, diagnosticWidth +1 geofence yapısal) | Cihazda 3. raporda: (a) UI AKTİVİTE "açık yüzey" 12'den gerçek görünür overlay'e (0-2) düşer; `10 ZAMANSIZ modal` gürültüsü biter, (b) geofence hatası OLAY İZİ'nde en fazla 1-2 kez görünür (boot'ta), her dakika TEKRAR ETMEZ | 2026-07-06 |
| 12 | **Tanı kalite turu — robotun bulduğu 3 gürültü/hata düzeltildi** (2. cihaz raporundan). (a) **Modal avcısı "kurt geldi"**: DrawerShell gibi yüzeyler KAPALIYKEN DOM'da kalır (`transform:translateY(100%)`→ekran-dışı) ama `getBoundingClientRect` öteleme'yi saymaz→ham alan %100→14 kapalı drawer "açık modal" sayılıyordu. FIX: viewport GÖRÜNÜR-KESİŞİM alanı + visibility/display/opacity guard'ı (`uiActivityRecorder.classifySurface`). (b) **`[object Object]` hataları**: `logError` Error-olmayan objeyi `String()` ile "[object Object]" yapıyordu (geofence Supabase PostgrestError izde okunmazdı). FIX: `_errToMsg` message/code/details/hint çıkarır→JSON→String (proje geneli). (c) **Mod flapping**: iz tek eşikle (speed>5) hız titreyince ~1-2sn'de bir sürüş/park satırı üretip izi boğuyordu. FIX: histerezis bandı (ON≥8/OFF≤3) + 4sn min-dwell. | `uiActivityRecorder.ts`, `crashLogger.ts`, `diagnosticTrail.ts` + testler (uiActivity +2, diagnosticWidth +2, diagnosticTrail +1) | 🟡 KISMEN (2. cihaz raporu 2026-07-06): (b) `[object Object]` GİTTİ — geofence hatası artık **gerçek mesaj**: `Could not find the table 'public.vehicle_geofences' [PGRST205]` 🟢, (c) mod flapping GİTTİ — iz temiz 🟢. AMA (a) modal avcısı HÂLÂ 12 gösterdi → **ilk fix'te bug**: opacity guard `parseFloat(op)||1` yazılmış, op=0 falsy→`0\|\|1`=1→opacity:0 KAPALI drawer (DrawerShell dış div inset:0 opacity:0) SIZDI. FIX 2: `Number.isFinite(op)&&op<0.05` (#13). 3. rapor doğrulaması bekliyor | 2026-07-06 |
| 11 | **Tanı genişliği turu 2 — OBD DERİN + PERF ZAMAN SERİSİ + AĞ/AI SAĞLIĞI + sesli/medya izi** ("hepsini ekle"). (a) `obdDeep`: adaptör/kaynak + sensör tazeliği (getObdHealth reliability%) + canlı sinyaller (speed/rpm/temp/fuel…) + keşfedilen extended PID örnekleri (yaş damgalı) + DTC arıza kodları (severity renkli). (b) `perfSeries`: oturum boyu halka tamponu (12s örnekleme) termal/bellek/fps/lag → panelde ASCII sparkline trend (bellek artışı=sızıntı işareti). Düşük-tier'da fps salvosu ATLANIR. (c) `netAi`: online + AI devre kesici + sağlayıcı 429/kota pencereleri (Gemini/Groq/Haiku ayrı). (d) sesli komut + medya-çal olayları `pushTrail`'e bağlandı (PII yok — intent tipi/kaynak, başlık/metin DEĞİL) | `diagnosticSections.ts` (OBD+netAi), `perfSeriesRecorder.ts` (yeni), `aiHealth.ts`/`companionChatProvider.ts`/`dtcService.ts` (snapshot getter'lar), `voiceService.ts`+`carosMediaLayer.ts` (trail), `remoteLogService.ts` (payload), `SystemBoot.ts` (perfSeries boot), `IncidentCenter.tsx` (3 yeni bölüm) | 🟡 KISMEN DOĞRULANDI (taze APK, tier:low telefon 2026-07-06): (a) OBD DERİN göründü — araç yokken "kaynak:none · durum:error · Extended PID keşif yapılmadı · DTC 0 hiç okunmadı" DÜRÜST boş ✅ [araç-bağlı PID/DTC BEKLİYOR], (b) PERF ZAMAN SERİSİ 18 örnek — termal 46°(L1) + bellek DÜZ 10MB (sızıntı yok) + fps "düşük-tier atlandı" ✅ + lag sparkline (spike görünür) ✅, (c) AĞ/AI SAĞLIĞI — ●online + AI devre kapalı + Gemini/Groq/Haiku "açık" ✅ [429 kırmızı BEKLİYOR — kota tetiklenmedi], (d) OLAY İZİ'nde mod geçişleri ✅ [sesli/medya action satırı BEKLİYOR — denenmedi]. KALAN: araçla OBD-derin, 429 anı, sesli/medya izi | 2026-07-06 |
| 1 | **ENGINE_OVERHEAT zinciri** (0x05 coolant → histerezisli olay → kırmızı alert + `safety-overheat.wav` + asistan) | `VehicleCompute.worker.ts`, `SystemOrchestrator.ts`, `GlobalAlert.tsx`, `VehicleEventHub.ts` (feat/obd-core-v2) | Motor >105°C'ye ısınınca: (a) kırmızı ısı uyarısı ekranda görünür, (b) "motor ısındı" sesli uyarısı çalar, (c) <100°C'ye düşünce uyarı kapanır (histerezis — titremez), (d) OBD/CAN sıcaklık sinyali yokken sahte uyarı ÜRETİLMEZ | 2026-07-06 |
| 9 | **Tanı genişlik backbone'u — olay izi (breadcrumb) + hata derinliği + 64KB cap** — `diagnosticTrail`: mod geçişleri (sürüş/park/geri-vites) + OBD bağlan/kopma (store aboneliği) + ekran değişimi (drawerBus) + snapshot anında crashLogger hataları & modal olayları TEK kronolojik zaman çizgisinde harmanlanır ("soruna ne yol açtı"). Payload'a `trail` + `uiActivity` eklendi; `lastErrors` 5→**15 + maskeli stack**. Sunucu payload limiti **16KB→64KB** (migration 027, canlıya uygulandı has_64k=true) — genişleyen rapor kırpılmadan sığsın | `diagnosticTrail.ts`, `SystemBoot.ts` Wave 1, `drawerBus.ts` (screen izi), `remoteLogService.ts` (payload+errors), `IncidentCenter.tsx` (TrailSection), `migration 027` | Cihazda: (a) `/admin/tani` detayında **OLAY İZİ** bölümü kronolojik (+Xs damgalı) mod/OBD/ekran/hata/modal satırları gösterir, (b) sürüşe geçiş → "sürüşe geçildi" izi; OBD takınca → "OBD kaynak none→bt" izi, (c) `lastErrors` 15 kayıt + stack (ham koordinat MASKELİ — konum sızmaz), (d) büyük rapor (self-test+trail+uiActivity+15 hata) 64KB'ye sığar, `metadata.truncated` OLMAZ, (e) iz PII'siz (yalnız olay türü+kısa etiket) | 2026-07-06 |
| 8 | **Zamansız-modal avcısı (UI aktivite kaydedici)** — modallar/overlay'ler merkezi store'dan değil ayrı bileşenlerden açıldığından, TEK bir `MutationObserver` ile (her bileşene dokunmadan) DOM'a binen yüksek-z-index `fixed` yüzeyleri merkezi izler; her açılışın bağlamını (hız/geri-vites/son-kullanıcı-dokunuşundan süre) kaydeder ve "zamansız" olanları işaretler (sürüşte / geri-viste / kullanıcı-dokunmadan >1.5sn / tekrar-tekrar flapping). Boot Wave 1'de EN ERKEN kurulur (disclaimer gibi boot-modalleri seed taramasıyla yakalar). Rapor `metadata.uiActivity`'ye gömülür + self-test `zamansız-modal` probu + `/admin/tani` "UI AKTİVİTE" bölümü | `uiActivityRecorder.ts`, `SystemBoot.ts` Wave 1, `remoteLogService.ts` (payload), `selfTestEngine.ts` (probe), `IncidentCenter.tsx` (UiActivitySection), `GlobalDiagnosticButton.tsx` (data-selftest-ignore) | Cihazda: (a) sürüş sırasında bir modal açılırsa robot raporunda `zamansız-modal` ✕ + "UI AKTİVİTE"de kırmızı ⚠ sürüşte satırı, (b) normal (park + dokunarak) açılan modal ZAMANSIZ işaretlenmez, (c) boot-time disclaimer açık yüzey olarak görünür, (d) tanı butonu (data-selftest-ignore) yüzey sayılmaz, (e) MutationObserver React churn'ünde ısı/kasma YAPMAZ (ucuz ön-filtre — pahalı getComputedStyle yalnız aday düğümlere), (f) düşük-tier'da gözle görülür ek yük yok | 2026-07-06 |
| 7 | **Sağlık false-critical — donanımsız cihazda (robotun bulduğu, FIX UYGULANDI)** `getGlobalHealthSnapshot` "healthy"yi yalnız ham heartbeat tazeliğinden hesaplıyordu → OBD'siz + konum-izinsiz HER cihaz tanıda `overallHealth:critical` gösteriyordu (VehicleDataLayer restart×2, GPS unhealthy; telefonda robot self-test ✕). **FIX:** rollup'a beklenen-yokluk sınıflandırması eklendi — pasif monitör (GPS/VehicleDataLayer) unhealthy'si, veri kaynağı yokken (`getOBDStatusSnapshot().source==='none'` + taze GPS fix yok, veya `gpsUnavailable`) `critical`'den DÜŞÜRÜLÜR; servis DETAYI ham/dürüst kalır. tsc temiz + guard 108 (yeni kilit: "Sağlık rollup false-critical") + 75 ilişkili test yeşil | `SystemHealthMonitor.ts` `getGlobalHealthSnapshot` + `regression.guards.test.ts` | Cihazda: (a) OBD/GPS'siz telefonda/tezgahta `overallHealth` artık `critical` DEĞİL (`degraded` — servis detayı yine healthy:false gösterir, dürüst), (b) OBD/GPS BAĞLI gerçek head unit'te `healthy`, (c) OBD bağlıyken kopunca (source≠'none') yine `critical` — gerçek arıza sinyali KORUNUR | 2026-07-06 |
| 6 | **Tanı Robotu — aktif self-test tarayıcı** (`selfTestEngine.runSelfTest`): global "Tanı Gönder" butonu artık pasif snapshot yerine **aktif tarama** koşturur — her alt sistemin kapısını izole+zaman-sınırlı+fail-soft yoklar (localStorage/IndexedDB, backend erişim, worker round-trip, ana-thread drift, OBD, izinler, servis-sağlığı, ekran-defteri) + **gizli-render performans probları** (canvas/WebGL bağlam sayısı, boşta-fps render döngüsü, gizli/düşük-tier animasyon, heap). Rapor `support_snapshot.metadata.selfTest`'e gömülür, `/admin/tani` detayında renkli (geçti/uyarı/kaldı) listelenir | `selfTestEngine.ts`, `remoteLogService.ts` (`reportSelfTestSnapshot`/`triggerSelfTestSnapshot`), `GlobalDiagnosticButton.tsx`, `IncidentCenter.tsx` (SelfTestSection) (feat/obd-core-v2) | 🟡 KISMEN DOĞRULANDI (2026-07-06, telefon): butona basınca "Tarama gönderildi ✓" + `/admin/tani`'de **TANI ROBOTU** bölümü 13 prob renkli göründü (✓9 !2 ✕1 ·1, tier:low, 806ms); worker 25ms + ana-thread 1ms + backend 80ms sağlıklı; robot GERÇEK bulgu çıkardı (sağlık-critical #7). ⚠️ **boşta-render probu KUSURLUYDU** (kendi rAF'ını çağırıp ~refresh fps yanılması=91fps false-warn) → DÜZELTİLDİ (geçici rAF-sarma, başka kodun frame isteğini sayar); düzeltilmiş prob cihazda RE-DOĞRULAMA bekliyor (boşta harita açıkken gerçek döngü varsa warn). KALAN: (c) düzeltilmiş boşta-render anlamlı mı, (f) eski WebView (Chrome 64-78) yedek animasyon taraması | 2026-07-06 |
| 2 | **Hibrit Runtime Scheduler** (§L.0, FAZ 13-16 grup-1): tek tik-wheel (`MASTER_TICK_MS=333`) + **periyot-tabanlı** `scheduleTask({periodMs,criticality:SAFETY\|NORMAL,deferIdle?})`; NORMAL görev düşük-tier'da mod çarpanıyla yavaşlar (BASIC_JS ×2, POWER_SAVE ×3, SAFE ×4), SAFETY muaf. **9 tüketici bağlandı:** vehicleIntelligence(500ms), smartCardEngine(8s), autoBrightness(60s,idle), breakReminder(30s), communityService(5dk,idle), hazard(10s), companionEngine(60s), arAlignment(500ms), carosMediaLayer(10s,idle) | `AdaptiveRuntimeManager.ts` (scheduler) + 9 servis (feat/obd-core-v2) | Düşük-uç head unit'te (BASIC_JS — K24/Duster): (a) vehicle-intel tick periyodu BALANCED ~666ms'den BASIC_JS ~999ms'e **yavaşlar** (net ~1.5× — 333ms grid yuvarlaması; adb log/CDP ile gözlemlenir), (b) 6 timer tek wheel'de → boşta uyanış sayısı + CPU/ısı ölçülebilir DÜŞER (ana-thread ~%67 idle tabanına göre iyileşme), (c) yüksek-tier'da hiçbir timer HIZLANMAZ (orijinal periyot korunur — community 5dk 5dk kalır), (d) hız/RPM 3Hz + güvenlik gecikmesi DEĞİŞMEZ, (e) dT/dt ısı-oranı (°C/dk) periyot değişse de DOĞRU (gerçek zaman damgası `_tempBufTsMs`) | 2026-07-06 |

---

## 🟢 CİHAZDA DOĞRULANDI (testten geçti)

| # | Özellik | Nasıl kanıtlandı | Doğrulandı |
|---|---------|------------------|------------|
| 14 | **Cloud geofence (güvenli bölge) — head unit yukarı senkronlar, UÇTAN UCA** — sürücü SecuritySuite'te bölge tanımlar → api_key auth'lu `push_geofence_zone` RPC → Supabase `vehicle_geofences` → head unit `get_geofence_zones` RPC ile OKUR → worker denetler. 🔒 anon'a tablo GRANT/`USING(true)` YOK; okuma SECURITY DEFINER RPC'den (yalnız kendi aracının bölgeleri). Migration 028 + 3 RPC + RLS. | **Taze APK cihazda (tier:low, 2026-07-07):** (OKUMA) boot sonrası PGRST205 KAYBOLDU, GÜVENLİ BÖLGE `readState:ok`+bulut senkron aktif; (YAZMA) SecuritySuite'te bölge tanımlandı → `bölge sayısı: 0→1` + taze boot sonrası HÂLÂ 1 (sunucuda kaldı, re-fetch edildi) → write+read+persist zinciri kanıtlandı. Migration 028 canlıya uygulandı+doğrulandı (anon-grant=0, RLS on, RPC=3, policy=1). | 2026-07-07 |
| 10 | **VehicleCompute worker "require is not defined" ÖLÜMÜ — robotun bulduğu, FIX DOĞRULANDI** — oxc es2015 class-field→`_defineProperty`→`require(@oxc-project/runtime)` (IIFE worker'da require yok) → worker satır-1 ölümü → araç veri katmanı ölü. Fix: `transformWithOxc` `assumptions.setPublicClassFields:true` + build-guard + regresyon kilidi (guard 109) | **Taze APK cihazda (tier:low, 2026-07-06):** OLAY İZİ'nde `require is not defined` / `VehicleCompute:onerror` / `No heartbeat` HEPSİ KAYBOLDU (önceki raporda ×4 vardı); `overallHealth:healthy`, VehicleDataLayer healthy:true restartCount:0, self-test worker round-trip 24ms sağlıklı. Fix ÖNCESİ↔SONRASI aynı cihazda birebir karşılaştırıldı | 2026-07-06 |
| B | **Backend yazma bug'ı — `push_vehicle_event` `text = uuid`** (migration 026): canlı `vehicle_events.vehicle_id` **TEXT** iken RPC'nin rate-limit sorgusu onu UUID değişkeniyle karşılaştırıyordu → `42883: operator does not exist: text = uuid` → **tüm log-tipleri** (support_snapshot/obd_diag/critical_error/voice_diag) İSTİSNA atıp hiç düşmüyordu; heartbeat/system_health (log-DIŞI) rate bloğunu atlayıp düşüyordu. Fix: rate sorgusu + INSERT'te `v_vehicle_id::text` | RPC yolu birebir test edildi: fix ÖNCESİ `push_vehicle_event(support_snapshot)` → `text=uuid` hatası; canlıya cast fix uygulandıktan SONRA aynı çağrı **UUID döndü** (event düştü). Management API ile doğrulandı. Repo: `migrations/20260706000026_*.sql` | 2026-07-06 |
| 3 | **Boot-time otomatik eşleştirme** (self-pair): `ensureDeviceRegistered()` boot'ta eşlenmemiş cihazı sessizce `register_vehicle` ile eşleştirir | Telefonda taze APK (self-pair'li, 17:40 build) ilk boot'ta **elle eşleştirme olmadan** `vehicles`'a kaydoldu → araç `5d8333eb` (18:35 UTC), sonrasında heartbeat + support_snapshot bu api_key'le aktı | 2026-07-06 |
| 4 | **Global "Tanı Gönder" butonu** (dilim 3): App.tsx köşe butonu, `triggerSupportSnapshot()` | Telefonda buton **"Tanı gönderildi ✓"** → (026 fix sonrası) `vehicle_events`'te `support_snapshot` satırı düştü (araç 5d83, appVersion 1.0.0, 18:49 UTC) | 2026-07-06 |
| 5 | **Tanı paneli — super_admin'siz** (`/admin/tani` + `get_recent_diagnostics` RPC) | Sıradan admin girişiyle (Selim, super_admin değil) `/admin/tani` açıldı → gerçek cihaz `support_snapshot` satırı listelendi + detay paneli SAĞLIK/OBD/OTA/SON KRİTİK gösterdi (konum/VIN/plaka YOK). Backend RPC Management API ile doğrulandı | 2026-07-06 |

---

## ❌ TEST EDİLDİ / DÜŞTÜ (geri dönüş gerekli)

| # | Özellik | Ne oldu (gözlemlenen hata) | Sonraki adım | Tarih |
|---|---------|----------------------------|--------------|-------|
| — | *(henüz yok)* | | | |

---

### Kullanım

- **Yeni özellik ekleyince:** üstteki 🔴 tabloya bir satır ekle — kabul ölçütünü
  *ölçülebilir* yaz ("çalışıyor" değil, "X koşulunda Y gözlemlenir").
- **Cihazda kanıtlayınca:** satırı 🟢 tabloya taşı, nasıl kanıtlandığını (log/ekran/ses) yaz.
- **Cihazda düşerse:** ❌ tabloya taşı, gözlemlenen hatayı ve sonraki adımı yaz.
- Bu kütük ile ilgili hafıza notu: `[[vision-vehicle-intelligence-os]]`.
