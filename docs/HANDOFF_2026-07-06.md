# ⭐ DEVİR / HANDOFF — 2026-07-06

> Yeni oturum bu dosyayı + `MEMORY.md` indeksini okuyarak **tam buradan** devam eder.
> Vizyon, anayasa, mimari, sıradaki iş — hepsi burada.

## 1. VİZYON (bağlayıcı)
CarOS Pro bir launcher değil, **aracın ikinci beyni** — aftermarket evrensel **Vehicle
Intelligence OS**. Referans Tesla DEĞİL (Tesla kendi aracını bilir; biz yüzlerce
*bilinmeyen* aracı öğreniriz → daha güçlü olmak zorundayız). **Zero-trust telemetry.**
**8 Kapı:** her sinyal → (1) doğru mu?Confidence (2) önemli mi?Rule (3) kullanıcı bilmeli
mi?Action (4) sadece sistem mi?Digital Twin (5) neyle birleşir?Fusion/Context (6) 5dk
sonra?Prediction (7) yerine karar?Intent/Brain (8) en doğru aksiyon?Vehicle Brain.
Tasarım testi: "Bu özellik Tesla'dan daha akıllı mı? Değilse yeniden tasarla."

## 2. ANAYASA REFORMU (yapıldı — commit YOK)
`STABILIZATION` → **`CONTROLLED EVOLUTION`**. Güncellenen dosyalar:
- `CLAUDE.md` — Vizyon Anayasası + Controlled Evolution + Performans-Uyarlanabilir Hibrit + Saha Kütüğü kuralı.
- `AI.md` — MODE reformu (vizyon-hizalı özellik serbest, hibrit bütçe altında).
- `GEMINI.md` — öncelik #7 "vizyon-hizalı özellik" + ANA GÖREV "runtime koruyarak VIOS'a dönüştür".
- `README.md` — "launcher/middleware" → "Vehicle Intelligence OS"; roadmap VIOS katmanları.
- **Performans-Uyarlanabilir Hibrit:** tüm katmanlar açık ama DeviceTier bütçesine abone;
  güvenlik-kritik her tier'da açık; ağır analiz cold-path/idle; hot-path'e (3Hz) dokunma;
  süslü görsel düşük-uçta feda. "Bütçesiz/kanıtsız özellik yasak."

## 3. SAHA DOĞRULAMA KÜTÜĞÜ (yeni)
`docs/DEVICE_VALIDATION_LEDGER.md` — 🔴 test edilmedi · 🟢 doğrulandı · ❌ düştü.
Her özellik cihazda kanıtlanana kadar 🔴. **🔴 bekleyen özellik "çalışıyor" diye sunulmaz.**

## 4. KODLANAN — ENGINE_OVERHEAT zinciri (FAZ 1)
0x05 coolant → histerezisli olay (ON 105°C / OFF 100°C) → kırmızı alert + `safety-overheat.wav`
+ asistan. 10 dosya değişti; **tsc temiz + 2129 test yeşil (doğrulandı).**
⚠️ **CİHAZDA DOĞRULANMADI** → kütükte 🔴 #1. Commit edilmedi (working tree, dal `feat/obd-core-v2`).

## 5. MİMARİ DOKÜMAN
`docs/CAROS_VEHICLE_INTELLIGENCE_ARCHITECTURE.md`:
Kuzey Yıldızı + §0 + §A-J (15 motor, PID tablosu, state machine, action matrix, güvenlik
preemption, AI katmanı, perf, genişleme, FAZ 0-6) + §K (6 ikinci-nesil katman + FAZ 7-12)
+ **§L YAZILDI** (satır 1167-1345, §L.0-L.4, FAZ 13-16). Doküman TAM.

### §L özeti (2026-07-06 yazıldı — caros-architect)
**§L Uygulama Mimarisi / Performans-Uyarlanabilir Hibrit Runtime.** Merkez tespit
(doğrulandı): ana-thread periyodik analiz için ortak tier-duyarlı zamanlayıcı YOK —
`vehicleIntelligenceService.ts:541` sabit `setInterval(_tick, TICK_MS=500)`, 20 platform
dosyasında 27 bağımsız setInterval, hiçbiri runtimeManager'a abone değil. §L.0 çözümü:
`AdaptiveRuntimeManager`'ı **genişleten** `scheduleTask({id, freqClass:HOT|WARM|COOL|IDLE,
criticality:SAFETY|NORMAL, fn}) → cleanup` + **tek "tick-wheel"** (HOT ~333ms master timer,
sayaçla WARM/COOL/IDLE tetikler). `runtimeManager.subscribe()` ile mod düşünce WARM/COOL/IDLE
ölçeklenir; HOT+SAFETY her tier'da sabit. İnvaryant uyumu: tek yazar · zero-alloc (#6) ·
Zero-Leak (#10) · safety preemption (#5). §L.1 sınıf↔katman eşlemesi, §L.2 migrasyon
(27 setInterval → tek scheduler, big-bang YOK), §L.3 Zero-Leak, §L.4 FAZ 13-16.

## 6. SIRADAKİ İŞ (§L bitti — sırada KODLAMA)
- **(a) FAZ 13 Scheduler iskeleti** = §L.0'ı koda dök: `AdaptiveRuntimeManager`'a
  `scheduleTask` + tek tick-wheel + `subscribe` mod-ölçekleme + `destroy` entegrasyonu.
  Tüketici taşınmaz. Ölçüt: BASIC_JS'te WARM periyodu ölçeklenir (birim test), soak
  sızıntısız, destroy sonrası timer=0. → FAZ 14 ilk migrasyon `vehicleIntelligenceService`.
- **(b) FAZ 7 Confidence Engine** kodlama (8 Kapı #1, aftermarket kilidi): füzyon-içi
  confidence'ı per-sinyal dışa açık API'ye terfi + "düşük güvende kararı bastır".
- **(c) ENGINE_OVERHEAT'i araçta doğrula** → ilk 🟢 (kütükte 🔴 #1 bekliyor).

## 7. Notlar
- Ajan/model otomatik yönlendir (CLAUDE.md tablosu); ajan çıktısı ana oturumda tsc/test ile doğrulanır.
- APK/commit yalnız kullanıcı isteyince.
- Architect ajanı `a748b47d691064ea8` durduruldu; yeni oturumda yeni ajan spawn edilir (eski oturum ajanı taşınmaz).
